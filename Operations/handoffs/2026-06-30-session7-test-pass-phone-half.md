---
description: Session 7 TEST pass phone-half (2026-06-30) — passes banked, bugs found (BOOK-1/2/3, MODEL-1, SMS-OBS-1, PHONE-CLEAR-1), overnight Text-Setter repair (v2 = ALL open bugs folded in, council-vetted) set up; remaining live items deferred.
---

# Session 7 — TEST pass, phone-half (2026-06-30)

Brendan-driven live sweep; Claude verified read-only. Continued the Session-7 TEST pass (UI half / LIVE-A was 2026-06-28). The big outcomes: the whole **voice call cluster + F9 lifecycle passed**, a **live-breaking bug (MODEL-1) was found + fixed**, and the **SMS-booking failure (BOOK-1) was root-caused** → it spawned a council-vetted **overnight Text-Setter repair session** (kickoff prompt at the bottom). The kickoff prompt below is the **v2** revision (council-vetted twice): it folds in **ALL open BUG_LIST items**, triaged by safety into fix-tonight / stage-on-branch / write-up / defer, keeps the deploy-nothing/branch-only guardrails, makes the build session run its **own planning + verification councils**, and **emits a tomorrow-testing kickoff prompt** at the end.

## Passed (→ COMPLETED_LOG)
- **B-4 (6.2)** client-role RLS + "My Account only" nav. Claude provisioned + deleted a throwaway client-role user; read-only RLS proof (own-agency user sees 0; cross-agency boundary solid). **Finding (by-design):** RLS is agency-scoped, not per-client; within one agency a client role is UI-scoped, not DB-isolated, from siblings (each real client = own agency via signup).
- **F2c** outbound calling E2E (cadence "Try-Gary: Property Coach" n3 phone_call → Property Coach; placed/dialed/correct agent/cadence advanced).
- **G3-3** outcome stamped + `active_call_id` cleared on `call_ended`.
- **6.12b** call-half (GHL Call Outcome=Answered / AI Summary / Last Call Date / Sentiment / Intent) + SMS-half (`last_sms_analyzed_at` advanced + SMS sentiment/intent on the contact).
- **F9 outbound-dials-while-locked** (call placed on the locked setter) + **F9 unlock** (rename cascaded to Retell + DB with NO 423 → full F9 lifecycle verified).
- **B4** call-side send-once (one dial). **Latency** acceptable (Trigger dequeue, tens of seconds).
- **B-3 (6.4)** phone-clear (BFD `phone=null` + GHL phone cleared).
- **6.11** voicemail/no-answer → fallback SMS ~9s after call end (not the old ~600s).
- Migrated LIVE-A UI passes: F2b, F6, B-6×2, F9 lock/bulk/Pull.

## Bugs found (→ BUG_LIST)
- **BOOK-1 (H)** — the SMS setter **fabricates "booked out / snapped up" and never books** against an OPEN calendar (live GHL = 180 free slots; the "full" wording exists nowhere in code → model invented it). Root structural cause: Text doesn't prefetch availability the way Voice does. **Fix = the overnight session below.**
- **BOOK-2 / BOOK-3 (L, shared-fn defer)** — `resolveCanonicalSlot` off-grid-minute false-negative; `toMs()` parses offset-less ISO as UTC (AU window skew). Both in the **shared** `voice-booking-tools` (zero tests, frozen Session-7 baseline) → write-up + characterization test only, supervised daytime edit.
- **SMS-OBS-1 (M)** — tool calls/responses not persisted (`processSetterReply.ts:344` `tool_calls:[]`) → booking failures are DB-blind. Persist them (overnight session's first deliverable).
- **MODEL-1-HARDENING (M)** — `clients.llm_model` was `google/gemini-flash-latest` (invalid OpenRouter id) → silently 400'd ALL SMS + cadence AI; `normalizeLlmModel` only strips `~`/whitespace. **Live value FIXED to `google/gemini-2.5-flash` via Mgmt API.** Hardening = validate the field.
- **PHONE-CLEAR-1 (L)** — clearing a lead's phone doesn't clear `leads.normalized_phone` → still inbound-/STOP-matchable by the old number.

## Live changes applied this session (Brendan-approved)
- `clients.llm_model` `google/gemini-flash-latest` → `google/gemini-2.5-flash` (MODEL-1 fix; unblocks SMS + cadence AI).
- `agent_settings.response_delay_seconds` all 7 BFD setters 60/82 → **12s** (SMS latency).
- (Transient, restored) Supabase PAT in `.env` expired mid-session → Brendan refreshed it.

## Still owed (→ Session 7-finish, after the overnight repair lands + is deployed)
B-5 (inbound from a non-CRM number), F1 (fresh GHL contact deep-link), LIVE-D (B-2 ×4 + manual-send/429), LIVE-E (F3/F4), G3-6 Tier-3 analytics, **3.12 SMS booking (blocked on BOOK-1)**, and live-verify of F11/UI-1/INB-1 + the overnight-staged fixes.

## Sequence from here (see `Docs/SESSION_PLAN.md`)
1. **TONIGHT — Session 7.5: overnight Text-Setter repair, ALL-BUGS v2** (prompt below). Branch-only, deploy-nothing; every open bug coded-or-staged on the branch + a deploy checklist for daylight.
2. **Fix pass — now largely ABSORBED into 7.5** (F9-1, VM-1, SMS-OBS-1, MODEL-1-HARDENING, PHONE-CLEAR-1, G3-8(a), BOOK-1 code all fold in). What truly remains as their own sessions: **API-DEPR-1** + **G3-7** (deferred — frozen-baseline multi-fn migration / breaking vite bump).
3. **Session 7-finish** — Brendan deploys the branch per the checklist (Voice-gated) + runs the remaining live TEST items + live-verifies the staged fixes.
4. **Session 8** — F8 cost-to-price calculator (PLAN mode).

Council verdict on the overnight session: **GO-with-changes** (twice). Key catches folded into the prompt: `voice-booking-tools` is shared with live Voice + the frozen Session-7 edge baseline (read-only tonight, no deploy); BOOK-2's HH:MM canonicalization is a deliberate load-bearing fix (don't "offset-fix"); no upstream git remote (reference = `n8n/exports/Text_Engine_REVERSE_ENGINEERED.md`); both prompts report-only; observability persistence first; spec-kit thin front-door only. v2 adds: a frozen per-bug ledger ratified by a planning council, an adversarial verification council before "done", a deploy-type discipline (Trigger redeploy ≠ Voice-gated edge deploy), and a truthful "staged not live" tomorrow prompt.

---

## Overnight Text-Setter repair — kickoff prompt, v2 ALL-BUGS (paste into a fresh session tonight)

```
BFD-setter — TEXT SETTER repair + FOLD-IN ALL OPEN BUGS (research → fix, own overnight session, UNSUPERVISED). Repo /srv/bfd/Projects/bfd-setter.

== WHAT THIS IS ==
This is research-then-REPAIR on an UNDERSTOOD pipeline. NOT a feature build, NOT a rewrite. Goal: guarantee a ready-to-book SMS lead ALWAYS reaches a real GHL booking by making the booking PLUMBING bulletproof and giving the model ground-truth availability — EVEN WHEN THE PROMPT IS WEAK. Match the Voice Setter on booking OUTCOME, not mechanism: keep Text async, debounced, message-grouped, STOP-interruptible. The trigger was the live finding that the SMS setter fabricates "booked out" against an OPEN calendar (BOOK-1).

== SCOPE EXPANSION (this revision): FOLD IN ALL OPEN BUGS, SAME GUARDRAILS ==
The Text-setter repair (BOOK-1 code + SMS-OBS-1 + BOOK-2/3 write-ups) remains the spine and is unchanged below. This revision ADDITIVELY folds in EVERY other open BUG_LIST item so nothing is dropped — WITHOUT relaxing any guardrail. The session stays BRANCH-ONLY / DEPLOY-NOTHING. "Folded in" means each bug is placed in exactly ONE of four classes (ratified by the PLANNING council before any code):
  [CODE-BRANCH-NODEPLOY] = text-safe / frontend / additive; code+TDD fully on the branch, NO deploy.
  [STAGE+DEPLOY] = needs an edge deploy or touches shared infra; code+tests on the branch, NOT deployed, + a deploy-checklist entry.
  [WRITEUP+CHAR-TEST] = shared-fn hazard; written finding + a characterization test of CURRENT behavior only, no edit.
  [DEFER] = explicit, one-line reason (e.g. a breaking dep bump, a frozen-baseline multi-fn migration needing live-API re-confirm).
Nothing may be silently dropped. The classes ONLY narrow after the planning gate (an item may demote to WRITEUP/DEFER); none may be promoted into an applied edit.

PROPOSED LEDGER (the PLANNING council RATIFIES or RECLASSIFIES this by reading the actual save path before coding; it is a default, not locked):
  BOOK-1 code  -> CODE-BRANCH-NODEPLOY (trigger/* prefetch+inject+anti-fabrication guard+tool_choice). Trigger redeploy = daytime checklist line, NOT Voice-gated.
  BOOK-1 prompt-> REPORT-ONLY (BRENDAN_TODO + project_pending_prompt_changes memory). Keep DISTINCT from the code half — do NOT silently absorb it.
  SMS-OBS-1    -> CODE-BRANCH-NODEPLOY, FIRST deliverable; new ADDITIVE table + migration FILE (NOT applied tonight). Fix processSetterReply.ts:344 tool_calls:[]. Gate the rest on it landing green.
  MODEL-1 (a) normalizeLlmModel in trigger/_shared/llmModel.ts -> STAGE+DEPLOY (Trigger runtime; characterization test; NOT zero-risk). (b) optional frontend save-validator -> CODE-BRANCH-NODEPLOY. Do NOT touch any live clients.llm_model (already remediated to google/gemini-2.5-flash). OpenRouter-list network lookup = written rec only (no network call overnight).
  F9-1         -> CODE-BRANCH-NODEPLOY, frontend-only. Lock state is at PromptManagement.tsx:5132 (lockMap)/5140 (isVoiceSlotLocked) — gate InlineSetterNameEditor.commitEdit; block the setter_display_names write when locked / reorder so the guarded set-agent-name runs first and surface the 423 body as an ERROR not a warning.
  VM-1         -> STAGE+DEPLOY, VOICE-REGRESSION GATED. retell-proxy/index.ts patchBody ~1573-1586: drop enable_voicemail_detection + voicemail_detection_timeout_ms, map intent onto voicemail_option, route through ensureEditableAgentDraft. retell-proxy is FROZEN v46 -> code+tests on branch, deploy daytime behind the Voice gate (v46->v47).
  API-DEPR-1   -> DEFER (own daytime session: FROZEN retell-proxy v46 + make-retell-outbound-call v27 + verify-credentials + elevenlabs-manage-agent + ~23 GHL Version callers + live-API re-confirm + multi-deploy). At most a READ-ONLY inventory tonight; do NOT edit retell-proxy.
  BOOK-2 / BOOK-3 -> WRITEUP+CHAR-TEST (shared voice-booking-tools, ZERO tests, frozen, EE1 fan-out precedent). Keep the offset-ignoring HH:MM match (load-bearing, live-proven) and the model contract STABLE tonight.
  PHONE-CLEAR-1-> STAGE+DEPLOY by default (server-side normalized_phone derive via _shared/leadResolve.ts / lead-insert.ts, shared with live by-phone/STOP ingress). The real UI write is ContactDetail.tsx updatePayload (~581-590) omitting normalized_phone (NOT LeadRow). FORK: a frontend-only recompute -> CODE-BRANCH-NODEPLOY; the planning council decides; chair recommends the server path for DRY/consistency.
  G3-7         -> DEFER (breaking vite 5.4->7/8 major bump; dev-server-only; no live exposure; out of scope). NO npm install / lockfile churn overnight. (The non-breaking dompurify/tar advisories are NOT G3-7; leave to that same future session.)
  G3-8(a)      -> STAGE+DEPLOY (LeadRow.tsx:126-160 forwards supabase_service_key to campaign.webhook_url; live via CampaignDetail.tsx:686 + ExecutionDashboard.tsx:299; needs a server-side reactivation edge fn FIRST, then repoint LeadRow). If the server path is too large to land cleanly+tested overnight, AUTO-DEMOTE to WRITEUP+design-note — never a half-applied secret-drop that breaks the live webhook.

== CONTEXT / CREDS ==
Branch off main (git pull first). Supabase ref bjgrgbgykvjrsuwwruoh. Creds in ./.env (SUPABASE_PAT, TRIGGER_DEPLOY_PAT, BFD_RETELL_API_KEY). Live DB via Supabase Management API /database/query with a BROWSER User-Agent (NOT postgres MCP, NOT for writes tonight). Live GHL via clients.ghl_api_key (a pit-... token; repo .env BFD_GHL_* are STALE). The TEXT setter prompt + chat history live in the CLIENT'S EXTERNAL Supabase (clients.supabase_url / supabase_service_key) in text_prompts.system_prompt (card_name "Setter-N") + chat_history. BFD client_id = e467dabc-57ee-416c-8831-83ecd9c7c925.

== ABSOLUTE GUARDRAILS (read twice; violating these is the failure mode) ==
1. DEPLOY NOTHING. No supabase functions deploy, no Trigger.dev deploy, no Retell PATCH/publish, no Supabase Management-API WRITE, no save-external-prompt call, no live SMS/outbound calls/GHL booking POSTs, no npm install / lockfile change. Work ONLY on an isolated git WORKTREE branch off clean main (e.g. overnight/text-setter-repair-allbugs). NEVER push/merge/deploy. End state = branch + docs + handoff for Brendan's daytime review.
2. FROZEN BASELINE: retell-proxy v46, make-retell-outbound-call v27, and the current live voice-booking-tools version MUST NOT be redeployed tonight — Session 7's PAUSED live TEST baseline depends on it. You may edit retell-proxy SOURCE on the branch for VM-1's set-voicemail handler ONLY, but the live versions stay frozen.
3. EDITABLE ALLOWLIST. The Text spine (edit + TDD): trigger/processSetterReply.ts, trigger/_shared/setterTools.ts, trigger/_shared/setterToolLoop.ts, trigger/_shared/llmModel.ts, their *.test.ts siblings, and a NEW tool-invocation-persistence migration/table. PLUS, for the folded-in bugs, edit + TDD ON THE BRANCH ONLY (none deployed tonight): frontend/src/components/setters/InlineSetterNameEditor.tsx (F9-1, frontend-only); frontend/supabase/functions/retell-proxy/index.ts — set-voicemail handler ONLY (VM-1; FROZEN v46 — edit SOURCE on the branch, do NOT deploy, do NOT touch any other handler, do NOT do the API-DEPR-1 list-endpoint migration here); the PHONE-CLEAR-1 path (server-side _shared/leadResolve.ts / lead-insert.ts OR the ContactDetail.tsx frontend recompute — per the council's fork decision; NOT both); the G3-8(a) reactivation path (a NEW/edited edge reactivation fn + repoint frontend/src/components/LeadRow.tsx) IF the council keeps it STAGE+DEPLOY; optionally a frontend save-time llm_model validator (MODEL-1 b); and *.test.ts siblings for all of the above.
   READ-ONLY TONIGHT (do NOT edit): frontend/supabase/functions/voice-booking-tools/**, make-retell-outbound-call/**, receive-twilio-sms/**, _shared/bfdVoiceTools.ts, ALL prompt content (Retell voice prompts AND the external text_prompts.system_prompt), and every retell-proxy handler EXCEPT VM-1's set-voicemail. API-DEPR-1 edits NOTHING tonight (at most a read-only inventory). voice-booking-tools is SHARED with the LIVE Voice path and has ZERO tests — treat it as hazardous-by-default (ref project_ee1_fanout_incident_2026_05_18: shared-surface fan-out wiped live data).
4. PROMPTS ARE REPORT-ONLY (both Retell voice AND external text_prompts — even though save-external-prompt makes the text one writable through our product). Do NOT edit prompt content. Stored-prompt weaknesses are REPORTED for Brendan (copy-paste-ready recommendation to BRENDAN_TODO + the project_pending_prompt_changes memory), never edited. Behavior is steered ONLY through editable code.
5. Do NOT change identity-injection order (contactId pinned LAST in setterToolLoop) — the EE1 cross-wiring guardrail.
6. Do NOT touch clients.llm_model or response_delay_seconds for real clients (MODEL-1 already remediated to google/gemini-2.5-flash). Any model/debounce change is a code-default or a written recommendation only.
7. Surgical only: every changed line traces to a numbered defect in the frozen ledger. No opportunistic refactor of the (already-robust) tool loop, no "cleanup" of the shared fn, no broad processMessages rewrite.
8. SCOPE-EXPANSION SAFETY (single-writer + deploy-type discipline). ONE coordinator owns ALL writes; subagents only get FILE-DISJOINT lanes and never edit the same file concurrently. retell-proxy/index.ts may be edited on the branch ONLY for VM-1's set-voicemail handler and MUST NOT be deployed; API-DEPR-1 must not touch it tonight (this removes the only real concurrent-edit hazard on that file). NO agent may run any deploy / network-write / Mgmt-API write / npm install / lockfile command. There are TWO deploy types and they must never be conflated on the checklist: (i) Trigger.dev redeploy (BOOK-1 code / SMS-OBS-1 / MODEL-1a) touches NO shared-Voice surface and leaves the frozen retell-proxy v46 + make-retell-outbound-call v27 baselines untouched -> NOT Voice-gated; (ii) edge-fn deploy — VM-1 bumps the FROZEN retell-proxy v46->v47 and PATCHes live voice agents -> Voice-regression GATED; PHONE-CLEAR-1 (server fork) and G3-8(a) need an in-project re-test, not the Voice gate. Staging an edit on the branch is NOT a license to deploy it.

== SKILLS ==
The SUPERPOWERS chain is the executor: brainstorming (ONCE, up front, for the BOOK-1 prefetch+inject design — the only genuinely creative choice) -> systematic-debugging (confirm each mechanism) -> writing-plans (the plan IS the frozen council ledger; do NOT author a second plan doc) -> test-driven-development -> verification-before-completion. dispatching-parallel-agents for the read-only research lanes + file-disjoint edits. Use spec-kit ONLY as a thin front-door: at most /speckit-specify (one spec covering the wider bug set), optionally /speckit-tasks. SKIP /speckit-clarify (no human overnight) and /speckit-analyze (the constitution is the empty [PROJECT_NAME] template — the gate is hollow). Do NOT use /speckit-implement as the executor and do NOT fill/rewrite the constitution as a side-quest. If spec-kit shows any process churn, drop it and run pure superpowers.

== COUNCILS (two bounded gates; they REPLACE the absent human reviewer) ==
The session is UNSUPERVISED, so the councils ARE the plan-approval and the pre-merge code-review a human would otherwise give — mandatory gates with veto teeth, not ceremony. Each convenes ONCE. They bracket the superpowers work; they never relax a guardrail.

PLANNING COUNCIL — convene ONCE at the END of Phase-1 research, on the FROZEN research output, BEFORE any code:
1. CONFIRM or RECLASSIFY each open bug into exactly one class (the highest-value catch — misclassification is how a deploy-nothing session accidentally needs a deploy or silently drops a bug). Reclassify by READING the actual save path, not guessing — specifically resolve the PHONE-CLEAR-1 fork (ContactDetail.tsx frontend recompute vs the server-side _shared normalizer; chair recommends server) and the G3-8(a) question (can the server-side reactivation fn land cleanly+tested overnight? if not, demote to WRITEUP+design-note).
2. CONFIRM the SEQUENCE: SMS-OBS-1 persistence FIRST (gate everything on it landing green) -> BOOK-1 ground-truth -> file-disjoint CODE-BRANCH-NODEPLOY items -> STAGE+DEPLOY items last + fenced -> WRITEUP items.
3. CONFIRM the single-writer / file-disjoint map: ONE coordinator owns trigger/_shared/ and every shared/edge file; the only real same-file hazard is retell-proxy/index.ts (VM-1 vs API-DEPR-1) — API-DEPR-1 is DEFER so it does not touch the file; ratify that.
4. ADVERSARIALLY PROBE: does any folded-in bug tempt a deploy, a voice-booking-tools edit, a prompt edit, a Mgmt-API write, an identity-injection reorder, or a frozen-baseline deploy? If yes -> STAGE or WRITEUP or DEFER, never applied-and-deployed.
OUTPUT = a FROZEN per-bug ledger (the build contract): bug -> class -> exactly what lands tonight -> guardrail check. After this gate, Phase-2 may only NARROW scope (demote), never expand. FAIL-SAFE: if research is insufficient to classify a bug, AUTO-DEMOTE to WRITEUP — do not loop-research it. ONE pass on the whole defect set, NOT one council per bug.

ADVERSARIAL VERIFICATION COUNCIL — convene ONCE before any 'done' claim, AFTER verification-before-completion has produced pasted npx tsc + npm run test:node (+ npm run test:edge for any edge-source change) output. It interrogates that evidence to FALSIFY 'done':
  PER FIX: (a) landed — failing-then-passing test present, diff surgical (every changed line traces to a numbered defect)? (b) live-VOICE regression risk — did anything reach voice-booking-tools / retell-proxy beyond VM-1's set-voicemail / make-retell-outbound-call / a shared prompt / a Retell or Mgmt-API write? (c) guardrail audit — deploy-nothing honored? frozen baselines untouched (retell-proxy source edited only for VM-1 + NOT deployed; make-retell-outbound-call v27 + voice-booking-tools untouched)? identity-injection order (contactId LAST) unchanged? prompts unedited? real clients' llm_model / response_delay_seconds untouched?
  WHOLE BRANCH: (d) deploy checklist complete + sufficient (every STAGE+DEPLOY item has target + exact command + version bump + covering test + Voice-gate where applicable, and is NOT deployed)? (e) branch coherent — builds green from a clean checkout, no orphaned/half-applied changes, no item the ledger said 'lands tonight' that only has a writeup (silent drop)? (f) FROZEN-LEDGER cross-check — all open bugs have a definite disposition, NOTHING silently missing. (g) HANDOFF validation — the emitted tomorrow prompt truthfully says staged/undeployed (not live); the 5 lists + SESSION_PLAN + handoff are consistent with the ledger.
  VETO POWER: a guardrail breach or Voice-regression path -> REVERT the offending fix on the branch (do not deploy-around it). A silently-dropped bug -> add it back as a writeup before close. It does NOT bless 'done' from green tsc alone.

ANTI-CHURN: each council convenes ONCE (planning on the frozen research; verification on the assembled branch). Bounded decision gates, not iterative debate loops, not per-bug. On a verification VETO, fix the specific item + re-run only the affected evidence — do NOT re-convene the whole council in a loop. Do NOT add a third/mid-implementation council. Specify the JOB / OUTPUT / VETO; do not over-specify personas or debate rounds.

== PHASE 0 — SETUP ==
Create the worktree branch. Write a one-page "shared-surface contract" to scratchpad classifying every relevant file (now including the folded-in files) as FREELY-EDITABLE / GATED (shared/edge, branch-only) / REPORT-ONLY (prompts). Give that doc to every subagent so none independently edits a shared fn or deploys.

== PHASE 1 — RESEARCH (read-only HARD GATE; no code edits until the planning council has frozen the ledger) ==
Fan out parallel READ-ONLY subagents on DISJOINT lanes:
  (A) TEXT path: processSetterReply -> setterToolLoop -> setterTools -> (shared) voice-booking-tools. System-prompt assembly + token SIZE. Model selection. Debounce.
  (B) VOICE path: make-retell-outbound-call slot PREFETCH (injects available_time_slots into a Retell dynamic var so the model sees open times BEFORE it speaks) + retell-proxy -> the SAME voice-booking-tools. The standalone-prompt UI (frontend/src/components/prompt-doc/PromptDocPage.tsx + PromptManagement/AgentConfigBuilder). Prompt SIZE.
  (C) REFERENCE: n8n/exports/Text_Engine_REVERSE_ENGINEERED.md. NOTE: there is NO upstream git remote; genokadzin/1prompt-os is our heavily-divergent fork BY DESIGN — do NOT clone it, do NOT treat it as a correctness oracle, do NOT revert deliberate divergences; never fabricate a diff.
  (D) TEST-COVERAGE inventory (note voice-booking-tools has ZERO tests).
  (E) NON-TEXT BUG CLASSIFICATION PACKET (read-only): for each folded-in bug (F9-1, VM-1, MODEL-1, PHONE-CLEAR-1, G3-8(a), API-DEPR-1, BOOK-2/3, G3-7), READ the actual save path / call site and produce the evidence the planning council needs to ratify or reclassify (esp. the PHONE-CLEAR-1 fork + the G3-8(a) reactivation-path size).
Output = ONE comparison doc: the "Text vs Voice structural map" table + a NUMBERED, file:line-cited defect list tagged [CODE-TEXT-SAFE]/[CODE-SHARED-DEFER]/[PROMPT-BRENDAN]/[INTENTIONAL-DIVERGENCE], with the pre-seeded BOOK-1/2/3 + OBSERVABILITY entries — WIDENED to carry the read-only classification packet for the non-Text bugs. Then convene the PLANNING council on it.

== PHASE 2 — IMPLEMENT (only AFTER the planning council has FROZEN the ledger; do ONLY what it marks 'lands tonight'; per-fix superpowers micro-loop: systematic-debugging -> TDD failing test first -> minimal implement -> green) ==
brainstorming runs ONCE up front (BOOK-1 prefetch+inject design only); the prescriptive fixes (F9-1, VM-1, MODEL-1 validation, PHONE-CLEAR-1, G3-8(a)) do NOT each need a brainstorm. SEQUENCE:
  (1) SMS-OBS-1 persistence FIRST — persist setterToolLoop.ts loopResult.toolInvocations (name/args/result) to a NEW additive table; fix processSetterReply.ts:344 tool_calls:[]. Everything gates on this landing green.
  (2) BOOK-1 ground truth — prefetch a get-available-slots window + inject a COMPACT real-open-times block into context every reply (mirror Voice's compactSlots, read-only) + an ANTI-FABRICATION guard (no "unavailable" claim without a get-available-slots result) + a tool_choice nudge (force get-available-slots before any availability assertion / book-appointments on acceptance) + tighten setterTools.ts TOOL_USAGE_INSTRUCTION.
  (3) the file-disjoint CODE-BRANCH-NODEPLOY items (F9-1; MODEL-1b frontend validator if kept).
  (4) the STAGE+DEPLOY items LAST and clearly fenced (MODEL-1a llmModel.ts; VM-1 set-voicemail on the frozen retell-proxy file; PHONE-CLEAR-1 / G3-8(a) per the council fork) — coded+tested, NOT deployed.
  (5) the WRITEUP+CHAR-TEST items (BOOK-2/3) — findings + characterization tests of CURRENT voice-booking-tools behavior (locks Voice parity); no edit.
Phase 2 may only NARROW scope vs the ledger (demote to writeup), never expand it.

== BOUND THE SWARM ==
Parallel subagents ONLY for independent read-only Phase-1 lanes and file-DISJOINT Phase-2 edits. ONE coordinator owns all writes to trigger/_shared/ and every shared/edge file. Forbid any subagent from running deploy/network-write/Mgmt-API-write/npm-install commands. Never let two agents edit the same file; never edit voice-booking-tools at all.

== DONE (falsifiable; "works completely" from green tsc alone is NOT acceptable) ==
DONE for the Text-safe scope = (a) tool invocations persisted to a DB table; (b) get-available-slots is always called/injected before the model may assert unavailability; (c) the off-grid-minute + tz-window cases have failing-then-passing UNIT tests (the SHARED fixes themselves WRITTEN UP, not applied); (d) npx tsc + npm run test:node pass with output pasted. PLUS for the EXPANDED scope: (e) every CODE-BRANCH-NODEPLOY + STAGE+DEPLOY item has a failing-then-passing unit test; (f) every STAGE+DEPLOY item has a deploy-checklist entry (files, tests, deploy target + exact command, version bump, Voice-gate where applicable) and is NOT deployed; (g) every DEFER carries a one-line reason; (h) the per-bug ledger shows a definite disposition for ALL open bugs with NONE silently dropped; (i) npm run test:edge passes for any edge-source change, output pasted. tsc/build green is necessary, NOT sufficient: live deploy + live booking + live-verify of every staged fix are Brendan's daytime job behind the gate. Do NOT claim BOOK-1 "fixed from code alone": code makes fabrication structurally impossible; the prompt-wording change is Brendan's to apply.

== DEPLOY CHECKLIST (required shape; write into the handoff) ==
For EACH staged item: file(s) changed -> deploy target (one of: 'frontend build only' | 'Trigger.dev deploy' | 'supabase functions deploy <fn> --use-api --no-verify-jwt' | 'apply migration <name> via Mgmt API') -> resulting version bump (e.g. retell-proxy v46->v47) -> its tests -> GATED-ON-VOICE-REGRESSION? (yes/no). GROUP BY deploy target so Brendan runs each target once. State explicitly which items are SAFE to deploy independently (Trigger.dev / frontend / in-project edge) vs which are GATED on the Voice-regression check (anything bumping retell-proxy / make-retell-outbound-call / voice-booking-tools — i.e. VM-1). END with the exact Voice-regression checklist: place a real outbound voice call on a canonical agent, confirm booking still works, confirm B-3 (latest_published) + B-5 (default vars) survive — if it fails, deploy ONLY the non-Voice items.

== CLOSE ==
Write: the comparison doc, a fix-summary (what changed; what is unit-verified vs what still needs Brendan's live test), the BOOK-1 prompt recommendation (copy-paste-ready), and the DEPLOY CHECKLIST + Voice-regression gate above. Doc updates keyed to the ledger: BUG_LIST.md — each coded/staged bug gets a [~] staged on branch <name>, deploy-pending note (do NOT move to COMPLETED_LOG, do NOT mark [x]: nothing is deployed/verified live yet); refresh DEFER reasons on API-DEPR-1 + G3-7; keep BOOK-2/3 with the shared-fn defer note. TEST_LIST.md — ADD a live-verify entry for every newly-coded fix (F9-1, VM-1, SMS-OBS-1 persistence visible in DB, MODEL-1, BOOK-1/3.12, PHONE-CLEAR-1, G3-8(a) if coded), keeping the still-owed B-5/F1/LIVE-D/LIVE-E/G3-6-Tier-3/3.12 items. SESSION_PLAN.md — update the Session 7.5 entry to the expanded all-bugs scope, mark it done when the branch is complete, and note the 'Fix pass' entry is now largely ABSORBED into 7.5 (what truly remains as their own sessions: API-DEPR-1 + G3-7). BRENDAN_TODO.md — add the concrete daylight DEPLOY steps + the Voice-regression gate + the BOOK-1 report-only prompt recommendation. Write the dated handoff at Operations/handoffs/2026-07-01-<topic>.md holding the comparison-doc summary, the per-bug disposition ledger, the deploy checklist, the Voice-regression gate, and the emitted tomorrow prompt. Commit to the branch. Do NOT push/merge/deploy.
EMIT a TOMORROW kickoff prompt (save to the handoff AND print in a fenced block) — written TRUTHFULLY: the branch holds STAGED, UNDEPLOYED fixes, NOTHING is live, Brendan DEPLOYS in daylight per the deploy checklist gated on a Voice-regression pass, then runs the remaining live tests. It MUST NOT read as if anything is merged/deployed/live. Do NOT invalidate the paused Session-7 TEST baseline. See the TOMORROW-PROMPT SPEC.

== TOMORROW-PROMPT SPEC (the build session emits this at close) ==
Contents IN THIS ORDER:
1. STANDARD CONTEXT BLOCK — paste SESSION_PLAN.md's Standard Context Block verbatim (branch / Supabase ref / ./.env creds / read the PHONE binding not old memory / both prompts report-only / Relay Protocol).
2. EXPLICIT STATE LINE (mandatory): "The overnight branch <name> contains STAGED, UNDEPLOYED fixes. NOTHING is live. Your FIRST job is to DEPLOY per the checklist, in daylight, gated on the Voice-regression check — only then run live tests. Do not run live tests against undeployed code."
3. STEP 1 = DEPLOY (daylight, gated). Point at the branch's DEPLOY CHECKLIST. Require the Voice-regression GATE to PASS before any retell-proxy / make-retell-outbound-call / voice-booking-tools version bump (covers VM-1 v46->v47). If the gate fails, deploy ONLY the Trigger.dev / frontend / non-Voice in-project items. List deploy targets grouped, each with its version bump.
4. STEP 2 = LIVE TESTS (Session-7-finish): B-5 (inbound from a non-CRM number — TEST_PHONE_A is a known lead so use a different number), F1 (fresh GHL contact deep-link — field provisioned id 4tDL3asiRNrQD3MKyP2E), LIVE-D (B-2 ×4 + manual-send/429), LIVE-E (F3 pause/resume + F4 tz nudge), G3-6 Tier-3 analytics, 3.12 SMS booking re-test (BOOK-1 acceptance: tool log shows get-available-slots THEN book-appointments=confirmed + a confirmed GHL appointment). PLUS live-verify the newly-coded/staged fixes: F9-1, VM-1, MODEL-1-HARDENING, SMS-OBS-1 (tool invocations visible in the new table), PHONE-CLEAR-1, G3-8(a) if coded — AND the already-BUILT-2026-06-29 items that only need live-verify: INB-1, UI-1, F11 (do NOT rebuild). Before the 3.12 re-test, apply the BOOK-1 report-only prompt recommendation from BRENDAN_TODO.
5. STEP 3 = CLOSE OUT (close out THEN emit next): passed -> COMPLETED_LOG, new failures -> new BUG_LIST entries; tick SESSION_PLAN Session-7-finish; dated handoff; commit+push origin + github; THEN emit the next prompt (Session 8 F8 in PLAN mode, or a fresh fix-pass if anything failed, or the API-DEPR-1 / G3-7 dedicated sessions).
6. USE COUNCILS WHERE USEFUL: a DEPLOY-READINESS council before flipping anything live (esp. the retell-proxy v47 bump behind the Voice gate), and a TEST-TRIAGE council if multiple Session-7-finish live tests fail.
7. READ FIRST: Docs/SESSION_PLAN.md + the 2026-07-01 handoff + Docs/BUG_LIST.md + Docs/TEST_LIST.md + Docs/BRENDAN_TODO.md.
No em dashes; fenced; self-contained.

READ FIRST (this session): Docs/SESSION_PLAN.md + Operations/handoffs/2026-06-30-session7-test-pass-phone-half.md + Docs/BUG_LIST.md (all 11 open bugs) + n8n/exports/Text_Engine_REVERSE_ENGINEERED.md. Follow the Relay Protocol but honor the DEPLOY-NOTHING / branch-only override above.
```
