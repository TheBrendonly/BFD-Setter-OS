---
description: Council-vetted overnight kickoff prompt to BUILD feature F8 (cost-to-price calculator) — branch-only/deploy-nothing, server-enforced agency-only markup boundary, own planning + verification councils, emits a tomorrow deploy+test prompt.
---

# F8 cost-to-price calculator — overnight build kickoff prompt (paste into a fresh session)

Council-vetted (3 lenses + chair). House style matches the Text-Setter overnight prompt: superpowers + thin spec-kit executor, the build session runs its OWN planning + verification councils (the planning council replaces the SESSION_PLAN PLAN-mode human approval since no human is present overnight), DEPLOY-NOTHING / branch-only (Brendan deploys + live-tests in daylight), emits a tomorrow prompt. The one real trap: the markup % + cost breakdown must be **server-enforced** agency-only (RLS here is agency-scoped and a client JWT shares its client's agency_id, so a markup column on `clients`/`clients_public` would be client-readable) — hence a dedicated `client_pricing_config` table with NO client-read policy + a `get-blended-rate` edge fn that returns only the scalar to a client.

```
BFD-setter — BUILD feature F8 (cost-to-price calculator), own overnight session, UNSUPERVISED. Repo /srv/bfd/Projects/bfd-setter.

== CONTEXT / CREDS ==
Branch off main (git pull first). Supabase ref bjgrgbgykvjrsuwwruoh. Creds in ./.env (SUPABASE_PAT, TRIGGER_DEPLOY_PAT, BFD_RETELL_API_KEY). Live DB via Supabase Management API /database/query with a BROWSER User-Agent (NOT postgres MCP, and READ-ONLY tonight — only to confirm column/RLS facts). To know which agent serves a direction, read the PHONE-NUMBER binding (list-phone-numbers), not old memory. Voice/Retell prompts are REPORT-ONLY. Follow the Relay Protocol in Docs/SESSION_PLAN.md. This is an OVERNIGHT, UNSUPERVISED session: no human is available, so the session runs its OWN councils in place of the SESSION_PLAN-mandated PLAN-mode human approval.

== MISSION (and why this is lower-risk than the Text session) ==
BUILD feature F8: an admin-configurable cost-to-price calculator that turns real per-minute provider cost (Retell USD + OpenRouter LLM USD + Twilio AUD + number rental + other) into a displayed BLENDED price/min in AUD, with an agency-set USD->AUD FX rate and a markup %. F8 is a CLEAN ADDITIVE feature, NOT a repair; it does NOT touch the voice/booking/shared-edge infra, so it is lower-risk than the Text-Setter session. BUT it has ONE real trap (client-vs-agency isolation is sometimes UI-layer-only because RLS is agency-scoped) — that trap is the whole reason this needs a verification council. SPEC SOURCE: FEATURE_ROADMAP.md "Feature spec - F8" (lines 21-54) + SESSION_PLAN.md Session 8.

== GOVERNANCE / SERVER-ENFORCEMENT GUARDRAILS (honor these or the build fails verification) ==
THE TRAP (Session-7 finding, grounded in code): client-vs-agency isolation is sometimes UI-LAYER ONLY. RLS on `clients` is ROW-level + agency-scoped with NO column protection — the `clients_public` migration (20260624160000) header says so. Each real client is its own agency, so a CLIENT-role JWT shares the same agency row-scope as its own client row. Hiding the markup only in React is defeated by calling the edge fn directly or .select()ing the table/view. The boundary MUST be a server-side role split.
1. RAW PRICING INPUTS ARE AGENCY-ONLY, SERVER-ENFORCED: markup_percent, fx_usd_to_display (+ buffer %), the full editable rate table, every component toggle, the per-component breakdown, and the pre-markup subtotal must be UNREADABLE by a client-role API token (the F8 analogue of the 13 omitted secrets).
2. STORE THEM ONLY IN A NEW client_pricing_config TABLE WITH NO CLIENT-READ POLICY. Copy the agency "FOR ALL" policy VERBATIM from client_account_field_config (mig 20260617120000, the agency_id-IN-profiles USING+WITH CHECK). DO NOT copy that migration's lines 38-45 "Client users can read their own ..." SELECT policy — that policy IS the trap. The client never reads this table.
3. NEVER put markup / fx / rate_table on `clients` or `clients_public` (client-readable surfaces). Only the DERIVED blended scalar may ever reach a client, and only via the gated edge read.
4. THE CLIENT RECEIVES ONLY THE FINAL NUMBER, VIA AN EDGE FN. Build `get-blended-rate` modeled on save-account-settings/index.ts: call resolveClientAccess(authHeader, client_id) (returns {userId, role}), load the merged config with the SERVICE-ROLE client, compute via the shared pure core, then BRANCH ON ROLE. role==='client': check show_rate_to_client; if false return { show:false }; if true return ONLY { show:true, blended_per_min, display_currency }. role==='agency': may return the full breakdown + markup + rate_table.
5. CONSTRUCT A FRESH WHITELISTED OBJECT FOR THE CLIENT — never spread the agency object and delete keys (deletion-by-omission leaks the next field added). Mirror clients_public enumerating safe columns.
6. THE TOGGLE IS A VISIBILITY SWITCH FOR THE FINAL NUMBER ONLY. show_rate_to_client is agency-set, stored in client_pricing_config, written ONLY by an agency role (a client patch including it is dropped server-side, like save-account-settings' allowedKeys at index.ts:219-223). Even with the toggle ON, a client gets ONLY the scalar. Enforce the toggle at the READ boundary in the edge fn (return { show:false } when off), NOT by hiding the card in client React.
7. MONEY MATH IS DETERMINISTIC + INTEGER. FX + markup in integer MINOR UNITS (cents, matching weekly_cost_ceiling_cents). FX as integer micros, optional buffer + markup in basis points. ONE explicit FX step; round-half-to-even ONCE at the very end so line items sum exactly. No JS float toFixed in the core. Fixed monthly costs (number rental, A2P) are a SEPARATE fixedMonthlyMinor, never folded into blendedMinorPerMin.
8. TWILIO DEFAULTS OFF: include_twilio_voice + include_twilio_sms default false (onboarding SOP: Twilio is client-BYO-and-billed).
9. DEPLOY NOTHING. Work ONLY on the isolated worktree branch overnight/f8-cost-price-calc. NO supabase functions deploy, NO Trigger deploy, NO Retell PATCH/publish, NO Supabase Mgmt-API WRITE, NO migration apply, NO real-client pricing write, NO npm install / lockfile change. The migration is a FILE only; the edge fn is coded+tested but NOT deployed. Frozen baselines retell-proxy v46 / make-retell-outbound-call v27 / voice-booking-tools stay untouched.
10. SURGICAL + ADDITIVE. Every changed line traces to F8. No opportunistic refactor of save-account-settings, clients_public, ClientSettings, or the USD-only UsageCredits.tsx:22 formatCurrency (add a NEW currency-aware helper; do not rewrite UsageCredits). types.ts: add ONLY the new client_pricing_config block (multi-DB app — never wholesale-regenerate).
VERIFICATION IS NOT EARNED BY tsc ALONE. Green tsc + passing computeBlendedRate tests do NOT prove the boundary. The mandatory proof is a real client-role token failing to read any markup/cost input (the verification council's client-cannot-read-markup test). Without that pasted evidence, 'done' is not earned.

== LOCKED DECISIONS (defaults the PLANNING council ratifies by reading the actual save paths; after the gate, Phase 2 may only NARROW) ==
1. CONFIG STORE = a NEW dedicated table client_pricing_config (id uuid pk, client_id uuid NOT NULL REFERENCES clients(id) ON DELETE CASCADE, config jsonb NOT NULL DEFAULT '{}', created_at, updated_at, UNIQUE(client_id)). Mirrors client_account_field_config. RLS = agency-manage-all ONLY, NO client-read policy.
2. GLOBAL DEFAULT + PER-CLIENT OVERRIDE: default rate card lives in code as DEFAULT_PRICING_CONFIG in a new _shared/pricingDefaults.ts (tunable via the editor, no sentinel DB row). A per-client row, when present, is merged over the default (mergeWithDefaults, mirroring useClientAccountFieldConfig.ts:56). No row = pure default.
3. COMPUTE CORE = a PURE, dependency-free computeBlendedRate in frontend/supabase/functions/_shared/computeBlendedRate.ts (NOT frontend/src/lib — package.json test:node globs ONLY trigger/_shared/*.test.ts; test:edge = deno test frontend/supabase/functions/ WILL run a co-located computeBlendedRate.test.ts). Model on _shared/retell-lock.ts + retell-lock.test.ts. The SAME util is imported by BOTH the agency editor (live preview) and the get-blended-rate edge fn (authoritative client number) — ONE math implementation. Integer minor units; micros FX; bps markup; single explicit FX step; round-half-even once at the end; separate fixedMonthlyMinor.
4. GOVERNANCE ENFORCEMENT = a NEW service-role edge fn get-blended-rate (role split as above), modeled on save-account-settings/index.ts (resolveClientAccess from _shared/assert-client-access.ts:54). The AGENCY WRITE path needs NO edge fn — the agency JWT has FOR ALL RLS on client_pricing_config, so a useClientPricingConfig hook upserts directly (like ClientAccountFieldConfigEditor writes its config table directly).
5. AGENCY PANEL = a NEW ClientPricingConfigEditor.tsx (model on ClientAccountFieldConfigEditor.tsx) + useClientPricingConfig.ts hook, mounted on frontend/src/pages/ClientSettings.tsx inside {isAgency && clientId && (...)} next to ClientAccountFieldConfigEditor (re-Read for the current line; the cost-ceiling block ~line 311 is the agency-gate idiom to copy). It renders a LIVE breakdown by calling the shared compute util on the in-memory form. Do NOT route its inputs through clients_public.
6. CLIENT CARD = a NEW ClientPricingDisplayCard.tsx mounted on frontend/src/pages/AccountSettings.tsx with {!isAgency && clientId && (...)} next to ClientAccountSettingsCard (~line 266) / ClientBillingCard (~line 272). It calls get-blended-rate, renders NOTHING when show===false, and shows ONLY "$X.XX /min (AUD)" via the new formatter — never markup/breakdown. Model the load/guard/return-null on ClientAccountSettingsCard.
7. CURRENCY FORMATTER = a NEW formatMinorCurrency(minor, currency, locale='en-AU') using Intl.NumberFormat (style:'currency'). Do NOT modify the USD-only UsageCredits.tsx:22 formatCurrency. Place so both the editor preview + client card import it (frontend/src/lib/formatCurrency.ts).
8. THE 5 OPEN DECISIONS (FEATURE_ROADMAP:47-53) — adopt: (1) audience = agency-internal tool PLUS the opt-in client display card; (2) per-sub-account override on a global default; (3) admin-set fixed FX + "last updated" stamp + optional buffer % (live feed = v2); (4) Twilio toggles default OFF; (5) fixed monthly costs = a SEPARATE line.
9. V2 DEFERRED: live model-aware LLM estimate from openrouter_usage_cache, post-call Retell actual reconciliation against call_history.cost, live FX feed.
10. SCOPE GUARD: v1 core only. Frozen baselines untouched. Deploy nothing.

== SKILLS / EXECUTOR ==
SUPERPOWERS is the executor: brainstorming ONCE (the creative choice = the config DATA MODEL + the computeBlendedRate contract, NOT the prescriptive plumbing) -> systematic-debugging only if a mechanism is unclear -> writing-plans (the plan IS the frozen council ledger; do NOT author a second plan doc) -> test-driven-development (TDD the PURE computeBlendedRate FIRST: failing money-math tests, then implement) -> verification-before-completion (paste npx tsc + npm run test:edge + frontend build before any 'done'). dispatching-parallel-agents for read-only research lanes + file-disjoint edits only. spec-kit = thin front-door: ONLY /speckit-specify (one spec covering compute core + config store + agency panel + show-rate-to-client governance); SKIP /speckit-clarify (no human) + /speckit-analyze (hollow [PROJECT_NAME] constitution); do NOT use /speckit-implement; do NOT rewrite the constitution. If spec-kit churns, drop it and run pure superpowers.

== PHASE 0 — SETUP ==
git pull clean main; create an isolated git worktree branch overnight/f8-cost-price-calc. Write a one-page shared-surface contract to scratchpad: FREELY-EDITABLE (new _shared/computeBlendedRate.ts + test, new _shared/pricingDefaults.ts, new get-blended-rate fn + test, new migration FILE, new ClientPricingConfigEditor + useClientPricingConfig hook, new ClientPricingDisplayCard, new formatCurrency util, the two page mounts, surgical types.ts) vs FROZEN/READ-ONLY (retell-proxy, make-retell-outbound-call, voice-booking-tools, the clients_public view def, save-account-settings behavior except additive, ALL prompts).

== PHASE 1 — RESEARCH (read-only HARD GATE; no edits until the planning council freezes the ledger) ==
Disjoint read-only lanes: (A) GOVERNANCE precedent — save-account-settings/index.ts role split + allowedKeys re-derivation; client_account_field_config mig RLS (note the client-read-own SELECT policy lines 38-45 = the leak to AVOID); ClientAccountFieldConfigEditor.tsx + useClientAccountFieldConfig.ts; ClientSettings.tsx isAgency gates + mounts. (B) THE TRAP — clients_public mig header (security_invoker, agency-scoped, NO column protection) + resolveClientAccess returning {role}; produce the exact server-enforcement design + the client-cannot-read-markup proof plan. (C) COST/PRICE inputs — clients.*_cost_ceiling_cents (cents precedent), client_cost_rollup, call_history.cost, openrouter_usage_cache (v2 only). (D) DISPLAY — UsageCredits.tsx:22 formatCurrency -> design the Intl replacement. (E) TEST infra + the two page mounts (AccountSettings client cards). Output = ONE design doc + a numbered build ledger. THEN convene the PLANNING council on it.

== PHASE 2 — IMPLEMENT (only after the planning council FREEZES the ledger; per-fix superpowers micro-loop; may only NARROW) ==
(1) brainstorming ONCE -> TDD the PURE computeBlendedRate FIRST. (2) _shared/pricingDefaults.ts (seed rate table + DEFAULT_PRICING_CONFIG: display_currency AUD, fx stamp, include_* with Twilio OFF, markup_percent, show_rate_to_client=false; mergeWithDefaults). (3) the additive migration FILE (client_pricing_config, agency-only RLS, NO client-read policy) — written, NOT applied. (4) the get-blended-rate edge fn (service-role role split, fresh whitelisted client object) + its deno test. (5) the agency-only ClientPricingConfigEditor + useClientPricingConfig hook on ClientSettings behind isAgency. (6) the client read-only ClientPricingDisplayCard on AccountSettings, shown only when toggle ON, sourced from get-blended-rate. (7) the Intl currency formatter. (8) surgical types.ts (only the new table block). THEN convene the ADVERSARIAL VERIFICATION council.

== THE TWO IN-SESSION COUNCILS (bounded, ONCE each, veto teeth; they replace the absent human reviewer) ==
PLANNING COUNCIL (ONCE, end of Phase-1, on frozen research, BEFORE any code; this IS the PLAN-mode approval): RATIFY or amend by reading the ACTUAL save paths: (1) the dedicated client_pricing_config table with agency-only RLS + NO client-read policy; (2) the governance server-enforcement design (raw config agency-only; client gets the scalar via get-blended-rate gated on show_rate_to_client; mirrors save-account-settings' role split; leaks via NO clients_public column); (3) the 5 open-decision answers; (4) v1 scope only (v2 deferred); (5) adversarial probe — does any part tempt a deploy, a clients_public markup column, a client-readable RLS row, a frozen-baseline touch, or a live pricing write? OUTPUT = a FROZEN build contract (config-store decision + RLS policy text + edge-fn return contract + compute spec + migration-as-file plan). VETO: any design storing markup/rate-table on a client-readable surface, a client-read RLS policy on the pricing config, or an implied deploy/live write -> redesign before any code.
ADVERSARIAL VERIFICATION COUNCIL (ONCE, before 'done', AFTER pasted npx tsc + npm run test:edge + frontend build): FALSIFY 'done': (a) MONEY-MATH — failing-then-passing tests for integer-minor-units, the explicit FX step (+ buffer), the markup multiplier, deterministic round-half-even (two runs identical), per-component enable/disable (Twilio OFF), line-items summing EXACTLY to total, the separate fixedMonthlyMinor. (b) GOVERNANCE PROOF (the gate, the F8 analogue of the G3-6 network-tab proof): provision a throwaway CLIENT-ROLE user owning a test client with markup_percent=100 + show_rate_to_client=true (as Session-7 B-4 provisioned+deleted a throwaway client user); mint its JWT; assert (i) get-blended-rate -> ONLY blended_per_min + display_currency, string-search the JSON for "markup"/"rate_table"/"fx"/"0.07" -> ZERO hits; (ii) direct client_pricing_config.select('*') -> 0 rows / denied; (iii) clients_public.select('*') -> NO markup/fx/rate_table column; (iv) flip toggle off (as agency), re-call -> { show:false }; (v) toggle on -> still only the scalar; (vi) client PATCH of show_rate_to_client/markup_percent -> dropped server-side; (vii) agency token -> full breakdown (proves a role split). Paste the request/response evidence; DELETE the throwaway. PLUS a unit test on the edge handler role-branching + a static proof markup/rate-table are on NO client-readable surface. (c) NO LIVE MUTATION — deploy-nothing honored; migration is a FILE only; no live pricing write; frozen baselines untouched. (d) BRANCH COHERENT — builds green from a clean checkout; docs consistent with the ledger; the tomorrow prompt truthfully says staged/undeployed. VETO: ANY client-token surface returning a markup/cost-input byte, or a live mutation -> revert that piece; a missing money-math test -> add it before close.
ANTI-CHURN: planning council once on the frozen research; verification council once on the assembled branch. On a veto, fix the specific item + re-run ONLY the affected evidence; do NOT re-loop the whole council; no third mid-implementation council.

== DONE (green tsc alone is NOT done; the client-cannot-read-markup proof is mandatory) ==
computeBlendedRate pure with failing-then-passing tests (integer minor units, explicit FX step + buffer, markup, deterministic rounding, per-component enable/disable with Twilio OFF, line-items sum to total, separate fixedMonthlyMinor); migration FILE exists + unapplied + agency-only RLS with NO client-read policy; governance server-enforced with the PASSING client-role-token proof pasted; agency panel on ClientSettings (isAgency) persists; client card on AccountSettings shown only when toggle ON, sourced from get-blended-rate; Intl currency formatter used (UsageCredits unchanged); DEPLOY-NOTHING honored; npx tsc + npm run test:edge + frontend build green from a clean checkout with output PASTED; both councils ran once; docs updated to the ledger; branch committed (NOT pushed/merged/deployed); a truthful tomorrow prompt emitted.

== CLOSE / DOC UPDATES (all on the branch, committed not pushed) ==
FEATURE_ROADMAP.md F8 -> [~] staged-on-branch/deploy-pending (NOT [x]); record the resolved 5 decisions + chosen config store; keep v2 DEFERRED. SESSION_PLAN.md Session 8 -> council-vetted overnight branch-only build (staged, undeployed) + done-when; unticked until landed; note F8 touches no Voice surface so its deploy is NOT Voice-gated. TEST_LIST.md -> ADD F8 live-verify entries (agency panel edits/persists; blended $/min + breakdown correct vs seeded figures; client display card respects the toggle; the trap test). BRENDAN_TODO.md -> daylight DEPLOY steps + the F8 acceptance checklist incl. the client-role isolation proof. DEFERRED.md -> F8 v2 with one-line reasons. Write a dated handoff Operations/handoffs/2026-07-0X-f8-cost-price-calc.md (design-doc summary, frozen ledger, deploy checklist, emitted tomorrow prompt). Do NOT move F8 to COMPLETED_LOG (nothing live yet). Commit to the branch; do NOT push/merge/deploy.

== EMIT A TOMORROW KICKOFF PROMPT (fenced block + saved into the handoff; written TRUTHFULLY — staged, undeployed; NOTHING live) ==
Contents in order: (1) Standard Context Block (branch overnight/f8-cost-price-calc). (2) EXPLICIT STATE LINE: "The overnight branch overnight/f8-cost-price-calc contains STAGED, UNDEPLOYED additive F8 work (a migration FILE not applied, a new get-blended-rate edge fn not deployed, frontend not built/deployed). NOTHING is live. Your FIRST job is to DEPLOY in daylight per the checklist, then run the F8 acceptance live-tests. Do not live-test against undeployed code." (3) STEP 1 = DEPLOY: apply the client_pricing_config migration via the Mgmt-API SQL runner; deploy get-blended-rate (supabase functions deploy <fn> --use-api --no-verify-jwt); build/deploy the frontend. F8 touches NO Voice surface -> NOT Voice-regression gated; still smoke-confirm the frozen baseline is unchanged. (4) STEP 2 = F8 ACCEPTANCE LIVE-TESTS -> TEST_LIST: agency panel edits+persists; blended $/min + breakdown correct vs seeded figures (hand-check the math + FX + markup); client card respects the toggle (visible only when ON, shows ONLY the blended $/min); THE TRAP TEST (provision+delete a throwaway client-role user as in Session-7 B-4 and confirm it CANNOT read markup/cost inputs via the API). (5) STEP 3 = CLOSE OUT then EMIT NEXT: passed -> F8 to COMPLETED_LOG + FEATURE_ROADMAP [x]; failures -> new BUG_LIST entries; tick SESSION_PLAN Session 8; dated handoff; commit+push origin + github; THEN emit the next prompt (the First-client milestone is Brendan-gated / not a code session, OR a fresh fix-pass if anything failed, OR resume the still-owed Session-7-finish live items). (6) READ FIRST: Docs/SESSION_PLAN.md + the new dated handoff + Docs/TEST_LIST.md + Docs/BRENDAN_TODO.md. No em dashes; fenced; self-contained.

READ FIRST (this session): Docs/SESSION_PLAN.md + FEATURE_ROADMAP.md "Feature spec - F8" + Operations/handoffs/2026-06-30-session7-test-pass-phone-half.md (the house-style precedent) + Docs/BUG_LIST.md. Follow the Relay Protocol but honor the DEPLOY-NOTHING / branch-only override above. No em dashes anywhere.
```
