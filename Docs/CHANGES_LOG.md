# Changes Log

Append-only. One row per phase / sub-phase as it ships. Used to roll back.

| Date | Phase | Commit SHA | Tag | Files changed | Revert |
|---|---|---|---|---|---|
| 2026-05-19 | phase-night-n1-per-client-last-synced-from — N1 from the 2026-05-18d handoff (D36). The echo-stamp value `"1prompt-os"` that `push-contact-to-ghl` writes into each GHL contact's custom field was hardcoded in two places: write side at push-contact-to-ghl:134 and read side at sync-ghl-contact:301 (the echo-loop guard that skips inbound webhooks within 60s of our own push). Hardcoding meant: (a) every client looked like BFD on inbound mirror calls, (b) multi-tenant deployments would cross-stamp, (c) the field VALUE wasn't actually multi-tenant despite the field ID being per-client since D-M5. **Fix:** new column `clients.ghl_last_synced_from_field_value text DEFAULT '1prompt-os'` (ALTER applied via Management API). Both edge fns now SELECT the column and use it (with `?? "1prompt-os"` fallback). The read side comparison is lowercased to match the existing trim-lowercase guard logic. Header comment at push-contact-to-ghl:9 and inline comment at sync-ghl-contact:281 refreshed to document per-client behaviour. SOP §4.1 INSERT template gains the column, §4.4 echo-loop guard section rewritten, RUNBOOK § Add a per-client custom field id rewritten to a single SQL UPDATE (no edge-fn redeploy needed after this commit). Deployed push-contact-to-ghl v5→v6 + sync-ghl-contact v13→v14 via `scripts/deploy_retell_proxy_bundle.mjs` (the canonical bundle endpoint moved in N9 above). **Verified BFD's row picked up the DEFAULT** — `ghl_last_synced_from_field_value = "1prompt-os"` post-migration, so BFD behaviour unchanged. | `<TBD>` | `phase-night-n1-per-client-last-synced-from` | `frontend/supabase/functions/push-contact-to-ghl/index.ts` (header comment + SELECT + new lastSyncedFromValue derivation + replaced hardcoded write); `frontend/supabase/functions/sync-ghl-contact/index.ts` (SELECT + inline comment + expectedStampValue derivation + replaced hardcoded read). `Docs/CLIENT_ONBOARDING_SOP.md` (§4.1 INSERT + §4.4 echo-loop guard). `Docs/RUNBOOK.md` (§ Add a per-client custom field id rewritten). SQL via Management API on bjgrgbgykvjrsuwwruoh. Deployed both fns via bundle endpoint. | **Code rollback:** `git revert <SHA>` then redeploy both fns. **SQL rollback (optional, preserves data):** `ALTER TABLE clients DROP COLUMN ghl_last_synced_from_field_value;`. Dropping the column safe because revert restores the hardcoded `"1prompt-os"` literal which matches BFD's existing data. **Recommended:** none — Client #2 blocker now closed; BFD behaviour preserved by DEFAULT. |
| 2026-05-19 | phase-night-n9-deploy-scripts-canonical-location — N9 from the 2026-05-18d handoff. Both `/tmp/deploy_with_shared.mjs` (batch, EE3-sweep list baked in) and `/tmp/deploy_retell_proxy_bundle.mjs` (single-fn, slug via argv) are critical infra — they're the canonical bundle-endpoint deploy method that replaced the broken supabase-CLI path during the EE3 regression. Living in `/tmp/` meant they evaporated on every server reboot and weren't in version control. Copied both verbatim to `scripts/deploy_with_shared.mjs` + `scripts/deploy_retell_proxy_bundle.mjs`. `Docs/RUNBOOK.md` § Deploys rewritten: old "Edge function (single)" + "Edge function (all changed)" sections replaced with "Edge function (single, canonical)" pointing at `scripts/deploy_retell_proxy_bundle.mjs` and "Edge function (batch / multiple slugs)" pointing at `scripts/deploy_with_shared.mjs`, with explicit note that the legacy `supabase functions deploy` CLI silently drops `_shared/` refs on Supabase's `--no-remote` runtime (root cause of the EE3 regression). `User Todos.md` N9 + EE-FOLLOWUP-1 + N6 lines struck through. No code change to the scripts themselves. | `<TBD>` | `phase-night-n9-deploy-scripts-canonical-location` | NEW `scripts/deploy_with_shared.mjs` (copy of `/tmp/deploy_with_shared.mjs`); NEW `scripts/deploy_retell_proxy_bundle.mjs` (copy of `/tmp/deploy_retell_proxy_bundle.mjs`); `Docs/RUNBOOK.md` (§ Deploys rewritten — Edge function single + batch sections); `User Todos.md` (N6 + N9 + EE-FOLLOWUP-1 struck through). | **Code rollback:** `git revert <SHA>` then push. /tmp copies remain until next reboot; otherwise re-copy from any prior commit's path or recreate the scripts from earlier CHANGES_LOG examples. **Recommended:** none — this just files the scripts where they belong. |
| 2026-05-19 | phase-night-n6-remove-paste-in-diagnostic — Removed the syncVoiceSetter voice_id console.log diagnostic at retell-proxy/index.ts:342. Kept past EE5 closeout (2026-05-17) in case the voice paste-in fix at RetellVoiceSelector.tsx needed evidence on the next push; fix has shipped + been verified. 4 lines deleted, no behaviour change. Deployed retell-proxy v16 → v17 via bundle endpoint (HTTP 201 ACTIVE; boot probe 400 expected — verify_jwt=false short-circuits body validation before auth). | `2e4119c` | `phase-night-n6-remove-paste-in-diagnostic` | `frontend/supabase/functions/retell-proxy/index.ts` (4 lines deleted). Deployed retell-proxy v17 via `/tmp/deploy_retell_proxy_bundle.mjs retell-proxy`. | **Code rollback:** `git revert 2e4119c` then redeploy via bundle endpoint — restores the diagnostic log line. **Recommended:** none — pure noise removal. |
| 2026-05-18 | phase-e3-followup-retell-agents-dedupe-versions — Brendan flagged the Voice Agents list on AI Rep Setup page (renamed from Configuration today) as "very cluttered and unuseful." Root cause: Retell's `/list-agents` endpoint returns ONE row per version, and every Save Setter / Push to Retell creates a new version. BFD's `agent_5ec5eb…` has v32-v39 → 8 rows shown for a single agent_id. `RetellAgentsTab.tsx` previously rendered `agents.map(...)` with no dedupe. **Fix:** new `groupedAgents` `useMemo` that buckets the raw `agents` array by `agent_id`, picking the entry with the highest `version` field as the canonical `latest` and keeping all versions in `versions[]` (sorted descending). The render loop now iterates `groupedAgents` and shows ONE Card per unique `agent_id`, displaying: agent_name, slot badges, current version number + relative timestamp ("5m ago" / "2h ago" via `formatDistanceToNow` from date-fns), agent_id (mono small), Published/Draft badge, and a NEW "N versions ▼" button (only shown when versionCount > 1). Clicking the button toggles `expandedHistory` state and reveals a flat list of all versions below the card header (separate from the edit-form expansion already triggered by clicking the card body). Each version row shows: version number, agent_name-at-that-version, voice_id (first 32 chars), is_published badge, relative timestamp. **No backend change** — same Retell API, same data; pure frontend re-aggregation. Existing slot-assignment UI, edit form, save/delete actions preserved unchanged. | `<TBD>` | `phase-e3-followup-retell-agents-dedupe-versions` | `frontend/src/components/retell/RetellAgentsTab.tsx` (added useMemo + formatDistanceToNow import; rewrote rendering loop from `agents.map` to `groupedAgents.map`; new `expandedHistory` state + dropdown UI). Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Restores the cluttered one-row-per-version listing. No data changes. |
| 2026-05-18 | phase-e3-followup-sidebar-fallback-fix-and-hide — Post-audit follow-up. Brendan reviewed the just-shipped sidebar and reported: (a) extra Analytics items still visible in sidebar; (b) Engagement page just "hyperlinks to a campaign/workflow" (not actually true — it's a 3,983-line workflow editor, but it's reachable via Workflows so the dedicated sidebar item is redundant); (c) Templates section duplicates the existing Source Files page (P26) in BACKEND. **Three fixes in this commit:** (1) `ClientLayout.tsx:807-823` — replaced the hardcoded fallback "Text Analytics" + "Voice Analytics" pair (firing when menuConfig is null, e.g., new sub-accounts) with a single "Analytics" entry routing to `/analytics/chatbot/dashboard` (the unified ChatAnalytics page with internal Text/Voice TabsList already merged in Phase A). Eliminates the divergence between hardcoded fallback and DEFAULT_MENU_ITEMS. (2) `useClientMenuConfig.ts` — `engagement` set `visible: false` (page kept at `/workflows/engagement`, reachable via Workflows list). (3) Same — `section-templates`, `voice-rep-templates`, `text-rep-templates` all set `visible: false` (Templates pages kept at their URLs; Source Files in BACKEND already covers the same template downloads as a unified library). **SQL on BFD's client_menu_config:** mirrored the 4 visibility changes so saved config matches new defaults. | `<TBD>` | `phase-e3-followup-sidebar-fallback-fix-and-hide` | `frontend/src/components/ClientLayout.tsx` (hardcoded fallback: 2 Analytics items → 1); `frontend/src/hooks/useClientMenuConfig.ts` (4 visible:false flags). SQL applied to `client_menu_config` row for `e467dabc-...`. Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Restores split Text/Voice Analytics fallback + sidebar Engagement + sidebar Templates section. **SQL rollback:** inverse UPDATE setting visible:true on the 4 keys. |
| 2026-05-18 | phase-e3-followup-merge-logs-tabs — Brendan audit Phase E. Merged 3 separate Logs surfaces (Logs.tsx P20 = 2511 LOC, ErrorLogs.tsx P47 = 651 LOC, RequestLogs.tsx P48 = 1047 LOC; 4209 total) into a unified user-facing experience via a shared tabs nav at the top of each page. Chose the lightweight Option Y (shared tab component, kept 3 separate routes) over Option X (single wrapper page with embedded children) to avoid refactoring 4,200 LOC across 3 pages, each with its own `usePageHeader` call that would conflict if nested. **NEW component `LogsTabsNav.tsx`**: derives active tab from `useLocation().pathname` (suffix match on /error-logs, /request-logs, default activity); on tab change `navigate()` to the corresponding route. shadcn TabsList with 3 triggers. **Inserted at the top of all 3 pages**: Logs.tsx (above the inner activity-tabs which are AI Jobs/Errors/Followups/Outbound-Calls/Bookings — SEPARATE concept, sub-categories within Activity), ErrorLogs.tsx, RequestLogs.tsx. Each page keeps its own usePageHeader so breadcrumbs + title remain accurate. **Sidebar "Logs" item already points at /logs only** — from there user reaches Errors + Requests via this tab nav. **No route changes** — `/logs`, `/error-logs`, `/request-logs` all remain functional (no redirects needed). | `<TBD>` | `phase-e3-followup-merge-logs-tabs` | NEW `frontend/src/components/logs/LogsTabsNav.tsx`; `frontend/src/pages/Logs.tsx` (LogsTabsNav import + tab nav inserted above inner activity-tabs); `frontend/src/pages/ErrorLogs.tsx` (LogsTabsNav import + insertion); `frontend/src/pages/RequestLogs.tsx` (LogsTabsNav import + insertion). No SQL. Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Removes the 3-tab nav from all 3 pages. Routes unaffected. No data changes. |
| 2026-05-18 | phase-e3-followup-templates-sidebar-and-engagement — Brendan audit Phase D. Surfaced 3 previously-orphaned routes by adding sidebar items: (1) new "Engagement" item under OPS (P18, `/workflows/engagement` — cadence canvas editor, critical per `[[reference_cadence_architecture_doc]]` memory), (2) new "TEMPLATES" section between OPS and BACKEND containing Voice AI Rep Templates (P44, `/voice-ai-rep/templates` — Retell agent JSON downloads) and Text AI Rep Templates (P45, `/text-ai-rep/templates` — n8n workflow JSON downloads). Verified both Templates pages are real renderable content (95-line + 106-line files with Card/CardHeader UI listing downloadable assets) before flipping the sidebar — not stubs. **useClientMenuConfig.ts:** DEFAULT_MENU_ITEMS updated — added 4 new entries (engagement at position 14, section-templates label at 17, voice-rep-templates at 18, text-rep-templates at 19); shifted BACKEND section + items from 16-20 → 20-24 to make room. MENU_ROUTE_MAP gains `voice-rep-templates` and `text-rep-templates` entries (engagement was already there). **SQL on BFD's client_menu_config:** appended 4 items using decimal positions (engagement 13.5, section-templates 15.5, voice-rep-templates 15.6, text-rep-templates 15.7) so they slot into the right gaps without conflicting with BFD's existing integer positions on Logs/BACKEND/etc. (the hook sorts by position client-side). Pre-check confirmed none of the 4 keys existed in BFD's saved config before insert. | `<TBD>` | `phase-e3-followup-templates-sidebar-and-engagement` | `frontend/src/hooks/useClientMenuConfig.ts` (4 new DEFAULT_MENU_ITEMS + 5 position shifts on existing BACKEND-section items + 2 new MENU_ROUTE_MAP entries). SQL applied to `client_menu_config` row for `e467dabc-...`. Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Restores old DEFAULT_MENU_ITEMS + MENU_ROUTE_MAP. **SQL rollback:** `UPDATE client_menu_config SET menu_items = (SELECT jsonb_agg(item) FROM jsonb_array_elements(menu_items) AS item WHERE item->>'key' NOT IN ('engagement','section-templates','voice-rep-templates','text-rep-templates')) WHERE client_id='e467dabc-...';` |
| 2026-05-18 | phase-e3-followup-rename-airep-setup — Brendan audit Phase C. Renamed the two AI Rep "Configuration" pages to "Setup" — these are one-time onboarding wizards (multi-phase setup-guide with VoiceAIRepSetupGuide / TextAIRepSetupGuide dialogs), distinct from the daily prompt-editing surface in PromptManagement.tsx. Old name implied recurring configuration; new name signals one-time-setup. `git mv frontend/src/pages/VoiceAIRepConfiguration.tsx → VoiceAIRepSetup.tsx`; same for Text. Internal class `VoiceAIRepConfiguration` → `VoiceAIRepSetup`; same for Text. **App.tsx routes:** new canonical `text-ai-rep/setup` + `voice-ai-rep/setup`. Old `text-ai-rep/configuration` + `voice-ai-rep/configuration` kept as `<Navigate to="../setup" replace />` aliases so previously-shipped AI REP CONFIG button + existing bookmarks resolve. Also updated the index redirects (`text-ai-rep` → `setup`, `voice-ai-rep` → `setup`). The `/api/configuration` legacy alias also now targets `TextAIRepSetup`. **PromptManagement.tsx:** AI REP CONFIG button target updated from `/voice-ai-rep/configuration` / `/text-ai-rep/configuration` → `/voice-ai-rep/setup` / `/text-ai-rep/setup`. Plus a stale code comment updated to reference the new component names. No SQL change. | `<TBD>` | `phase-e3-followup-rename-airep-setup` | 2 files git-mv'd; `frontend/src/App.tsx` (2 lazy imports renamed, 2 new `/setup` routes, 2 legacy `/configuration` redirects, 1 button target). `frontend/src/pages/PromptManagement.tsx` (button onClick target + 1 comment). Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Restores old names + routes. **Manual partial revival:** `git mv` files back + update App.tsx imports/routes. |
| 2026-05-18 | phase-e3-followup-archive-webinar-legacy — Brendan audit Phase B. Eight pages git-mv'd to `frontend/src/pages/_archived/` (no deletes per his "don't delete anything at this stage" instruction). The 7 Webinar pages (P59 WebinarSetup.tsx, P60 WebinarChecklist.tsx, P61 WebinarSetupLinks.tsx, P62 WebinarAnalytics.tsx, P63 WebinarAnalyticsEnhanced.tsx, P64 WebinarPresentationAgent.tsx, P65 PresentationAgent.tsx) — Brendan confirmed Webinar product is dormant. Plus P83 VoiceAISetter.tsx (explicit `/voice-ai-setter-legacy` route name). **App.tsx:** removed 8 `lazyRetry()` imports + 7 `<Route path="webinar-setup/...">` entries + the `<Route path="webinar-presentation-agent">` + the `<Route path="voice-ai-setter-legacy">`. Preserved comment markers in App.tsx showing where these were so revival is easy. Preserved the `/voice-ai-setter` → `/prompts/voice` redirect (still useful). Pre-archive ref check showed: PresentationAgent referenced in ClientLayout.tsx is just a pathname string match (`location.pathname.includes("/webinar-presentation-agent")`) not an import — safe. The webinar-related components (WebinarSetupGuideDialog, PresentationAgentChatInterface, WebinarPresentationAgentChatInterface) are now orphaned but not archived — kept in case the pages are revived. **No SQL change** (these routes aren't in any sidebar menu_config). | `<TBD>` | `phase-e3-followup-archive-webinar-legacy` | 8 page files git-mv'd from `frontend/src/pages/` to `frontend/src/pages/_archived/`; `frontend/src/App.tsx` (removed 8 lazy imports + 8 routes; preserved comments). Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Restores 8 pages + their lazy imports + routes. No data changes. **Manual partial revival:** `git mv frontend/src/pages/_archived/<Page>.tsx frontend/src/pages/<Page>.tsx` + restore lazy import + `<Route>` in App.tsx. |
| 2026-05-18 | phase-e3-followup-sidebar-analytics-cleanup — Brendan audit Phase A. Two cleanup moves: (1) Hide Analytics v2 from sidebar (he confirmed it was visible + unused — experimental metric builder backed by analytics-v2-process / analytics-v2-suggest-widgets edge fns, kept routed at `/analytics-v2` for direct access in case revived later). (2) Merge Text Analytics + Voice Analytics into a single sidebar item — they both pointed at the same `ChatAnalytics.tsx` component (different default tab via `isVoice = location.pathname.includes('/voice-ai/')`). Now there's one "Analytics" sidebar item that lands on the default text view, and a new Text/Voice TabsList at the top of the page navigates between the two URL variants while preserving the dashboard / chat-with-ai sub-route. **useClientMenuConfig.ts:** `analytics` label renamed "Text Analytics" → "Analytics"; `analytics-voice` set `visible: false` + label suffix "(legacy)"; `analytics-v2` set `visible: false`. Both kept in MENU_ROUTE_MAP for backward-compat with saved configs that explicitly re-enabled them. **ChatAnalytics.tsx:** new Tabs (with shadcn TabsList/TabsTrigger imports already present) inserted at line 2683 — `value={isVoice ? 'voice' : 'text'}`, onValueChange builds the target route (channel × suffix). **SQL on BFD's client_menu_config row:** mirrored the three changes so the cached saved config reflects new state. **No archived/deleted code yet** (Brendan's "don't delete anything at this stage" stands). | `<TBD>` | `phase-e3-followup-sidebar-analytics-cleanup` | `frontend/src/hooks/useClientMenuConfig.ts` (3 menu items adjusted); `frontend/src/pages/ChatAnalytics.tsx` (Channel TabsList added at top of return JSX, navigates between /analytics/chatbot/* and /analytics/voice-ai/* preserving chat-with-ai sub-route). SQL applied to `client_menu_config` row for `e467dabc-...`. Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Reverts hide-v2 + analytics-voice removal + analytics rename + ChatAnalytics tab UI. **SQL rollback:** re-run the inverse UPDATE setting `analytics-v2.visible = true`, `analytics-voice.visible = true` + `analytics-voice.label = "Voice Analytics"` + `analytics.label = "Text Analytics"`. |
| 2026-05-18 | phase-e3-followup-inline-rename-and-rep-nav — Brendan testing feedback: (a) "the setter title still isn't there, not in the setter edit page anymore" — the SetterDisplayNamesCard I dropped in the editor was treated as not-present-enough; he wants the rename to happen on the heading itself, click-to-edit. (b) "on the screenshot where it says SETTER-1, I should be able to hover and a pencil should come up" — wants inline-edit on the card title. (c) "the same field for at the top where it says VOICE SETTER > SETTER-1" — wants the breadcrumb to reflect the rename too. (d) "add a way to click into the AI Rep configuration page for voice and text — should be a card at the top, similar and next to refresh tool messages button." **NEW component `InlineSetterNameEditor.tsx`:** click-to-edit heading with pencil-on-hover affordance. Reads `clients.setter_display_names[setterKey(kind, slot)]` from `useClientCredentials`. Edit mode shows an inline `<input>` (autofocused + select-all) bordered by primary; enter or check icon commits, escape or X cancels, blur commits. On commit: dual-write to setter_display_names AND (voice only) PATCH Retell agent_name via retell-proxy `set-agent-name` action. Toast variants: success, Retell-push-warning, error. **Wired in 3 places (same component, same data):** (1) Front Voice/Text Setter list cards — replaced static `<CardTitle>{slot.staticName}</CardTitle>` with the inline editor (fallback to staticName when no custom name set); existing small-grey-subtitle-with-staticName preserved when a custom name IS set. (2) Editor body — replaced the full SetterDisplayNamesCard surface with a compact "SETTER NAME" grooved card containing the inline editor inline. (3) Editor breadcrumb — labels at top of header now use `editorBreadcrumbLabel` which prefers `setter_display_names` lookup, falls back to `headerBadgeInfo.label || editingSlotId`. Breadcrumb is read-only (not click-editable) but reflects the rename so visual consistency holds across card + editor body + breadcrumb. **New page-header action "AI REP CONFIG"** added to the Voice Setter and Text Setter list views, positioned between REFRESH TOOL MESSAGES and CREATE NEW SETTER. Navigates to `/client/<clientId>/voice-ai-rep/configuration` or `/client/<clientId>/text-ai-rep/configuration` respectively. These pages have the full Retell config + the now-canonical SetterDisplayNamesCard + ClientTimezoneCard. | `<TBD>` | `phase-e3-followup-inline-rename-and-rep-nav` | NEW `frontend/src/components/setters/InlineSetterNameEditor.tsx`; `frontend/src/pages/PromptManagement.tsx` (removed unused SetterDisplayNamesCard import; added InlineSetterNameEditor import; replaced 3 surfaces: editor body card, voice list CardTitle, text list CardTitle; new `editorBreadcrumbLabel` computed via setter_display_names lookup + applied to breadcrumb; new AI REP CONFIG action button). Backend unchanged (`set-agent-name` was shipped in the previous tag — this just wires more callers to it). Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Removes inline-edit affordance from cards + editor body + breadcrumb dynamic label + AI REP CONFIG button. The SetterDisplayNamesCard route via `/voice-ai-rep/configuration` and `/text-ai-rep/configuration` still works (canonical write path). **Recommended:** none — pure UX additive. |
| 2026-05-18 | phase-e3-followup-voice-setter-page-surface — Brendan's nav reality check. The previous cleanup added ClientTimezoneCard + enhanced SetterDisplayNamesCard on `/client/<id>/voice-ai-rep/configuration` (VoiceAIRepConfiguration) — but that route is NOT linked from the sidebar Brendan uses. His primary nav goes through the VOICE SETTER sidebar item which routes to `/client/<id>/prompts/voice` (PromptManagement.tsx). So none of the previous cleanup was discoverable from where Brendan actually works. **Three additive fixes on PromptManagement.tsx:** (1) Drop `ClientTimezoneCard` at the TOP of the list view (above both voice + text setter card grids) so the TZ Select is visible the moment the page loads. Same source of truth (clients.timezone), same component used on the orphaned config pages — no schema change, no duplicate logic. (2) Reuse `SetterDisplayNamesCard` INSIDE the editor view (above the EE1 direction toggle), passing `slots={[{slot: N}]}` for just the current slot. This restores the "rename here" capability Brendan asked for after my Phase 7 revert — but routes through the canonical setter_display_names path so the name propagates to front cards, Logs, Simulator, OutboundCallProcessing AND pushes to Retell agent_name on blur via set-agent-name. Kind is parsed from editingSlotId prefix (`Voice-Setter-N` → voice, `Setter-N` → text); slot number parsed from the suffix (`Setter-followup` maps to slot 11). (3) No changes to write paths — both reuses just consume existing components. **Per-setter TZ deferred:** Brendan asked if TZ should be per-setter. Decision is client-level for now (simpler — voice-booking-tools, make-retell-outbound-call, retell-proxy DYNAMIC_VARS_BLOCK all read clients.timezone today; per-setter would require touching all three plus cadence quiet-hours and the prompts.directions schema). Can add a per-slot override later via `prompts.timezone_override text NULL` if a real multi-zone customer emerges. | `<TBD>` | `phase-e3-followup-voice-setter-page-surface` | `frontend/src/pages/PromptManagement.tsx` (3 imports added — SetterDisplayNamesCard, ClientTimezoneCard, plus existing useClientCredentials/setterKey were already there; ClientTimezoneCard inserted at top of list view; SetterDisplayNamesCard inserted at top of editor view with slot-derivation IIFE). No backend changes. No schema changes. Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then push. Removes the two card surfaces from the Voice Setter page (Brendan would need to navigate to /voice-ai-rep/configuration which has no sidebar link). **Recommended:** none — this is pure surface/discovery additive, no behavior change. |
| 2026-05-18 | phase-e3-followup-setter-rename-cleanup — Cleanup after Brendan's testing of the Phase 5 SETTER NAME Input revealed three problems: (1) "doesn't propagate anywhere" — my Input wrote to `prompts.name` but the front list card reads from AgentConfigBuilder's `agent_name` config + Logs/Simulator read from `clients.setter_display_names`. THREE overlapping naming systems with no convergence. (2) "doesn't push to be saved" — my Input didn't trigger `markNeedsSync`, so `hasUnsavedChanges` stayed false and the SAVE button stayed disabled. (3) "Save Setter errors with push to retell" — root cause: retell-proxy's `syncVoiceSetter` had 3 try/catch blocks around the auto-publish step that silently swallowed errors, so when publish-agent transiently failed, the UI got a vague "edge fn non-2xx" toast and the agent was left at the new draft version without being published live. **Fixes in this commit:** (a) REVERTED the Phase 5 SETTER NAME Input in PromptManagement.tsx editor (it was the wrong layer for renaming). (b) ENHANCED the existing `SetterDisplayNamesCard` (already used by VoiceAIRepConfiguration + TextAIRepConfiguration + Logs + Simulator + OutboundCallProcessing) to also push the name to Retell as `agent_name` on blur — for voice kind only. Calls a NEW retell-proxy action `set-agent-name` (lightweight: PATCH agent_name + publish-agent + repoint phone version; NEVER touches LLM prompt or voice settings). If the slot has no Retell agent yet, returns `skipped_no_agent` and the UI just saves the local display name. If publish-agent fails, returns `patched_but_publish_failed` with the error message so the toast can warn explicitly. (c) UPDATED the front list cards in PromptManagement.tsx (both voice + text branches) to use `setter_display_names[setterKey(kind, slotNumber)]` as the CardTitle, falling back to `slot.staticName` when empty. Empty staticName shows below as small grey text so users still see the slot ID. (d) FIXED the silent auto-publish swallow in syncVoiceSetter — added a `publishWarning` variable captured in the 3 try/catch blocks and surfaced in the return values as `publish_warning` field. (e) NEW `ClientTimezoneCard` component dropped into both VoiceAIRepConfiguration and TextAIRepConfiguration (Brendan reported "can't see where this is" for the ClientSettings Select — there's no sidebar link to /client/<id>/settings). Same 17 IANA options as the ClientSettings Select. Saves on change directly to `clients.timezone`. ClientSettings Select retained for parity. | `<TBD>` | `phase-e3-followup-setter-rename-cleanup` | `frontend/supabase/functions/retell-proxy/index.ts` (new `set-agent-name` case at line ~895; new `publishWarning` variable + 3 catch blocks updated; 3 return statements include `publish_warning` field); `frontend/src/pages/PromptManagement.tsx` (reverted Phase 5 SETTER NAME Input; added useClientCredentials + setterKey imports; added setterDisplayNames lookup; both list-card CardTitle blocks updated to derive displayName from setter_display_names with slotMatch regex); `frontend/src/components/setters/SetterDisplayNamesCard.tsx` (handleBlur now takes slot param; voice kind invokes retell-proxy set-agent-name on save; toast variants for success/warning/error based on Retell response); `frontend/src/components/setters/ClientTimezoneCard.tsx` (NEW); `frontend/src/pages/VoiceAIRepConfiguration.tsx` (renders ClientTimezoneCard above SetterDisplayNamesCard); `frontend/src/pages/TextAIRepConfiguration.tsx` (same). Deployed retell-proxy v16 via bundle endpoint. Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then redeploy retell-proxy via bundle endpoint. Reverts: (a) restores the Phase 5 SETTER NAME Input (with original misalignment); (b) reverts SetterDisplayNamesCard to local-save-only (no Retell push); (c) reverts front cards to slot.staticName; (d) reinstates silent publish swallow; (e) removes ClientTimezoneCard from both rep pages. **Recommended:** none — this resolves real user pain reported in testing. |
| 2026-05-18 | phase-e3-followup-phone-first-contact-lookup — Brendan's testing ask #4: "Have it use the incoming number to search for contact details instead of email. This is mostly needed for inbound calls and will mostly be used for when a lead calls back a missed call. This also needs to be in the code and master prompt for how it is built, so it works with all new clients." Backend already supported phone-first (`voice-booking-tools/index.ts:133-243 resolveContactId` phone-first → email-fallback; lines 564-693 `toolLookupContact` returns `match_quality`; lines 730-736 auto-injects `call.from_number` into body.phone). The agent's prompt didn't know to use it. **Master template** (`frontend/src/data/bfdVoiceSetterPrompt.ts`) rewritten in three places: (a) BOOKING FLOW Step 6 now says "Inbound: call lookup-contact with no args (phone auto-injected) → confirm by name → if match_quality='phone', NO email needed. Outbound: skip lookup, dynamic vars known. Fallback to email only when phone lookup returns 'none'." (b) New Step 6.5 "email mismatch handling" tells agent to confirm if lookup-by-phone returned a contact whose email differs from what caller states (Brendan-approved option (b) "ask to confirm"). (c) DYNAMIC VARIABLES ARE NOT AVAILABLE ON INBOUND CALLS section now clarifies that **phone IS available** via auto-injection — explicitly called out the missed-call callback use case Brendan described. (d) AVAILABLE TOOLS section promotes `lookup-contact` to PREFERRED, deprecates `get_contact`, updates `book-appointments`/`get-contact-appointments`/`update-appointment` to accept phone OR email. **Affects new clients only** (template is seed for new client provisioning). **For BFD specifically:** the live prompt won't have these instructions until BFD's `prompts.content` (the 44,888-char per-client AgentConfigBuilder state) is updated. The conflict: BFD's content currently says "The prospect's details (name, email, phone, business) are already loaded - never ask for information you already have" + lists {{first_name}}/{{email}}/{{phone}} as pre-loaded — which is FALSE for inbound BYO Twilio calls. **Per Brendan's "don't change anything else" instruction, NOT surgically SQL-patched.** Brendan should either (i) edit via the AgentConfigBuilder UI's relevant section, (ii) regenerate via the "MODIFY SETTER WITH AI" button, or (iii) tell Claude next session to apply a surgical SQL patch. The new TZ-aware DYNAMIC_VARS_BLOCK (Phase 4 above) auto-appended by retell-proxy v15 DOES include phone-first guidance — that block ships on every Push to Retell — so BFD's next push will at minimum have the auto-appended phone-first note even if the main prompts.content is unchanged. | `<TBD>` | `phase-e3-followup-phone-first-contact-lookup` | `frontend/src/data/bfdVoiceSetterPrompt.ts` (BOOKING FLOW Steps 6 + 6.5 + 7 rewritten; DYNAMIC VARIABLES section updated; AVAILABLE TOOLS section updated — 5 tools now document phone OR email body; new `lookup-contact` tool documented as PREFERRED). Backend unchanged. No edge fn redeploy needed (Phase 4 already shipped retell-proxy v15). | **Code rollback:** `git revert <SHA>` then push. Reverts the master template only — does not affect any live LLM (live LLMs only update on next UI push or script-deploy). **Recommended:** none — purely additive guidance + repositioning. |
| 2026-05-18 | phase-e3-followup-setter-name-editor — Adds an editable "Setter Name" Input at the top of the Voice/Text Setter editor in `frontend/src/pages/PromptManagement.tsx`. Brendan's testing ask #2: "is there a way to change the name of the setters and have that push through to retell ai as well? That would help with labeling and naming conventions." Surprisingly the underlying plumbing was ALREADY there: `promptContent.title` is in state, saved to `prompts.name` via the existing upsert path (lines 5126-5168), and passed as `agentName` in the sync-voice-setter payload (line 6055), which retell-proxy already uses to PATCH Retell's `agent_name` (line 531, 549, 587). The missing piece was a visible Input bound to `promptContent.title` — there wasn't one, so Brendan had no way to actually edit the name. The new Input is placed in a grooved card at the very top of the editor body (above the EE1 direction toggle), shown on every editor view (voice + text setters), with a `htmlFor`-linked Label "SETTER NAME" and helper text explaining it pushes to Retell as agent_name on next Save Setter. Placeholder shows `editingSlotId` (e.g. "Voice-Setter-1") so Brendan sees the fallback if blank. **No schema change** — `prompts.name` already exists. **No edge fn change** — retell-proxy already routes `agentName` to `agent_name`. **No new column `display_name` needed** — the prior plan called for one but inspection showed `name` already serves this purpose. Multi-tenant safety: Retell agent names are scoped per-API-key (per-tenant), so different clients can name a setter "Sales Closer" without collision. | `<TBD>` | `phase-e3-followup-setter-name-editor` | `frontend/src/pages/PromptManagement.tsx` (new SETTER NAME card above direction toggle, ~25 lines). Frontend auto-deploys via Railway on push. No backend changes. | **Code rollback:** `git revert <SHA>` then push. Removes the visible Input but does NOT clear any existing names users may have set — `prompts.name` would still hold whatever was last typed. **Recommended:** none — pure feature add, no behavior change for users who don't touch the field. |
| 2026-05-18 | phase-e3-followup-tz-propagation — Fixes the TZ confusion Brendan flagged in the 2026-05-18 inbound booking test ("Monday May 18" said for tomorrow when today IS Monday May 18 in Sydney). Two-layer fix: **retell-proxy v15** (`frontend/supabase/functions/retell-proxy/index.ts`) replaces the hardcoded `Current Date & Time (ET)` line in DYNAMIC_VARS_BLOCK with `Current Date & Time (${clientTimezone})` sourced from `clients.timezone` (fetched alongside intake_lead_secret in syncVoiceSetter). Also adds a new `Caller Timezone (IANA)` line + an "When dynamic variables are EMPTY (common on inbound calls)" section that instructs the agent: never guess day-of-week, call get-available-slots to discover today's date, use call.from_number for caller identity, default to client timezone for "X time" phrasing. This addresses the root cause that inbound BYO Twilio calls receive empty dynamic vars and the agent was hallucinating today's date in ET. **ClientSettings.tsx UI** adds a Timezone Select after the Description field with 17 IANA options (Australia/Sydney default, AU/NZ/US/EU/Asia coverage + UTC). State + load + save flows updated. Helper text explains it drives booking time formatting, cadence quiet-hours, and what the voice agent says. Brendan can now self-serve TZ changes per sub-account without SQL. Note: existing live agents won't pick up the new DYNAMIC_VARS_BLOCK until they're re-pushed via "Push to Retell" in the UI (or syncVoiceSetter is invoked) — this is expected, the block is composed at push time. | `<TBD>` | `phase-e3-followup-tz-propagation` | `frontend/supabase/functions/retell-proxy/index.ts` (clients SELECT adds timezone, DYNAMIC_VARS_BLOCK now templated with clientTimezone + inbound empty-vars instructions); `frontend/src/pages/ClientSettings.tsx` (Select import, clientData.timezone state, SELECT/UPDATE includes timezone column, Timezone Select UI block). Deployed retell-proxy v15 via bundle endpoint. Frontend auto-deploys via Railway. | **Code rollback:** `git revert <SHA>` then redeploy retell-proxy via bundle endpoint — restores the hardcoded (ET) string in DYNAMIC_VARS_BLOCK + removes the empty-vars guidance. Existing pushed agents are NOT affected by revert; they retain whatever they had at last push time. **Recommended:** none — this fixes a documented production bug. |
| 2026-05-18 | phase-e3-followup-test-call-error-ux — UX hardening for the TEST CALL button in the Voice AI Setter editor. During the 2026-05-18 smoke test Brendan opened the TEST CALL modal, filled in phone + names + email, clicked "Make Test Call" and got a useless "Edge Function returned a non-2xx status code" toast — the actual backend message ("No Retell agent configured for voice-setter-1") was lost because the supabase-js v2 client wraps non-2xx responses in a `FunctionsHttpError` whose `.message` is the generic "Edge Function returned a non-2xx status code" string; the parsed JSON body lives on `.context` instead. **Backend** (`frontend/supabase/functions/make-retell-outbound-call/index.ts`): four validation early-returns now emit structured `{error, code, hint, slot_id, slot_number}` JSON bodies — `no_retell_api_key` (HTTP 409 with hint pointing at API Credentials), `no_agent_for_slot` (HTTP 409 with hint to push voice setter first), `invalid_voice_setter_id` (400), `no_contact_phone` (400 with hint about country code). Deployed v10 → v11 via bundle endpoint (no `_shared` deps but bundle endpoint is the now-default for safety). **Frontend** (`frontend/src/components/TestCallDialog.tsx`): the modal's error handler now extracts `error.context.json()` (or `error.context.text()` fallback) to surface the backend's structured `error` message + `hint` as a toast description with 10s duration, instead of the generic `error.message`. Same logic applied to the existing `data?.error` branch so structured errors returned in the 2xx response body also surface the hint. Falls back to `error.message` when the body isn't parseable. | `<TBD>` | `phase-e3-followup-test-call-error-ux` | `frontend/supabase/functions/make-retell-outbound-call/index.ts` (4 validation paths enhanced with `code`+`hint`+structured fields; 2 paths bumped from 400 → 409); `frontend/src/components/TestCallDialog.tsx` (error handler now parses `error.context` body; both error branches show `hint` as toast description). Deployed make-retell-outbound-call v11 via `/tmp/deploy_retell_proxy_bundle.mjs make-retell-outbound-call`. Frontend auto-deploys via Railway on push. | **Code rollback:** `git revert <SHA>` then redeploy via bundle endpoint — restores the generic-toast behaviour. **Recommended:** none — this is a pure UX improvement with no behaviour change in the happy path (the actual call dial flow is unchanged from line 446+). |
| 2026-05-18 | phase-e3-followup-ee1-safety-guard — **Root-cause fix** for the 2026-05-18 wipe incident. Added a structured safety guard in `syncVoiceSetter` (`frontend/supabase/functions/retell-proxy/index.ts`) that runs immediately after resolving `existingAgentId` and BEFORE any Retell mutation. The guard fetches all 10 `clients.retell_*_agent_id` columns at once, computes which columns currently point at this slot's `existingAgentId`, and if more than one column shares the agent AND the push's selected `directions` don't claim every shared column, the function throws an `agent_shared_across_slots` error with HTTP 409 + `shared_columns` + `conflicting_agent_id` in the JSON body. This catches the exact incident scenario (slot 2 pushes with `directions=[]` while shared agent is also slot 1's anchor → `fanOutDirections` clear-loop would release slot 1's column from the agent → next push wipes shared LLM). Same change extended the outer catch handler so any structured error with a `.status` field returns that code instead of being flattened to 400, and includes `code`/`shared_columns`/`conflicting_agent_id` in the JSON body. **Toggle UI fix** in `frontend/src/pages/PromptManagement.tsx` (Phase 2.2 same commit): the 3 direction `ToggleGroupItem` components in the Voice AI Setter editor now have explicit `data-[state=on]:!bg-green-500 !text-white !border-green-600` classes so active toggles are unmistakably green — Brendan reported he couldn't tell which directions were on, which contributed to the wipe scenario (he didn't realize slot 2 had empty selection). Default border-2 keeps inactive state visible. **Frontend toast** for the new 409: PromptManagement's push-to-retell flow now detects `safetyCode === 'agent_shared_across_slots'` (reading from `retellResult.code` or `retellError.context.body.code`) and shows a 12s destructive toast with the actionable recovery message ("delete this voice-setter slot and recreate so a fresh agent is provisioned") instead of the generic "Retell sync warning". | `<TBD>` | `phase-e3-followup-ee1-safety-guard` | `frontend/supabase/functions/retell-proxy/index.ts` (safety guard before line 519 if/else; outer catch returns structured status + body); `frontend/src/pages/PromptManagement.tsx` (3 ToggleGroupItem classNames + safetyCode toast branch at line ~6101). retell-proxy deployed via `/tmp/deploy_retell_proxy_bundle.mjs` bundle endpoint → v14 ACTIVE. Frontend auto-deploys via Railway on push. | **Code rollback:** `git revert <SHA>` then redeploy retell-proxy via bundle endpoint (this would re-enable the wipe vulnerability). **Recommended:** none — this restores defence-in-depth against the 2026-05-18 incident class. If a legitimate push is blocked by the guard, recovery is: (a) Delete the slot via the Delete Voice Setter button (clears the column), (b) recreate the slot — retell-proxy will provision a fresh agent, (c) push the new config. |
| 2026-05-18 | phase-e3-followup-ee1-incident-recovery — **INCIDENT.** Brendan's Stage 0 smoke test pushed Voice-Setter-2 (empty config, `prompts.content` 340 chars, `directions=[]`) to Retell. The EE1 `fanOutDirections` helper resolved slot 2's "primary anchor" agent_id from `clients.retell_outbound_agent_id`, found `agent_5ec5eb…` (the SAME agent slot 1 was actively serving — shared because the Phase A bug #2 hotfix on 2026-05-09 pointed all 3 `clients.retell_*_agent_id` columns at one agent), pushed slot 2's empty config (663-char dynamic-vars stub) to the shared LLM `llm_22e795de…`, and then ran the release loop at retell-proxy/index.ts:243-255 which — with `valid=[]` because slot 2 had no directions selected — cleared every column pointing at that agent. Damage: live LLM general_prompt 11,386 chars → 663 chars (Gary persona, TZ instructions, booking flow, tool docs all gone); LLM model gemini-3.0-flash → gpt-5.2; agent name Voice-Setter-1 → Voice Setter 2; all 3 `clients.retell_*_agent_id` columns NULL; orphan agent `agent_d52a138405881d8ae0f3869411` (Voice-Setter-2, 11labs-Myra) + orphan `llm_d193ceb5dbc2f7d3cba88a0f6192` created; orphan `prompts` row `98651fc3-…` (Voice-Setter-2 stub) created. Despite this, the most recent inbound call still booked (`call_8b2b88a6a0e41b18a43b5354bea`, `appointment_booked=true`) but exhibited TZ + identity confusion ("Monday May 18" mid-call when it's Sunday in Sydney). **Recovery executed via `scripts/recover_bfd_voice_2026_05_18.mjs`:** PATCH LLM with Gary v3 prompt (11,386 chars from `bfdVoiceSetterPrompt.ts`) + `model=gemini-3.0-flash` → PATCH agent `agent_name="Voice-Setter-1"` → POST publish-agent (minted v37, is_published=true) → PATCH phone `+61481614530` with `inbound_agent_version=37` + `outbound_agent_version=37` → DELETE orphan agent + orphan LLM. SQL: restored 3 `clients.retell_*_agent_id` columns (all = `agent_5ec5eb…`), set `prompts.directions = ARRAY['inbound','outbound_initial','outbound_followup']` for Voice-Setter-1 row `45c501f6-…`, DELETE orphan Voice-Setter-2 prompts row `98651fc3-…`. **Post-recovery verification:** v37 published with Voice-Setter-1, gemini-3.0-flash, 11386 chars, contains "Gary"/"Sydney time"/"Australia/Sydney", zero "Eastern"/"America/New_York" leaks; phone pinned to v37 for both inbound + outbound; orphan agent GET returns 404; DB clients + prompts restored. **No platform code changes** — Phase 2 in the same session ships the safety guard that prevents this incident class from recurring. | `<TBD>` | `phase-e3-followup-ee1-incident-recovery` | `scripts/recover_bfd_voice_2026_05_18.mjs` (new — captures the recovery sequence for audit + reuse); `Docs/CHANGES_LOG.md` (this row). SQL run via Supabase Management API SQL runner, not in a migration file (one-shot data restore for BFD only). Retell API mutations not in code. | **Code rollback:** the script is idempotent — re-running it after another wipe restores again. **State rollback (recreate the broken state):** NOT recommended — requires re-running the EE1 wipe scenario without the Phase 2 safety guard, which would gut the live agent again. To revert SQL only: `UPDATE clients SET retell_inbound_agent_id=NULL, retell_outbound_agent_id=NULL, retell_outbound_followup_agent_id=NULL WHERE id='e467dabc-…';` (would break TEST CALL + cadence). |
| 2026-05-17 | phase-e3-followup-time-bomb-sweep — Proactive sweep to neutralise the remaining "time-bomb" edge functions. After the EE3 regression + platform audit fixed 34 functions, a second platform-wide scan found 36 MORE functions that boot today (because their original CLI deploys pre-resolved imports) but would fail with BOOT_ERROR on the NEXT simple-PATCH redeploy — the same class of bug we already fought twice this session. Migrated all 35 remaining `https://esm.sh/PKG@VER` imports to `npm:PKG@VER` and redeployed every one of the 36 (35 esm.sh users + 1 `_shared`-only user `compute-analytics`) via the multi-file bundle endpoint with `_shared/*.ts` attached. Critical functions in the sweep include `voice-booking-tools` (the function the Retell agent calls for slot lookup + booking — was the proven-working path last night), `bookings-webhook` (GHL appointment callback), `sync-ghl-contact`, `intake-lead`, `trigger-engagement`, `twilio-send-sms`. **Post-sweep platform probe: 0/81 BOOT_ERROR.** Same playbook as the regression-fix commit but applied to functions that hadn't yet been redeployed in this session. No code logic changed — only import path strings. | `2c833bb` | `phase-e3-followup-time-bomb-sweep` | 35 fns esm.sh→npm: substitutions (bookings-webhook, campaign-enroll-webhook, check-reset-eligibility, create-client-user, crm-send-message, delete-external-prompt, elevenlabs-manage-agent, fetch-thread-previews, fix-business-messages, get-campaign-stats, get-chat-history, intake-lead, kb-ingest, list-external-setter-slots, outbound-call-processing, push-contact-to-ghl, refresh-usage-cache, restore-setter-config, save-external-prompt, stop-bot-webhook, stop-engagement, supabase-project-usage, sync-ghl-contact, trigger-engagement, twilio-configure-webhook, twilio-inbound-sms, twilio-list-numbers, twilio-send-sms, twilio-status-webhook, unipile-proxy, unipile-webhook, update-client-password, voice-booking-tools, workflow-execute, workflow-inbound-webhook). All 36 redeployed via bundle endpoint. | **Code rollback:** `git revert <SHA>` then redeploy via simple PATCH on each — would reintroduce the time-bomb state. **Recommended:** none — this restores future-proof state. |
| 2026-05-17 | phase-e3-followup-edge-fn-platform-audit — Full-platform boot-health audit triggered after the EE3 regression. Probed all 81 edge functions, found 4 additional CRITICAL-PATH functions still in BOOT_ERROR state from prior simple-PATCH deploys (not just the EE3-touched 30): `retell-call-webhook` (Retell event webhook — every call event), `make-retell-outbound-call` (cadence outbound calls), `receive-twilio-sms` (inbound SMS reception), `retell-call-analysis-webhook` (already addressed earlier in this session but had esm.sh import that re-broke it after the EE4 deploy). Fixed by: (a) esm.sh→npm: migration in retell-call-webhook + make-retell-outbound-call + retell-call-analysis-webhook (3 single-line edits); (b) re-deploying receive-twilio-sms via the multi-file bundle endpoint with `_shared/*.ts` attached (it was already on `npm:` but its `pushSmsToGhl` import from `../_shared/ghl-conversations.ts` was lost in a prior simple-PATCH). Post-fix full-platform probe: 0/81 BOOT_ERROR. Pinned deploy template guidance: any function importing from `../_shared/` must deploy via `/tmp/deploy_with_shared.mjs`, never the simple PATCH template. | `3222cda` | `phase-e3-followup-edge-fn-platform-audit` | `frontend/supabase/functions/retell-call-webhook/index.ts`, `frontend/supabase/functions/make-retell-outbound-call/index.ts`, `frontend/supabase/functions/retell-call-analysis-webhook/index.ts` (esm.sh→npm:). receive-twilio-sms code body unchanged; redeployed via bundle endpoint. | **Code rollback:** `git revert <SHA>` then redeploy via simple PATCH — would reintroduce BOOT_ERROR on these 3. **Recommended:** none — this restores critical-path functions that were silently broken. |
| 2026-05-17 | phase-e3-followup-ee3-regression-fix — Fixes a regression introduced by my own EE3 sweep. Probe of all 30 migrated functions found 27/30 returning HTTP 503 BOOT_ERROR. **Two root causes:** (1) Supabase's `--no-remote` edge runtime flag blocks `https://esm.sh/*` AND `jsr:*` imports, not just `deno.land/std`. (2) The Management API simple PATCH endpoint (`PATCH /v1/projects/{ref}/functions/{slug}`) only deploys the entrypoint body, losing `../_shared/*.ts` references that the CLI's previous deploys had bundled. **Fixes shipped:** (a) Sed-migrated every `https://esm.sh/PKG@VER` to `npm:PKG@VER` across all 30 function index.ts files (Supabase resolves `npm:` natively). (b) Removed the unused `import "https://deno.land/x/xhr@0.1.0/mod.ts"` polyfill from generate-ai-prompt — was a leftover for an old @supabase/supabase-js shim. (c) Inlined a small base64 encoder in process-lead-file to replace `jsr:@std/encoding/base64`. (d) Switched to the Management API's multi-file bundle endpoint (`POST /v1/projects/{ref}/functions/deploy?slug=<name>` with `multipart/form-data`) and pushed all 30 functions WITH the 4 `_shared/*.ts` modules (assert-client-access, business-hours, ghl-conversations, request-logger) attached. Post-deploy probe: 30/30 boot cleanly. `check-client-subscription` (critical for voice booking) returned to 401 (was 503), so the booking flow is restored. New reusable script `/tmp/deploy_with_shared.mjs` for future bundle deploys; future PRs should prefer this over the simple PATCH template. | `565e775` | `phase-e3-followup-ee3-regression-fix` | 27 fns esm.sh→npm: substitutions (analytics-v2-process, analytics-v2-suggest-widgets [no esm.sh, only _shared loss], analyze-chat-history, analyze-metric, analyze-simulation, bulk-insert-leads, campaign-executor, chat-with-history, check-client-subscription, copy-setter-config, format-metric-chart, generate-ai-prompt, generate-conversation-examples, generate-setter-config, generate-simulation-config, generate-simulation-personas, generate-simulation-report, match-webinar-contacts, modify-prompt-ai, process-lead-file, push-contact-to-external, run-simulation, stripe-checkout, stripe-portal, stripe-webhook, sync-external-contacts, sync-external-credentials, test-external-supabase) + xhr-polyfill removal in generate-ai-prompt + jsr→inline base64 in process-lead-file. | **Code rollback:** `git revert <SHA>` then redeploy via simple PATCH template (which will REINTRODUCE the regression). **Recommended rollback:** none — revert would re-break 27 functions. If individual functions need to be restored to older bundles, use the bundle endpoint with a previous git revision's body. |
| 2026-05-17 | phase-e3-followup-ee4-remove-external-call-history-mirror — Removes the `retell-call-analysis-webhook` Step 4 block that tried to upsert every analyzed call into `call_history` on each tenant's external Supabase project. None of the active external projects (notably bfd-setter-live `qildpilxjodxdifggmto`) have a `call_history` table, so every analyzed call was writing a noisy `'public.call_history' not found in schema cache` row to `error_logs`. Per [[project_session4_state]] the external-Supabase pattern is being retired in favour of bfd-platform consolidation; adding the table later would just delay that. Cleanup also removed the now-unused `externalSupabaseUrl`/`externalServiceKey` declarations, the second-fallback assignment of `supabase_url`/`supabase_service_key` columns, the `EXTERNAL_STRINGIFIABLE_COLUMNS` constant, and the `safeJsonStringify` helper. Primary `call_history` insert on bfd-platform earlier in the same function is unchanged. Deployed v11 → v12 ACTIVE. | `9f8e2f5` | `phase-e3-followup-ee4-remove-external-call-history-mirror` | `frontend/supabase/functions/retell-call-analysis-webhook/index.ts` (removed Step 4 block + 4 unused vars/helpers) | **Code rollback:** `git revert <SHA>` then redeploy. **Behavioural rollback:** without this change, every analyzed call writes a `'public.call_history' not found in schema cache` row to `error_logs` and the external sync logic runs its 12-retry loop before logging a permanent failure. Restoring requires that someone first CREATE TABLE call_history on each tenant's external Supabase. |
| 2026-05-17 | phase-e3-followup-ee1-direction-toggle — Voice AI Setter inbound/outbound direction multi-select (architectural). Brendan's request: each setter can now own any subset of {Inbound, Outbound (initial), Outbound (follow-up)}. Saving one slot's selection no longer overwrites another slot's directions. **Schema:** new column `prompts.directions text[]` (migration `20260517220000_phase_e3_ee1_prompt_directions.sql`). Backfill derives each voice_setter row's directions from the current clients-row agent_id matches against retell_inbound/outbound/outbound_followup_agent_id. For BFD, slot 1 backfilled to ["inbound", "outbound_initial", "outbound_followup"] correctly. **types.ts:** updated `prompts.Row/Insert/Update` with `directions: string[] \| null`. **UI:** new toggle group (ToggleGroup type="multiple") rendered above the AgentConfigBuilder on Voice-Setter-N slots only, with three buttons (Inbound, Outbound (initial), Outbound (follow-up)). Cross-slot conflict warnings render inline ("Outbound (initial) is currently owned by Voice-Setter-2; pushing will move it to this slot"). Empty-selection warning ("not routed to any phone"). **State:** new `voiceSetterDirections` + `otherSlotDirections` state in PromptManagement.tsx, loaded from prompts rows on slot change. Saved alongside prompt content via the existing UPDATE/INSERT paths (only on voice-setter slots). Passed to retell-proxy in `sync-voice-setter` body. **retell-proxy:** new `directions` param (whitelisted to {inbound, outbound_initial, outbound_followup}, deduped). New `fanOutDirections()` helper called after publish: (a) sets each selected direction's `clients.retell_*_agent_id` column to this slot's agent_id, (b) clears any column previously pointing at THIS slot's agent_id but no longer selected (releases the direction for another slot), (c) atomically rewrites sibling voice_setter `prompts.directions` rows to remove any direction we just claimed (cross-slot consistency). Existing SLOT_TO_AGENT_COLUMN "primary anchor" mapping unchanged (still governs where slot N's agent_id lives initially). retell-proxy deployed v12 → v13 ACTIVE. Frontend auto-deploys to Railway on push. | `f1fcd65` | `phase-e3-followup-ee1-direction-toggle` | `frontend/supabase/migrations/20260517220000_phase_e3_ee1_prompt_directions.sql` (new), `frontend/src/integrations/supabase/types.ts` (prompts type), `frontend/src/pages/PromptManagement.tsx` (state + UI + save + push), `frontend/supabase/functions/retell-proxy/index.ts` (DIRECTION_TO_AGENT_COLUMN + fanOutDirections helper + 3 publish-site calls + sync-voice-setter param) | **Code rollback:** `git revert <SHA>` then redeploy retell-proxy. **Schema rollback:** `ALTER TABLE prompts DROP COLUMN IF EXISTS directions;` — destructive, only do if the column is causing issues. The fan-out columns on `clients` remain unchanged. **Behavioural rollback:** if a user pushes from a slot with empty directions, NO fan-out runs (the helper is a noop), so the pre-EE1 state is preserved. |
| 2026-05-17 | phase-e3-followup-ee3-deno-serve-migration — Mechanical sweep migrating all 30 edge functions off the legacy `https://deno.land/std@*/http/server.ts` import (5 were currently throwing `worker boot error: failed to bootstrap runtime` because Supabase's edge runtime now sets `--no-remote`; the other 25 still booted but would have broken on the next runtime bump). Replaced `import { serve } from "deno.land/std..."` + `serve(handler)` with built-in `Deno.serve(handler)` across all 30 files. Bonus: `process-lead-file` also had a legacy `deno.land/std@0.168.0/encoding/base64.ts` import — migrated to `jsr:@std/encoding/base64` (same API, future-proofed). Each function's `verify_jwt` setting was read via GET first and preserved on PATCH so we didn't accidentally weaken or strengthen any auth surface. All 30 deployed via Management API back-to-back; 30/30 ACTIVE. Functions migrated: analytics-v2-process, analytics-v2-suggest-widgets, analyze-chat-history, analyze-metric, analyze-setter-prompt, analyze-simulation, bulk-insert-leads, campaign-executor, chat-with-history, check-client-subscription, copy-setter-config, format-metric-chart, generate-ai-prompt, generate-conversation-examples, generate-setter-config, generate-simulation-config, generate-simulation-personas, generate-simulation-report, match-webinar-contacts, modify-prompt-ai, notify-webhook, process-lead-file, push-contact-to-external, run-simulation, stripe-checkout, stripe-portal, stripe-webhook, sync-external-contacts, sync-external-credentials, test-external-supabase. | `ff08a8f` | `phase-e3-followup-ee3-deno-serve-migration` | 30 files in `frontend/supabase/functions/*/index.ts` (see list above) | **Code rollback:** `git revert <SHA>` then re-deploy via Management API. **Behavioural rollback:** none needed — the legacy `serve(handler)` and `Deno.serve(handler)` are functionally equivalent. The 5 previously-broken functions (run-simulation, analyze-metric, analyze-setter-prompt, sync-external-credentials, stripe-portal) now boot. |
| 2026-05-17 | phase-e3-followup-ee5-voice-paste-in — Fixes silent voice paste-in loss in the Voice AI Setter editor. Last night Brendan pasted `custom_voice_fdd472681ecd08a12f2b616bd2` into the RetellVoiceSelector but the agent ended up with a different `custom_voice_xxx` after push. Diagnostic logs from the retell-proxy v9 window aged out before they could be inspected, so triaged from code analysis. **Root cause** in `frontend/src/components/RetellVoiceSelector.tsx`: the search input inside the popover only fires the parent `onChange` on three explicit user actions — Enter key, voice button click, or "Use as custom voice ID" button click. The popover's open→close `useEffect` at line 56-59 unconditionally cleared `search` WITHOUT firing `onChange`, so any paste that wasn't followed by Enter or button click was silently dropped on dismiss. **Fix:** when the popover closes (open=false transition), commit the pending search value via `onChange` if it's non-empty, differs from current `value`, and doesn't match a hardcoded preset name. Belt-and-suspenders: also added `onBlur` to the input that runs the same commit logic synchronously on focus loss, in case the user clicks straight to "Push to Retell" without an explicit popover dismiss. Frontend auto-deploys to Railway on push. **Diagnostic kept:** the `console.log` at `retell-proxy/index.ts:177` stays in place to verify the fix on the next push — clean up will land in a follow-up. | `b381d78` | `phase-e3-followup-ee5-voice-paste-in` | `frontend/src/components/RetellVoiceSelector.tsx` (popover close-effect + onBlur both commit pending paste) | **Code rollback:** `git revert <SHA>`. Railway will redeploy automatically. **Behavioural rollback:** none needed — pre-fix users could lose pastes silently; post-fix they don't. |
| 2026-05-17 | phase-e3-followup-ee2-phone-version-repoint — Fixes the silent multi-tenant gap discovered last night: after the retell-proxy `sync-voice-setter` flow PATCHes the LLM + publishes the agent (creating v34, v35…), Retell phone-number version pins (`inbound_agent_version` / `outbound_agent_version`) do NOT auto-update. Every "Push to Retell" silently failed to make tool/prompt changes live for actual phone calls — for every tenant — because the phone kept routing to the previously-pinned (stale) agent version with stale tool URLs. Last night this required a manual PATCH from outside the codebase to make the booking flow live. Adds `repointPhoneVersionsAfterPublish()` helper in `retell-proxy/index.ts` that runs after each of the 3 `POST publish-agent` call sites (existing-agent + existing-LLM, existing-agent + new-LLM, brand-new-agent). Helper captures the published version (with `GET get-agent` fallback if the publish response is missing `version`), reads `clients.retell_phone_1/2/3`, and PATCHes each non-null phone with the slot-appropriate field: slot 1 → `inbound_agent_version`, slots 2 + 3 → `outbound_agent_version`, slots 4-10 → skip with a log (no canonical phone routing). All errors are non-blocking (try/catch, console.warn) so a bad phone doesn't fail the whole sync. **Live-state:** retell-proxy v11 → v12 ACTIVE (via Management API) at `bjgrgbgykvjrsuwwruoh`. | `3173670` | `phase-e3-followup-ee2-phone-version-repoint` | `frontend/supabase/functions/retell-proxy/index.ts` (added helper + updated 3 publish call sites) | **Code rollback:** `git revert <SHA>` then redeploy retell-proxy via Management API with the pre-revert body. **Behavioural rollback:** none needed — without the helper, phone numbers keep their version pin until manually PATCHed (the pre-EE2 status quo, unchanged in BFD's case because last night's manual PATCH already set v33). |
| 2026-05-17 | phase-e3-night-end-to-end-verified — Full end-to-end voice-booking flow PROVEN working. Call `call_c682ce5135e4d4b82af20af49ed` (gpt-5.4 / gemini-3.0-flash, agent_version 33) fired `get-available-slots` (1679ms) + `book-appointments` (3542ms) against `voice-booking-tools` → real GHL appointment `OfwxxJT9L5jO3IMGUTnn` created → GHL "BFD bookings → 1prompt (BOOKED)" workflow fired → `bookings-webhook` wrote row `59780225-dfa5-49f7-95ca-c9f1ac486a1c` (`source=ghl_calendar`, `appointment_time=2026-05-21 04:00 UTC`, `status=confirmed`). Zero `error_logs`. Tonight's session shipped 4 supporting commits to get here, plus identified 4 deferred punch-list items + 1 architectural rebuild proposal for the next session. **Tonight's commits:** (1) `8ba393e` multi-tenant booking-tool URL substitution + en-AU language + Onboarding `intake_lead_secret` auto-mint. Aria diagnostic log added to retell-proxy. (2) `012e5c9` SHA backfill. (3) `1e9b808` check-client-subscription Stripe-optional path — was throwing 500 because `STRIPE_SECRET_KEY` not configured, blocking the entire save → push flow before retell-proxy could fire. (4) `7a177bf` retell-proxy SyntaxError fix — my (1) shipped a duplicate `clientErr` declaration that crashed the function at boot with `Identifier 'clientErr' has already been declared`. Worker boot errors silently 503'd every Push to Retell attempt for ~10 minutes until I fixed it. **Live-state fixes (out of repo, executed in-session):** retell-proxy v8→v9→v10→v11 ACTIVE (via Management API). check-client-subscription v7→v8 ACTIVE. Manually published agent `agent_5ec5eb…` from v30→v33. Manually repointed phone number `+61481614530` from `inbound_agent_version: 30` → `33` and `outbound_agent_version: 30` → `33` via PATCH `/update-phone-number/%2B61481614530` — THIS is the bug that hid for hours: Retell stores phone-number-level agent_version pins separately from the agent's current version, and every retell-proxy PATCH+publish updates the agent but NOT the phone-number routing pin, so every call still hit the original v30 with legacy n8n URLs. | `8ba393e` + `012e5c9` + `1e9b808` + `7a177bf` | `phase-e3-followup-multi-tenant-tools` (at `8ba393e`) | **Files in commits this session:** `frontend/supabase/functions/retell-proxy/index.ts` (v9→v11), `frontend/src/lib/retellVoiceAgentDefaults.ts` (sentinel placeholder), `frontend/src/components/VoiceRetellSettings.tsx` (en-AU), `frontend/src/pages/Onboarding.tsx` (intake_lead_secret auto-mint), `frontend/supabase/functions/check-client-subscription/index.ts` (Stripe-optional). **Data ops (out of repo, executed in-session):** PATCH `/update-phone-number/%2B61481614530` to set inbound + outbound `agent_version: 33`. POST `/publish-agent/agent_5ec5eb…` (manual publish that bumped to v33). The earlier SQL scrub on `voice_prompts.Setter-1.system_prompt` (from row 2 below) was on the WRONG table — the source-of-truth for the Aria stale content is `prompts.content` on bfd-platform (`bjgrgbgykvjrsuwwruoh`), per-client per-slot row. See "Aria builder investigation" in `Operations/handoffs/2026-05-17-phase-e3-end-to-end-verified-handoff.md` for the full architectural story. | **Code rollback:** `git revert 7a177bf 1e9b808 012e5c9 8ba393e` then redeploy edge fns via Management API. **Phone number revert:** `curl -X PATCH ".../update-phone-number/%2B61481614530" -d '{"inbound_agent_version":30,"outbound_agent_version":30}'` (don't do this — v30 had legacy n8n URLs). **Edge fn revert:** retell-proxy v11 → previous body from git history at `8ba393e`'s parent. **Note:** none of these are likely needed — end-to-end was proven working tonight at `call_c682ce5135e4d4b82af20af49ed` with a real GHL booking landing. |
| 2026-05-16 | phase-e3-followup-2-multi-tenant-tools — Closes the class of bug where the upstream 1prompt-os fork's hardcoded URLs in frontend defaults silently overwrite per-tenant Retell config on every UI "Push to Retell". Tonight's test call `call_dee1a3b7db2eb1c38394df56c82` (gpt-5.4 model swap) confirmed tools FIRED successfully on the Retell side (get-available-slots + book-appointments + end_call) but went to `https://n8n-1prompt.99players.com/webhook/e4cffeea-…` (the legacy upstream n8n endpoint), so bookings never landed in BFD's `bookings` table. The Phase A3 fix (`1f1ca45` 2026-05-04) had repointed the Retell-dashboard side but never touched the frontend defaults, so every subsequent UI push reverted the URLs. This commit makes the substitution happen server-side at push time, with per-tenant `clientId` + `intake_lead_secret` auto-injected — no manual credential pasting required for any new tenant onboarded going forward. Also folds in two follow-up asks: (1) the voice-paste-in diagnostic logging Brendan asked for (still hadn't been fixed; the prior session addressed only the latent path for raw ElevenLabs IDs, not the actual `custom_voice_xxx`-paste-defaults-to-different-custom-voice symptom — needs Brendan's next UI push to log + diagnose; pinpointed fix in follow-up commit), and (2) `en-AU` added to the language dropdown (was missing despite Retell supporting it). | `8ba393e` | `phase-e3-followup-multi-tenant-tools` | **Repo code changes:** `frontend/supabase/functions/retell-proxy/index.ts` lines 145-180 — added client-row fetch via Supabase admin client (reads `intake_lead_secret`), auto-mint logic if the secret is null (24-byte base64 via `crypto.getRandomValues` + persisted to `clients` table; one-time per tenant), diagnostic `console.log` of received `voiceSettings.voice_id` for the still-unsolved paste-in bug; lines 198-220 — booking-tool URL substitution loop: any tool whose URL matches `__BFD_VOICE_BOOKING_TOOLS__` placeholder OR contains `n8n-1prompt.99players.com` gets rewritten to `${SUPABASE_URL}/functions/v1/voice-booking-tools` with `query_params: {tool: <name>, clientId: <clientId>}` + `headers: {Authorization: 'Bearer <intake_lead_secret>'}`; `end_call` and `transfer_call` pass through unchanged. `frontend/src/lib/retellVoiceAgentDefaults.ts` — added exported `BFD_VOICE_BOOKING_TOOLS_PLACEHOLDER` constant + header comment; replaced 5 hardcoded `https://n8n-1prompt.99players.com/webhook/e4cffeea-…` URLs (lines 55, 97, 141, 180, 214) with `"__BFD_VOICE_BOOKING_TOOLS__"`. `frontend/src/components/VoiceRetellSettings.tsx` line 126 — added `{ value: 'en-AU', label: 'English (Australia)' }` to LANGUAGES array. `frontend/src/pages/Onboarding.tsx` lines 88-93 — generates `intake_lead_secret` client-side via `crypto.getRandomValues(new Uint8Array(24))` + `btoa()` for any new tenant created via the UI wizard, included in the initial INSERT payload (mirrors `scripts/onboard-client.mjs:169` pattern). **Edge function deploy:** retell-proxy v9 → v10 ACTIVE on `bjgrgbgykvjrsuwwruoh` via Management API PATCH. **Multi-tenant outcome:** every new tenant onboarded via the UI wizard now gets `intake_lead_secret` auto-minted at row creation, and every "Push to Retell" auto-rewrites booking-tool URLs to BFD's `voice-booking-tools` edge fn with per-tenant routing. Existing tenants (just BFD today) get backfilled with a fresh secret on the next push. **Out of scope (deferred — `phase-e3-followup-3-secondary-url-cleanup`):** ~15 other hardcoded upstream URLs across the codebase (analytics chat webhook, prompt webhook, webinar webhook, 11labs booking caller webhook, us.1prompt.com widget URLs in copy templates, lovable.app origin headers in n8n workflow JSON templates, `1prompt-os` literal as GHL customField tag value). Also deferred: builder re-introducing Aria/upstream scaffolding phrases on UI save (separate bug; different defaults file). | **Code revert:** `git revert <COMMIT_SHA>` then redeploy retell-proxy via Management API PATCH (CLI hangs in docker env, same as Phase A Bug 2 closeout). **Live retell-proxy revert:** PATCH back to v9 body — saved to git history at commit `4d8c008`. **Data revert (intake_lead_secret mints):** If retell-proxy auto-minted secrets for any tenant during this session, those values are persisted in `clients.intake_lead_secret`. Revert leaves them in place (harmless — voice-booking-tools accepts the Bearer auth; just stops getting called once retell-proxy is reverted). To explicitly clear: `UPDATE clients SET intake_lead_secret = NULL WHERE intake_lead_secret IS NOT NULL AND id = '<tenant>';` — but this would invalidate any embed snippets / form integrations that reference the secret. Don't do this unless you know what you're doing. |
| 2026-05-15 | phase-e3-followup-platform-fixes — Test-1 of the earlier Phase E3 closeout surfaced two underlying platform-side bugs that the original E3 plan didn't address: (1) `voice_prompts.Setter-1.system_prompt` on bfd-setter-live external supabase (`qildpilxjodxdifggmto`) contained 8 verbatim `'Aria'` mentions plus upstream scaffolding phrases (`drowning in DMs`, `best damn gatekeeper`, `coaches and consultants who are swamped`, `AI setter service`) — stale data the platform builder didn't clean when the persona name was set to "Gary" in the UI. Source-code-side was already clean (rebrand R3 swept it in `95313cb`); the leak lived in the row. (2) The Retell voice-selector paste-in (`RetellVoiceSelector.tsx`) accepted a `custom_voice_xxx` ID and saved it to state, but `retell-proxy/index.ts` line 226 had a gating bug: it always forwarded `voice_model` from `PromptManagement.tsx:6018` (hardcoded `'eleven_turbo_v2_5'`) only when `voice_id` started with `11labs-`, which was correct for that case but not defensive for raw ElevenLabs IDs (where voice_model is required to disambiguate the provider). For Brendan's specific `custom_voice_xxx` paste, the bug class is state-staleness in the UI's voice-settings load/save path (separately observed but not pinned to a single line; defensive code fix layered on top covers most future failure modes). | `4d8c008` | `phase-e3-followup-platform-fixes` | **Repo code changes:** `frontend/supabase/functions/retell-proxy/index.ts` lines 220-237 — voice_id forwarding tightened (rejects empty strings, trims whitespace); `voice_model` gating broadened from "11labs- only" to "anything NOT a Retell-managed prefix (custom_voice_/openai-/cartesia-/play-)", so raw ElevenLabs IDs will now ship with `voice_model: 'eleven_turbo_v2_5'` and resolve correctly. `frontend/src/components/RetellVoiceSelector.tsx` lines 72-85 + 110 + 187-198 — input placeholder changed from `"Search voice or paste custom ID..."` to `"Search or paste custom_voice_xxx / 11labs-Name..."`; renders the committed custom ID below the selector so the user can verify the paste landed; renders an amber warning when a pasted ID has no recognised Retell prefix (raw ID heuristic). **Data change (out of repo, executed in-session, backup at `/srv/bfd/Operations/handoffs/2026-05-15-e3-voice-prompts-bfd-pre-scrub-backup.json`):** `UPDATE voice_prompts SET system_prompt = regexp_replace(... 5 chained replacements ...) WHERE card_name = 'Setter-1'` on bfd-setter-live (`qildpilxjodxdifggmto`). Replacements: `'Aria'` → `'Gary'` (8 hits), `'drowning in DMs'` → `'with slow follow-up'` (4 hits), `'the best damn gatekeeper'` → `'a great first point of contact'` (1 hit), `'coaches and consultants who are swamped'` → `'property advisors and finance coaches drowning in inbound enquiries'` (1 hit), `'AI setter service'` → `'AI voice and text setter service'` (5 hits). Post-scrub verification: `regexp_count(system_prompt, 'Aria', 1, 'c') = 0`, `regexp_count(system_prompt, 'Gary', 1, 'c') = 10`, system_prompt length 46,086 → 46,211 chars. `booking_prompt` left untouched (0 case-sensitive Aria mentions). **Edge function deploy:** `retell-proxy` v?? deployed to bjgrgbgykvjrsuwwruoh via `npx supabase functions deploy retell-proxy --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. **What this commit explicitly does NOT touch:** the live Retell LLM `llm_22e795de…` (still serving the prior session's 11k handwritten prompt from commit `f7b6c2f`). Brendan must trigger a UI re-save of the Voice AI Setter for Voice-Setter-1 to fire the now-clean `voice_prompts.system_prompt` through `retell-proxy/sync-voice-setter` to overwrite Retell with the proper builder output. After that, the live LLM should be ~52k chars (system_prompt 46,211 + booking_prompt 6,421 + framing) with zero Aria. **Verification flow (Brendan-side):** (a) UI re-save in Voice AI Setter for Voice-Setter-1 (touch any field, hit save). (b) Push to Retell button. (c) `curl /get-retell-llm/llm_22e795de…` confirms zero Aria + length ~52k. (d) Re-dial `+61 481 614 530` for Test 1 of the runbook; opener should now use Gary identity from the builder, no Aria leak. (e) Optional: paste a different `custom_voice_xxx` in the UI to confirm the paste-in path is no longer silently dropping the voice. | **Code revert:** `git revert 4d8c008` then redeploy retell-proxy via the same supabase deploy command. **Data revert (voice_prompts row):** restore from backup JSON: `UPDATE voice_prompts SET system_prompt = (SELECT (data->>'system_prompt')::text FROM (SELECT '<paste-row-from-backup-file>'::jsonb AS data) sub), booking_prompt = ..., model = ..., updated_at = now() WHERE card_name='Setter-1';` (or use the Management API with the file contents). **Live Retell LLM revert (if Brendan's re-save makes things worse):** PATCH `/update-retell-llm/llm_22e795de…` with general_prompt from `/srv/bfd/Operations/handoffs/2026-05-15-e3-pre-rewrite-backup-llm.json` + `POST /publish-agent/agent_5ec5eb…`. |
| 2026-05-15 | phase-e3-voice-agent-gary-rewrite — Closed Phase E3 of the rebrand: BFD's live Retell voice agent now introduces itself as Gary the AI in the first sentence, follows the canonical Gary tone spec from `wiki/concepts/gary-persona-tone-rules.md`, and the deploy script works cross-platform. Scope locked to option α (rewrite only the one LLM that's actually wired into BFD's `clients` row — `llm_22e795de19b4d25cb579013586be` via agent `agent_5ec5eb129f3165cfa07b581a1a`). The other 2 LLMs (`llm_692b220d…`, `llm_1807516860…`) exist in Retell but are orphaned (not referenced on the `clients` row); deferred to a future split-personas phase. **Why two compliance gaps had to close on this prompt:** (1) AU compliance gap — the prior live prompt's opener said `"Hey there, I'm Gary from Building Flow, Brendan asked me to reach out"` with NO proactive AI disclosure. The wiki source-of-truth at `/srv/bfd/wiki/concepts/au-compliance-non-negotiables.md` §3 mandates `"I'm an AI assistant"` (or equivalent) in the first sentence of every AI voice call (ASIC misleading-conduct rule). The repo template even explicitly told Gary `"You are NOT a robot and do NOT identify as an AI"` — directly contradicting the spec. (2) Missing canonical tone elements: no filler-word guidance, no verbal-nod guidance, no interruption rule, no text-slang ban, no deflection rule, no founder backstory. All present now. **Live-state observation captured pre-rewrite:** the deployed prompt was 53,154 chars and was an upstream Anne/Eugene fork with `Gary` identity-anchored on top + the 2-string Sydney patch from Bug 2 closeout. Example transcripts inside it still said `"My name's Aria, calling from Building Flow Digital"` (line 684 of the backup). After this commit: live prompt is 11,386 chars, no `Aria` or `Eugene` leak (`contains_aria_leak=false`, `contains_eugene_leak=false` on jq read-back), agent version bumped 24 → 25, model preserved as `gemini-3.0-flash`. | `f7b6c2f` | `phase-e3-voice-agent-gary-rewrite` | **Repo files changed:** `frontend/src/data/bfdVoiceSetterPrompt.ts` (full rewrite: v3 2026-05-15; 11,386 chars; AI + recording disclosure in sentence 1; AI reveal pattern; voice-call rules section with fillers + nods + interruption rule + text-slang ban; persona rules section with AU spelling + first-name-only + max 1 exclamation + deflection rule + em-dash strip; founder backstory paragraph; ICP context; preserved 3-question qualification flow, 7-step booking flow, 6 tool descriptions verbatim including `startDateTime`/`endDateTime` ISO format + `Australia/Sydney` IANA default; preserved + AU-tone-passed objection responses table; preserved wrap-up). `scripts/deploy_voice_prompt.mjs` (cross-platform path via `new URL('../...', import.meta.url)` replacing the Windows `c:/Projects/...` hardcode that blocked greenserver runs; consolidated to single `retellRequest()` helper; added `[3/3] POST /publish-agent/${AGENT_ID}` step — without this a PATCH on the LLM does not propagate to the running agent; reads `BFD_RETELL_AGENT_ID` from env with `agent_5ec5eb129f3165cfa07b581a1a` literal default). `scripts/update_voice_prompts_setter1.mjs` (same cross-platform path fix; comment `BFD Aria v2` → `BFD Gary`). `.env.example` already had `BFD_RETELL_LLM_ID=llm_22e795de19b4d25cb579013586be` + `BFD_RETELL_AGENT_ID=agent_5ec5eb129f3165cfa07b581a1a` from a prior session; no edit needed. **Live-state changes (out of repo):** PATCH `/update-retell-llm/llm_22e795de19b4d25cb579013586be` with new `general_prompt` (HTTP 200, length 11,386); POST `/publish-agent/agent_5ec5eb129f3165cfa07b581a1a` (HTTP 200, version 24 → 25). **Pre-rewrite backup (out of repo, for revert):** `/srv/bfd/Operations/handoffs/2026-05-15-e3-pre-rewrite-backup-llm.json` (full `get-retell-llm` response, 53,154-char general_prompt) + `...backup-agent.json` (agent state at version 24). **Verification:** opener jq read-back contains `"I'm Brendan's AI assistant at Building Flow Digital"` + `"call is being recorded"` (passes ASIC disclosure rule); zero `Aria`/`Eugene` leaks remain in the live LLM; agent version bumped; model `gemini-3.0-flash` preserved. **Live test calls (Brendan-side, deferred to next session):** real inbound from Brendan's mobile to confirm opener is heard with AI disclosure + AU tone + 1-2 sentence responses + booking flow end-to-end (then test-data cleanup per Phase A pattern); outbound cadence enrollment with `bookings.cadence_execution_id` populated. **Hard constraints respected:** other 2 LLMs (`llm_692b220d…`, `llm_1807516860…`) untouched; downloadable Retell agent JSON templates in `frontend/public/retell-agents/*.json` untouched (those were already rebranded in `95313cb`); Sydney IANA reference and tool-payload shapes untouched; agent + LLM IDs untouched. **Out of scope (deferred):** option β (rewrite all 3 LLMs as distinct personas) once agents are split; option γ (delete/collapse the orphaned LLMs); E4 setup-guide screenshots; E5 pun-quiz rewrite. | **Code rollback:** `git revert <this-commit>` then redeploy. **Live-state rollback:** PATCH the live LLM back to its pre-rewrite state and republish: `curl -X PATCH https://api.retellai.com/update-retell-llm/llm_22e795de19b4d25cb579013586be -H "Authorization: Bearer $BFD_RETELL_API_KEY" -H "Content-Type: application/json" -d @<(jq '{general_prompt: .general_prompt, model: .model}' /srv/bfd/Operations/handoffs/2026-05-15-e3-pre-rewrite-backup-llm.json)` then `curl -X POST https://api.retellai.com/publish-agent/agent_5ec5eb129f3165cfa07b581a1a -H "Authorization: Bearer $BFD_RETELL_API_KEY"`. Restores the 53,154-char Gary-renamed-but-upstream-scaffolded prompt plus the 2-string Sydney patch as it stood at the start of this session. |
| 2026-05-14 | phase-e-rebrand-complete — Closed Phase E1+E2 of the rebrand: codebase, docs, public assets, and brand-bearing strings all swapped from upstream-1prompt branding to BFD/Building Flow/Gary. Six commits cover the full pass. (1) `95313cb` rebrand R1+R2+R3: persona Aria→Gary (he/him) in BFD's production prompts; `1prompt` brand string→`Building Flow` (customer-facing) or `BFD-setter` (internal-docs) across ~30 docs + frontend UI + 3 n8n workflow JSONs; pure name swap (Geno/Katherine→Gary, Eugene Kadzin→Brendan Green, Quimple→Building Flow Digital, 1Prompt→Building Flow) inside upstream demo default-prompt templates exposed in admin UI; Retell agent JSON templates in `frontend/public/retell-agents/` (5 files) had their personas replaced with Gary; 4 upstream Meta Pixel scripts scrubbed from `frontend/index.html` (eliminated data leak to upstream creators' Facebook accounts); 50 files changed, +699/-529 lines. (2) `25ce9d2` Phase E2: Lovable cleanup — removed orphan `frontend/.lovable/plan.md`, fixed `twitter:site` falsely crediting `@lovable_dev`, fixed `User-Agent: lovable-source-files-page` in `github-proxy/index.ts`, added Deployment topology section to `Docs/RUNBOOK.md`, added topology block to `README.md`, rewrote `frontend/README.md` from Lovable boilerplate to BFD pointer, corrected actively-wrong "Edge functions deployed via Lovable" claims in `SETUP_OVERVIEW.md`. (3) `b63826b` root `package.json` `"name": "1prompt-os"` → `"bfd-setter"` + regenerated `package-lock.json`. (4) `01da5ca` project folder rename `/srv/bfd/Projects/1prompt-os/` → `bfd-setter/` with back-compat symlink (see following row for OS-mechanics detail). (5) `eb0c158` trailing `frontend/README.md` path-ref fix. (6) `86d6eac` BFD logo + production URL: dropped real BFD logo into `frontend/src/assets/bfd-logo.png`, `frontend/public/bfd-logo.png`, `frontend/public/bfd-favicon.png` (source-of-truth at `/srv/bfd/Company/knowledge/brand/bfd-logo.png`); updated 11 reference points (7 auth pages + AppHeader + Settings + 2 favicon links); deleted orphan `frontend/src/assets/1prompt-logo.png`; corrected production URL refs to `app.buildingflowdigital.com` in 3 doc files. **Brand hierarchy now locked everywhere customer-facing or admin-visible:** Agency = Building Flow Digital (BFD); Product = Building Flow (customer-facing); Persona = Gary (he/him) — replaces Aria; Codebase (internal) = BFD-setter; Upstream OSS = `genokadzin/1prompt-os` (attribution only). | `95313cb`, `25ce9d2`, `b63826b`, `01da5ca`, `eb0c158`, `86d6eac` | (no tag; rebrand has many commits — refer to date) | **R1 files (persona):** `frontend/src/data/bfdTextSetterPrompt.ts`, `frontend/src/data/bfdVoiceSetterPrompt.ts`. **R2 files (brand strings) — top-level docs:** `README.md`, `SETUP_OVERVIEW.md`, `User Todos.md`, `.env.example`, `Docs/MASTER_PLAN.md`, `Docs/ARCHITECTURE.md`, `Docs/CLIENT_ONBOARDING_SOP.md`, `Docs/WEBHOOKS.md`, `Docs/RUNBOOK.md`, `Docs/GHL_PUSH_AUDIT.md`, `Docs/FUTURE.md`, `Docs/RAILWAY_ENV.md`, `Docs/CAMPAIGN_PLAYBOOK.md`, `Docs/NEXT_SESSION_PROMPT.md`. **R2 files (frontend UI):** `frontend/index.html`, `frontend/src/pages/{Auth,ForgotPassword,Register,ResetPassword,VerifyEmail,Onboarding,Home,Workflows,VoiceAISetter,AccountSettings,ClientSettings,ManageClients,ApiManagement,WebinarSetup,WhatToDo,Templates,SyncGHLBookings,SyncGHLContacts}.tsx`, `frontend/src/components/{SetupGuideDialog,WebinarSetupGuideDialog}.tsx`, `frontend/src/components/editor/sections/TextAISection.tsx`. **R2 files (n8n templates):** `frontend/public/workflows/voice-sales-rep/Appointment_Booking_Functions.json`, `Appointment_Booking_Functions_v2.json`, `frontend/public/workflows/text-engine/Text_Engine.json` — booking title `"AI Strategy with Eugene x 1Prompt"` → `"Strategy Call with Brendan"`. **R3 files (upstream demo defaults):** `frontend/src/data/defaultPromptTemplates.ts`, `frontend/src/pages/PromptManagement.tsx`, `trigger/runAiJob.ts`, `frontend/src/pages/ApiCredentials.tsx`. **R3 files (Retell agent JSON templates):** `frontend/public/retell-agents/{Inbound_Agent,Inbound_Voice_Agent,Outbound_Agent,Outbound_Voice_Agent,Voice-Setter-1}.json` — agent_name strings retitled `"Gary - … (BFD template)"` + Voice-Setter-1's 70k-char Anne/Eugene persona replaced with BFD's Gary voice setter content. **Logo files:** added `frontend/src/assets/bfd-logo.png`, `frontend/public/bfd-logo.png`, `frontend/public/bfd-favicon.png`; deleted `frontend/src/assets/1prompt-logo.png`. **Lovable cleanup:** deleted `frontend/.lovable/plan.md`; rewrote `frontend/README.md` from Lovable boilerplate; updated `frontend/supabase/functions/github-proxy/index.ts` User-Agent (still needs `supabase functions deploy github-proxy --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt` — cosmetic; deferred). **package.json rename:** `package.json:2` `name: bfd-setter`; `package-lock.json` regenerated. **Verification:** `git grep '\bAria\b'` against frontend/trigger/Docs → zero hits; `git grep -iE '\b(geno\|katherine\|quimple)\b'` against frontend/trigger (excluding `.lovable/`, public assets, migrations, image assets) → zero hits; `git grep '\bEugene\b'` same scope (excluding `speedToLeadData.ts` generic test data) → zero hits; `cd frontend && npx tsc --noEmit` → exit 0. Pushed to BOTH origin (Forgejo `http://100.112.250.77:3080/bfd/1prompt-os.git`) AND github (`https://github.com/TheBrendonly/1prompt-os.git`). Railway auto-deployed from main; live at `https://app.buildingflowdigital.com/`. Tab title confirmed `"BFD-setter"` post-deploy; favicon + dashboard logo render BFD branding (may need hard-refresh for favicon cache bust). **Hard constraints respected (NOT touched):** GHL pipeline literal `"1Prompt"` + id `7LAuP7pYshc0c5HGYI0D`; Retell agent id `agent_5ec5eb129f3165cfa07b581a1a`; `https://us.1prompt.com/widget/bookings/...` widget URLs inside upstream default-template copy; `https://n8n-1prompt.99players.com/...` live n8n URLs; `last_synced_from = "1prompt-os"` GHL custom-field value; GHL tag literals `1prompt - new lead` / `1prompt - stop setter`; Railway service name `1prompt-os-production`; Forgejo repo URL `bfd/1prompt-os.git`; GitHub repo `TheBrendonly/1prompt-os`; all shipped Supabase migrations; Trigger.dev task names; upstream MIT LICENSE / attribution comments. **Server-side ops (not in repo):** `cd /srv/bfd/Projects/ && mv 1prompt-os bfd-setter && ln -s bfd-setter 1prompt-os` (back-compat symlink); `mv ~/.claude/projects/-srv-bfd-Projects-1prompt-os ~/.claude/projects/-srv-bfd-Projects-bfd-setter`; `mv /srv/storage/syncthing/claude-memory/-srv-bfd-Projects-1prompt-os /srv/storage/syncthing/claude-memory/-srv-bfd-Projects-bfd-setter` + repointed `~/.claude/projects/-srv-bfd-Projects-bfd-setter/memory` symlink. Source-of-truth BFD logo at `/srv/bfd/Company/knowledge/brand/bfd-logo.png` (filename `bfd-logo.png`; original delivered as `79681_Building Flow Digital_flat_NP_01.png`). **Laptop-side ops (Brendan ran):** `git pull origin main`; `mv ~/bfd-os/Projects/1prompt-os ~/bfd-os/Projects/bfd-setter && ln -s bfd-setter 1prompt-os`; `mv ~/.claude/projects/-mnt-bfd-Projects-1prompt-os ~/.claude/projects/-mnt-bfd-Projects-bfd-setter`; repointed `memory` symlink to `~/.claude/memory-bfd/-srv-bfd-Projects-bfd-setter` (Syncthing-shared). **Open follow-ups (deferred):** E3 live Retell agents still run upstream Anne/Eugene/1Prompt persona — needs ground-up rewrite via Retell dashboard (Brendan); E4 setup-guide screenshots paired with `lovable-uploads/ghl-*.png` need re-shoot before next client onboarding; E5 upstream pun-quiz lessons in `MultiAgentLogicStep.tsx`/`VoiceInboundLogicStep.tsx` need rewrite; Forgejo + GitHub repo rename optional (separate UI action + `git remote set-url`); forward migration for shipped seed-data defaults containing `1Prompt` strings (defer until Client #2 surfaces in a real tenant); `github-proxy` edge function User-Agent change needs deploy (cosmetic). | **Code rollback:** `git revert 86d6eac..95313cb` (range covers all 6 rebrand commits) then redeploy. Be aware historical handoff files at `/srv/bfd/Operations/handoffs/` (35 of them) reference the old folder path which only resolves via the back-compat symlink — revert leaves the symlink intact so they still work. **Folder rollback (server):** `cd /srv/bfd/Projects/ && rm 1prompt-os && mv bfd-setter 1prompt-os && ln -s 1prompt-os bfd-setter`. **Folder rollback (laptop):** equivalent under `~/bfd-os/Projects/`. **Claude memory rollback:** `mv ~/.claude/projects/-srv-bfd-Projects-bfd-setter ~/.claude/projects/-srv-bfd-Projects-1prompt-os` AND `mv /srv/storage/syncthing/claude-memory/-srv-bfd-Projects-bfd-setter /srv/storage/syncthing/claude-memory/-srv-bfd-Projects-1prompt-os` AND repoint the memory symlink back. **Logo asset rollback:** restore `frontend/src/assets/1prompt-logo.png` from git history; revert the 11 reference-point edits via `git revert 86d6eac`. **Note:** none of these are likely needed — rebrand is purely surface-level (no schema, no live infra). |
| 2026-05-14 | phase-e-folder-rename — Renamed project directory `/srv/bfd/Projects/1prompt-os/` → `/srv/bfd/Projects/bfd-setter/` to complete Phase E rebrand. Back-compat symlink created (`/srv/bfd/Projects/1prompt-os` → `bfd-setter`) so all 35 historical handoff files at `/srv/bfd/Operations/handoffs/` continue to resolve. Same inode (2891028) reachable via either name. Moved Claude per-project memory dir in lockstep: `~/.claude/projects/-srv-bfd-Projects-1prompt-os/` → `-srv-bfd-Projects-bfd-setter/`. Forgejo repo URL still `bfd/1prompt-os.git` (separate decision deferred). Restic INCLUDES is glob-based at `/srv/bfd` so backup config unchanged. Laptop mirror (`~/bfd-os/Projects/1prompt-os/`) requires matching rename on next laptop session — see handoff. | _path-only + 1 doc edit_ | (no tag; rename has no commit-able state beyond the doc edit) | **OS-level operations (server):** `cd /srv/bfd/Projects/ && mv 1prompt-os bfd-setter && ln -s bfd-setter 1prompt-os`. `mv /home/brendan/.claude/projects/-srv-bfd-Projects-1prompt-os /home/brendan/.claude/projects/-srv-bfd-Projects-bfd-setter`. **Repo-internal edit:** `Docs/NEXT_SESSION_PROMPT.md` — 5 path refs updated to new path (the only in-repo file containing a literal `/srv/bfd/Projects/1prompt-os/...` path). All other 35 historical handoff files at `/srv/bfd/Operations/handoffs/` left unchanged; they resolve via the back-compat symlink. **Verification:** `pwd` from inside repo returns `/srv/bfd/Projects/bfd-setter`; `ls /srv/bfd/Projects/1prompt-os/README.md` resolves (via symlink); `ls -id` shows both paths point to inode 2891028; `npx tsc --noEmit` from `frontend/` exits 0; `git log -1` returns `b63826b` (rename commit from this session). | **Revert (server):** `cd /srv/bfd/Projects/ && rm 1prompt-os && mv bfd-setter 1prompt-os && ln -s 1prompt-os bfd-setter`. `mv /home/brendan/.claude/projects/-srv-bfd-Projects-bfd-setter /home/brendan/.claude/projects/-srv-bfd-Projects-1prompt-os`. Then `git revert <this-commit>..HEAD` to roll back the NEXT_SESSION_PROMPT.md edit. |
| 2026-05-14 | night-bug2-bug3-closeout — Closed the two remaining Phase A punch-list bugs (item (a) voice-agent ET timezone, item (e) Twilio error extraction). Both small + independent. Bug 2 required code + DB + Retell-side patches; Bug 3 was a 2-file rename. With these closed, BFD is on Australia/Sydney across the entire voice booking stack and Twilio failures surface real carrier codes in error_logs. Cadence v2 activation is now only gated on Brendan's Bug 1 manual verification. | `756c7bd` (Bug 3), `05106aa` (Bug 2) | (no tag yet) | **Bug 3 — `trigger/runEngagement.ts:201-208` + `trigger/processMessages.ts:413-417`:** Twilio's REST API returns failed-send fields as `code` + `message`, not `error_code` / `error_message`. Both helpers were reading the wrong keys; failures silently surfaced as `"Twilio SMS failed: ? unknown"` (cost 10 min guessing on the 2026-05-13 21610 carrier opt-out). Both files now read `code`/`message`. Helper external return shape (`errorCode`/`errorMessage`) preserved so call sites unchanged. Deployed Trigger.dev v20260514.1 (10 tasks). **Bug 2 — `frontend/supabase/functions/make-retell-outbound-call/index.ts` (edge fn v8 → v9 via Management API PATCH):** clients SELECT extended to include `timezone` column (added 2026-05-05 in phase-night-a4-clients-timezone). New `clientTz = client.timezone \|\| "America/New_York"` resolution; `const tz = timezone \|\| clientTz` replaces prior `tz = timezone \|\| "America/New_York"` (caller's explicit tz still wins, falls through to client's tz, ET as absolute fallback only). `currentTimeET` → `currentTimeLocal` rendered in `tz`. New `current_timezone: tz` dynamic var passed to Retell so the agent can reference it directly. `fetchGhlFreeSlots` default param kept as ET fallback with comment noting all call sites resolve `client.timezone` first. `addStep` "Manual Trigger" metadata logs `timezone: timezone \|\| "(client default)"` for accuracy. **Bug 2 DB-side (not in this repo — executed in-session, backups saved to `/tmp/bfd_voice_prompts_BACKUP.json` + `/tmp/bfd_retell_llm_BACKUP.json`):** BFD `voice_prompts` row (1 row in BFD's external supabase `qildpilxjodxdifggmto`): patched 2 strings in BOTH `system_prompt` (46k chars) AND `booking_prompt` (6.4k chars). String 1: `"I'm guessing you're on Eastern time - is that right?"` → `"I'm guessing you're on Sydney time - is that right?"`. String 2: `Always use IANA timezone format for tool calls (e.g., "America/New_York").` → `Always use IANA timezone format for tool calls (e.g., "Australia/Sydney").`. **Bug 2 Retell-side:** BFD's Retell LLM `llm_22e795de19b4d25cb579013586be` `general_prompt` (53k chars, hits=2+2=4 because Retell concatenates system+booking). Same 2-string patch via `PATCH /update-retell-llm/{id}` + `POST /publish-agent/agent_5ec5eb129f3165cfa07b581a1a` to make the new prompt live. **Verification (deferred to next live call):** outbound call's `pre_call_context.metadata.timezone` should be `Australia/Sydney`; agent should say "Sydney time" not "Eastern time"; `call_history.pre_call_context` JSON should contain `timezone: "Australia/Sydney"`. **Multi-tenant note:** the voice_prompts ↔ Retell LLM sync pattern is BFD-specific today. Future onboarding needs a deploy step that pushes voice_prompts → tenant's Retell agent (currently the "Voice AI Configuration" UI's Sync button or `copy-setter` edge fn). | **Bug 3 revert:** `git revert 756c7bd..HEAD` (or just revert the single commit) and redeploy Trigger.dev. Trivial — restores the silent failures but doesn't break anything. **Bug 2 revert (code):** `git revert 05106aa..HEAD`, redeploy edge fn via Management API PATCH back to the prior body. **Bug 2 revert (BFD voice_prompts):** PATCH `https://qildpilxjodxdifggmto.supabase.co/rest/v1/voice_prompts?id=eq.<id>` with the contents of `/tmp/bfd_voice_prompts_BACKUP.json`. **Bug 2 revert (Retell):** PATCH `https://api.retellai.com/update-retell-llm/llm_22e795de19b4d25cb579013586be` with the `general_prompt` from `/tmp/bfd_retell_llm_BACKUP.json`, then `POST /publish-agent/agent_5ec5eb129f3165cfa07b581a1a`. |
| 2026-05-13 | cadence-v2-mvp — Multi-phase lead nurture redesign approved 2026-05-13. Today's 9-node v1 cadence (3 SMS + 2 calls over 25h, then silence) is preserved; a NEW 28-node v2 cadence is built alongside as a DRAFT (is_active=false) for Brendan to eyeball + activate manually. v2 runs Phase 1 Hot Burst (Day 0-2, 6 touches, static SMS + 2 voice) → Phase 2 Warm Pursuit (Day 4-10, 5 touches, AI-generated SMS + email + 1 voice) → Phase 3 Cool Down (Day 11-21, 3 educational email touches, AI-generated). Plus a new `nudgeColdReply` Trigger.dev scheduled task that closes the cold-reply gap: leads who reply once then go silent get AI-generated nudges at +24h and +72h, then drop into long-tail nurture (Phase B). 5 commits, 7 deploys (1 schema migration via Management API + 5 Trigger.dev versions). | `35d1925` (Day 1+2), `0125af7` (Day 3), `c2352f1` (Day 4-5), `84728e0` (Day 6), `524ac08` (Day 7) | `phase-cadence-v2-mvp` (LOCAL, not yet pushed) | **Schema (bfd-platform Supabase, Management API):** 3 migration files applied across 5 commits. Files: `frontend/supabase/migrations/20260513140000_cadence_v2_phase_a_tracking.sql` (Day 1+2: adds `leads.last_outbound_at`, `last_inbound_at`, `last_reply_at` timestamptz; `leads.nudge_count` smallint; `leads.tagged_silent_after_engagement` bool; `engagement_workflows.schedule` jsonb), `20260513150000_cadence_v2_day3_email_metrics.sql` (Day 3: adds `cadence_metrics.emails_sent`, `ai_cost_cents`, `cost_estimate_cents` int columns), `20260513170000_cadence_v2_day7_bfd_28_node_workflow.sql` (Day 7: inserts new engagement_workflows row id `c206da3e-b8b7-41f8-9de0-997679abefcb` with 28-node v2 cadence as is_active=false draft). **Edge functions:** `frontend/supabase/functions/receive-twilio-sms/index.ts` v16 (both leads upserts now stamp `last_inbound_at`+`last_reply_at`; main inbound block also resets `nudge_count` to 0 so re-engaged leads start fresh). `frontend/supabase/functions/retell-call-webhook/index.ts` v11 (inbound calls stamp `last_inbound_at`+`last_reply_at`+reset `nudge_count`; outbound is handled in runEngagement upstream). Both deployed via Management API `PATCH /functions/{slug}` (CLI bundle hangs in this docker env, per Bug-1 session learning). **Trigger.dev tasks (v20260513.2 → v20260513.7, 10 detected tasks at end):** `trigger/runEngagement.ts` — extended EngageChannel.type union to include "email"; added `ai_generate` + `ai_prompt` channel fields; added `subject`, `body_format`, `from_email` for email; new helpers `parseSchedule` (mirrors parseQuietHours) + email send branch in engage-node loop + AI-copy generation gate. `sendTwilioSmsAndStamp` now takes `clientId` arg + bumps `leads.last_outbound_at` after every outbound SMS. Both phone_call sites (engage channel + legacy flat node) bump `leads.last_outbound_at` when the call is placed. Workflow SELECT extended to pull `schedule`, `openrouter_api_key`, `llm_model`. Schedule gating: const-null replaced with `parseSchedule(workflow.schedule)` so the always-built `getScheduleAwareBatchTime`/`getNextScheduleWindow` primitives are now reachable. metricsBuffer extended with `emails_sent` + `ai_cost_cents` counters; writeCadenceMetrics writes both + `cost_estimate_cents` on every terminal state, and inserts an `error_logs` warning if cost>500c. `trigger/processMessages.ts` — AI setter reply path bumps `leads.last_outbound_at` (separate from `last_message_at` which is the chat-list timestamp). NEW `trigger/_shared/aiGenerateEngagementCopy.ts` — OpenRouter call using `openai/gpt-4.1-nano` default (per-client override via `clients.llm_model`); reads last 10 chat_history rows from the client's external supabase; channel-specific system prompt; JSON-format response with subject (email only) + body; cost estimate in cents via openrouter usage tokens. Fallback discipline: if AI throws, runEngagement logs warning and falls back to static `ch.message`. NEW `trigger/_shared/ghl-conversations.ts pushEmailToGhl` — POST /conversations/messages with type:"Email" so GHL sends + logs in one call; Notes fallback if email infra not configured at the location. NEW `trigger/nudgeColdReply.ts` — schedules.task at cron `0 6 * * *` (06:00 UTC). Eligibility filter: `setter_stopped=false AND tagged_silent_after_engagement=false AND last_inbound_at IS NOT NULL AND last_outbound_at > last_inbound_at AND age(now()-last_outbound_at) >= tier-threshold AND age <= 14d AND nudge_count<3`. Tiers indexed by `nudge_count` at fire time: 0→+24h gentle nudge, 1→+72h reframe nudge, 2→no SMS, tag silent. maxLeadsPerRun=100, maxDuration=600s. AI-generates each nudge via aiGenerateEngagementCopy. Sends direct Twilio with same StatusCallback path. Stamps `nudge_count+1`, `last_outbound_at`, `message_queue`. **types.ts:** `engagement_workflows.schedule jsonb`, 5 new `leads` columns added to Row/Insert/Update. **BFD's v2 workflow content:** 14 engage + 14 wait_for_reply = 28 nodes. Channels: 4 static SMS (Phase 1) + 2 AI SMS (n15, n19) + 3 phone_call (Voice-Setter-2 all 3, treat_pickup_as_reply=true) + 5 AI email (n13, n21, n23, n25, n27). All AI channels carry explicit `ai_prompt` strings describing the touch intent for the LLM. **BFD prerequisites verified:** openrouter_api_key SET, llm_model='google/gemini-2.5-pro' (override; will use gemini-2.5-pro instead of nano — slightly more expensive per touch, ~$0.005 vs ~$0.001, but no blocker), supabase_url+supabase_service_key SET, ghl_api_key SET, ghl_location_id='xo0XjmenBBJxJgSnAdyM', timezone='Australia/Sydney'. **Out of MVP scope (Phase B):** Multi-workflow enrollment state machine (`engagement_enrollments` table); long-tail nurture as a separate workflow; behavioral re-warm via email click tracking; outbound LinkedIn DM; per-tenant timezone-aware nudgeColdReply cron; brand-voice prompt per-client/per-workflow override (today uses helper default). | **Schema rollback (in dependency order):** `ALTER TABLE engagement_workflows DROP COLUMN schedule;` `ALTER TABLE leads DROP COLUMN last_outbound_at, DROP COLUMN last_inbound_at, DROP COLUMN last_reply_at, DROP COLUMN nudge_count, DROP COLUMN tagged_silent_after_engagement;` `ALTER TABLE cadence_metrics DROP COLUMN emails_sent, DROP COLUMN ai_cost_cents, DROP COLUMN cost_estimate_cents;` Each is safe IF no code references the column. **Code rollback:** `git revert phase-cadence-v2-mvp..HEAD` (range includes 5 commits 35d1925..524ac08). Redeploy edge functions via Management API PATCH (paste prior body content from git history); redeploy Trigger.dev via `TRIGGER_ACCESS_TOKEN=$TRIGGER_DEPLOY_PAT npx trigger.dev@4.4.4 deploy`. **BFD v2 workflow rollback (the safer middle-ground):** `DELETE FROM engagement_workflows WHERE id='c206da3e-b8b7-41f8-9de0-997679abefcb';` Removes the draft only; v1 is untouched. Alternatively just leave v2 dormant — it's `is_active=false` so it never runs unless `clients.auto_engagement_workflow_id` is repointed to it. |
| 2026-05-13 | night-bug1-call-outcome-coordination — Phase A punch-list item (b) closed. Cadence now waits for Retell `call_ended` webhook before advancing past any `phone_call` channel, fixing the 2026-05-13 live-test bug where SMS n5 ("just tried calling...") fired 10 seconds into an active call. Coordination primitive: new `engagement_executions.last_call_outcome JSONB` column. **Write side:** `retell-call-webhook/index.ts` on `call_ended` stamps `{call_id, disconnect_reason, call_status, ended_at}` keyed by `dynamicVars.execution_id` (`make-retell-outbound-call` already passes `execution_id` through to Retell as a dynamic var, so no payload changes needed upstream). **Read side:** `runEngagement.ts` polls every 15s (`wait.for` is frozen — zero compute) for up to 10 min after each `phone_call` channel, breaking when `last_call_outcome.call_id` matches the call_id it placed. Classification (voicemail / no_connect / error / human_pickup) mirrors the existing inline classifier at `retell-call-analysis-webhook/index.ts:740-750` so the two webhooks stay consistent. On human pickup + per-channel `treat_pickup_as_reply=true`, the cadence terminates with new `stop_reason='call_engaged'`; otherwise (missed/voicemail/no_connect/timeout) advances normally so the missed-call SMS still fires when appropriate. Both phone_call sites in `runEngagement.ts` covered: the engage-node `phone_call` channel (~line 902) AND the legacy flat `phone_call` node (~line 1168). Helper functions `waitForCallOutcome` + `classifyCallOutcome` extracted (lines 243-298) for reuse. **Edge cases handled:** booking-during-call (`isCancelled()` catches `exec.status='completed'` from `bookings-webhook`); STOP keyword mid-call (existing setter_stopped check); `call_ended` arriving before the poll enters (first poll cycle finds the match immediately); `call_ended` never arriving (10-min ceiling treats as missed and advances). **Pre-existing safety net left in place:** `retell-call-analysis-webhook/index.ts:733-770` still has its own pickup termination on `call_analyzed`; it now gracefully no-ops because runEngagement terminates the exec to `status='completed'` first and the analysis-webhook filter `.in("status", ["pending", "running"])` skips the update. **Explore subagent audit** cleared all 6 cross-area risks (collision check, stop_reason enum breakage, retry semantics, redundant termination paths, callers of placeOutboundCall, orphaned stage_description). | `571e18f` | `phase-night-bug1-call-outcome-coordination` (LOCAL, not yet pushed) | **Schema (bfd-platform Supabase, Management API):** `ALTER TABLE engagement_executions ADD COLUMN IF NOT EXISTS last_call_outcome JSONB;` Migration committed at `frontend/supabase/migrations/20260513120000_bug1_last_call_outcome.sql`. Applied via Management API HTTP 201, verified via `information_schema.columns`. **Edge function — `frontend/supabase/functions/retell-call-webhook/index.ts:99-129`:** new block after the existing leads-upsert that, on `call_ended`, writes `last_call_outcome = { call_id: call.call_id\|\|call.id, disconnect_reason: call.disconnection_reason, call_status: call.call_status\|\|call.status, ended_at: now }` to `engagement_executions WHERE id = dynamicVars.execution_id`. Idempotent; harmless if exec already terminated. Deployed via Management API `PATCH /v1/projects/{ref}/functions/{slug}` route (Supabase CLI's docker-based `edge-runtime bundle` step was hanging — fell back to direct PATCH which works fine for single-file functions). Edge function bumped v9 → v10. **Trigger.dev — `trigger/runEngagement.ts`:** new helper `waitForCallOutcome(args)` at line 255 (polls every 15s up to 10 min, returns the outcome object or `"cancelled"` or `null` for timeout) and `classifyCallOutcome(outcome)` at line 282 (returns `"human_pickup"\|"voicemail"\|"no_connect"\|"error"\|"unknown"`). Engage-node `phone_call` channel handler (lines ~872-942) now calls placeOutboundCall, then `await waitForCallOutcome`, then `classifyCallOutcome`, then either terminates with `call_engaged` or falls through to `continue`. Same shape applied to legacy flat `phone_call` node handler (lines ~1140-1207) using `legacyTreatPickupAsReply`. Also fixes a pre-existing inconsistency where the legacy node path didn't increment `metricsBuffer.calls_attempted` (now it does, for symmetry with the engage path). Deployed via `TRIGGER_ACCESS_TOKEN=$TRIGGER_DEPLOY_PAT npx trigger.dev@4.4.4 deploy` — version `20260509.2` → `20260513.1` (9 detected tasks). **types.ts — `frontend/src/integrations/supabase/types.ts:1963`:** added `last_call_outcome: Json \| null` to `engagement_executions` Row/Insert/Update (memory `feedback_types_ts_drift` — keep types.ts in lockstep with prod schema). **Manual verification pending:** the deploy chain ran but the user will re-run the same live-form path test from §D tonight. Two scenarios: (1) pickup test — answer the call, expect NO mid-call SMS and `stop_reason='call_engaged'` or `'booking_created'` if booking happens; (2) missed test — let it ring out, expect `call_ended` with `disconnect_reason` like `no_answer`/`voicemail_reached` and SMS n5 fires AFTER call_ended (not during). | **Schema rollback (will re-break cadence on every phone_call):** `ALTER TABLE engagement_executions DROP COLUMN last_call_outcome;`. **Code rollback:** `git revert phase-night-bug1-call-outcome-coordination..HEAD` then redeploy edge function (PATCH the index.ts back to the v9 state, or use `pnpm dlx supabase functions deploy retell-call-webhook --project-ref bjgrgbgykvjrsuwwruoh` if the docker-bundle hang resolves) and redeploy Trigger.dev (`TRIGGER_ACCESS_TOKEN=$TRIGGER_DEPLOY_PAT npx trigger.dev@4.4.4 deploy`). Reverting will restore the mid-call-SMS bug — only do this if a follow-up fix replaces the coordination primitive with something else. |
| 2026-05-13 | night-ghl-form-pattern-b-verified — Phase A punch-list item (d) closed. GHL form → AI setter cadence chain verified end-to-end via the upstream-snapshot tag-based ingress (Pattern B): website form submit → form-to-tag workflow adds `1prompt - new lead` → `Add Lead to 1Prompt OS` workflow fires → webhooks `sync-ghl-contact` → CREATE path inserts leads + engagement_executions + fires Trigger.dev run-engagement → Twilio SMS → Retell voice call → voice-booking-tools books appointment → `BFD bookings → 1prompt (BOOKED)` workflow → bookings-webhook → cadence terminated with `stop_reason: booking_created`. Live test on Brendan's phone (+61405482446) successful — answered the AI call, booked 11:30 AM Sydney 2026-05-13 (`ghl_appointment_id=FZ2HiZvVBGbZ8VXBILIP`, booking row `e69574ba-81e0-400e-84a8-8c3cbdcff0f1`). Surfaced + fixed `clients.sync_ghl_enabled` column gap (types.ts drift hit #3) and identified two follow-up bugs (mid-call SMS, Twilio error extraction). | _DB-only + docs_ | `phase-night-ghl-form-pattern-b-verified` (LOCAL, not yet pushed) | **DB-only (bfd-platform Supabase, Management API):** `ALTER TABLE public.clients ADD COLUMN IF NOT EXISTS sync_ghl_enabled BOOLEAN NOT NULL DEFAULT true;` Column was selected at `sync-ghl-contact/index.ts:247` and gated at line 262, but had never existed in BFD's schema (since the original `5074dea` upstream import). Every call to `sync-ghl-contact` was returning `400 "No client found for the provided GHL_Account_ID"` regardless of payload — the SELECT errored before reaching the auth-resolution branch, the function caught it as `clientErr` and returned the generic "not found" message. Phase A never surfaced this because Phase A used the `intake-lead` path (which doesn't reference `sync_ghl_enabled`). Default `true` preserves the function's intent (gate ON for all tenants until explicitly disabled). **GHL UI (per-tenant, Brendan's work):** (1) Reconfigured `Add Lead to 1Prompt OS` workflow (id `7a4a8de3-2d77-43ce-a1e7-e387b9d4652b`) — Action 1 (Update Contact Field "GHL Account ID") value set to `xo0XjmenBBJxJgSnAdyM`; Action 2 (Custom Webhook) URL replaced from upstream-snapshot's `https://ffstcedrobcphobfuosl.supabase.co/...` (a different Supabase project) to BFD's `https://bjgrgbgykvjrsuwwruoh.supabase.co/functions/v1/sync-ghl-contact?clientId=e467dabc-57ee-416c-8831-83ecd9c7c925`. Body shape unchanged (already correct for sync-ghl-contact: Lead_ID, Name, Email, Phone, GHL_Account_ID). (2) Built form-to-tag bridge workflow `Form Submit - "1prompt - new lead" tag added` (id `848fbe89-a8d7-450d-8127-f1d9b09edee6`) — trigger Form Submitted filtered to `UpOvxLxfWHJvLE1NuZQR` only; single action Add Tag `1prompt - new lead`. (3) Disabled snapshot's `Add Booking to 1Prompt OS` workflow — BFD already uses the two-workflow status-aware booking pattern per WEBHOOKS.md §G. **Docs:** Added `Docs/CLIENT_ONBOARDING_SOP.md` §5.13 "GHL Snapshot ingress — Add Lead to 1Prompt OS (Pattern B — snapshot-imported clients)" — full architecture diagram, 5-step per-tenant config, dormant-Send*-workflow table, 8-hop verification checklist, common-failure troubleshooting. Updated §5.10 to re-title as "Pattern A — greenfield" and cross-link to §5.13. **Diagnostic shortcut documented:** pre-existing Twilio carrier-level opt-out (error 21610) from Phase A STOP testing blocked the first SMS attempt; Brendan cleared it by texting `START` from his phone. Memory `reference_twilio_carrier_optout` written. **Two new bugs identified (deferred):** Bug 1 (medium) — `runEngagement.ts` treats Retell `create-phone-call` 201 response as "call complete" and immediately advances to the next cadence node, firing the followup SMS during an active call. Should wait for `retell-call-webhook` `call_ended` event and branch on `disconnect_reason`. Files: `trigger/runEngagement.ts` phone_call handler + retell-call-webhook coordination. Bug 2 (small) — `runEngagement.ts:171` reads `error_code`/`error_message` from Twilio's response, but Twilio uses `code`/`message`. Today's failure surfaced as `"Twilio SMS failed: ? unknown"` instead of `"21610 Attempt to send to unsubscribed recipient"`. 15-min fix. Bug 3 (low-medium, re-confirmed) — voice agent `pre_call_context.metadata.timezone` = `America/New_York` instead of `clients.timezone='Australia/Sydney'`. Already listed in Phase A punch list (a). Lives in Retell agent prompt + voice-booking-tools dynamic vars. **No git changes today** — all schema + workflow config + doc edits; commit + tag pending (Brendan to push). | **DB-only rollback:** `ALTER TABLE public.clients DROP COLUMN sync_ghl_enabled;` — will re-break sync-ghl-contact for every tenant (it'll go back to the silent 400). Don't do this unless you also patch the function to remove the SELECT. **GHL rollback:** in Brendan's GHL location `xo0XjmenBBJxJgSnAdyM`, re-point `Add Lead to 1Prompt OS` webhook URL back to upstream's `ffstcedrobcphobfuosl.supabase.co/...` (will become a no-op for BFD; safe), delete the form-to-tag bridge workflow `848fbe89-...`, and re-publish `Add Booking to 1Prompt OS`. The cadence ingress will then revert to whatever it was before today (de facto: nothing, since the upstream snapshot was never wired to BFD's Supabase). **Docs rollback:** `git revert phase-night-ghl-form-pattern-b-verified..HEAD` once the tag exists. |
| 2026-05-09 | night-a-end-to-end-verified — Phase A end-to-end real-lead test PASSED on Brendan's phone (+61405482446). Three scenarios (voice-booking happy path, SMS reply, STOP keyword opt-out) all green. Surfaced + fixed 4 bugs in-session: (1) intake-lead + sync-ghl-contact auto-enroll trigger payload missing `make_retell_call_url`; (2) intake-lead + sync-ghl-contact missing `contact_fields` (so `make-retell-outbound-call` returned 400 "No phone number provided"); (3) `runEngagement.isCancelled()` ignored `leads.setter_stopped`, so STOP raced cadence-advance and let in-flight phone_call fire post-STOP; (4) `@supabase/supabase-js` accidentally bumped to 2.105.x by deploy lockfile-regen, breaking Trigger.dev runtime ("Node.js 21 detected without native WebSocket support"). Plus DB-only fix: BFD `clients.retell_outbound_agent_id` + `retell_outbound_followup_agent_id` were NULL — filled with the single existing agent (`agent_5ec5eb…`). | `edff01f` | `phase-night-a-end-to-end-verified` | **Edge functions:** `frontend/supabase/functions/intake-lead/index.ts` lines 186-208 (added `supabaseUrlEnv = Deno.env.get('SUPABASE_URL')!; makeRetellCallUrl = `${supabaseUrlEnv}/functions/v1/make-retell-outbound-call`;` plus added `make_retell_call_url` and `contact_fields: { phone, email, name, first_name }` fields to the Trigger.dev `run-engagement/trigger` payload). Same change shape applied to `frontend/supabase/functions/sync-ghl-contact/index.ts` lines 110-135. Both deployed to bfd-platform Supabase via `cd frontend && SUPABASE_ACCESS_TOKEN=$SUPABASE_PAT /tmp/supabase-cli/supabase functions deploy <slug> --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt --use-api`. **Trigger.dev task:** `trigger/runEngagement.ts` lines 291-336 — replaced single-table `isCancelled()` with `Promise.all` reading both `engagement_executions.status` AND `leads.setter_stopped` (joined on `client_id` + `lead_id`); when `setter_stopped=true` the helper UPDATEs the exec to `status='cancelled', stop_reason='setter_stopped', stage_description='Cancelled — lead opted out (setter_stopped).'` so analytics + UI reflect the opt-out cleanly. Deployed to Trigger.dev prod as `v20260509.2` (9 detected tasks) via `npx trigger.dev@4.4.4 deploy` with `TRIGGER_ACCESS_TOKEN=$TRIGGER_DEPLOY_PAT`. CLI version pinned to 4.4.4 to match installed `@trigger.dev/sdk` + `@trigger.dev/build`. **Package pin:** `package.json` `@supabase/supabase-js` constraint changed from `^2.101.0` → `2.101.0` (exact); `pnpm-lock.yaml` regenerated via `pnpm install --ignore-scripts`. Newer 2.105.x eagerly constructs `RealtimeClient` inside `createClient()` which calls `getWebSocketConstructor()` and throws when `globalThis.WebSocket` is undefined (Node 21 base image, no native WS). 2.101.0 doesn't have the eager init path. **DB-only:** `UPDATE clients SET retell_outbound_agent_id='agent_5ec5eb129f3165cfa07b581a1a', retell_outbound_followup_agent_id='agent_5ec5eb129f3165cfa07b581a1a' WHERE id='e467dabc-57ee-416c-8831-83ecd9c7c925';` Both slots were NULL; BFD intentionally only has one provisioned Retell agent. The cadence (`40e8bea3-…`) references Voice-Setter-2 (n3) and Voice-Setter-3 (n9) per the editor's slot-2-then-slot-3 schema; before this UPDATE, `make-retell-outbound-call` returned 400 "No Retell agent configured for Voice-Setter-2". **Verified end-to-end:** Test 1 (voice-booking) — booking `7ad909cf-7d1c-4671-bad5-7006105e6bd3` for Fri 8 May 10:30 AEST, `call_history.appointment_booked=true`, `cadence stop_reason=booking_created`. Test 2 (SMS reply) — "Hi" inbound, `stop_reason=inbound_reply`, AI setter reply landed at +1m58s. Test 3 (STOP) — `lead_optouts` row `b3b8f0f9-…` created, `setter_stopped=true`, cadence `stop_reason=setter_stopped`, no further outbound. Test 1's GHL appointment soft-cancelled in cleanup. Test 2 + 3's leads/execs left in-place per Brendan. **Punch list (deferred):** (a) Voice agent first offers slots in America/New_York despite `clients.timezone='Australia/Sydney'`; agent prompt or tool-arg default has ET baked in. (b) Cadence node n4 (`wait_for_reply` after phone_call) `timeout_seconds=1` so n5 SMS fires while live call still in-flight — bump to ~180s; same for n8. (c) Retell `custom_analysis_data.success_rate` is a boolean (looks like schema typo). (d) Manual end-to-end test from BFD website lead form. | **Code rollback:** `git revert phase-night-a-end-to-end-verified..HEAD`, then redeploy `intake-lead` + `sync-ghl-contact` (`SUPABASE_ACCESS_TOKEN=$SUPABASE_PAT /tmp/supabase-cli/supabase functions deploy <slug> --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt --use-api`) and `npx trigger.dev@4.4.4 deploy`. **Package rollback:** safe to leave `@supabase/supabase-js` pinned to `2.101.0` — older but functional. Re-bump only after confirming Trigger.dev's runtime supports native WebSocket OR the codebase passes a `transport: ws` option to `createClient`. **DB-only revert (per-slot):** `UPDATE clients SET retell_outbound_agent_id=NULL, retell_outbound_followup_agent_id=NULL WHERE id='e467dabc-57ee-416c-8831-83ecd9c7c925';` — but DON'T do this without first patching the cadence to use Voice-Setter-1 only, otherwise n3 + n9 phone_call nodes will fail with "No Retell agent configured". |
| 2026-05-06 | night-a7-bfd-auto-enrolment-on — Flip BFD's auto-enrolment ON: any new lead created via `sync-ghl-contact` (GHL Contact Created webhook) or `intake-lead` (web form) will now auto-enrol into the Default New-Lead Cadence (`40e8bea3-b6f6-4562-98d1-f7e6599af6a1`) and start firing SMSes + voice calls immediately. Phase A item A7. | _DB-only_ | `phase-night-a7-bfd-auto-enrolment-on` | **DB-only operation (no code change in this repo).** `UPDATE clients SET auto_engagement_workflow_id = '40e8bea3-b6f6-4562-98d1-f7e6599af6a1' WHERE id = 'e467dabc-57ee-416c-8831-83ecd9c7c925'`. Returned 1 row updated. **Pre-flight verified clean before flip:** `engagement_workflows.40e8bea3-…` has `is_active=true`, `node_count=9`, zero `[BRENDAN:…]` placeholders in any node's `message`, zero "placeholder" mentions; `clients.subscription_status='active'`; `clients.timezone='Australia/Sydney'`. **Code path that fires this:** `sync-ghl-contact/index.ts:380-400` and `intake-lead/index.ts:136-250` both read `auto_engagement_workflow_id` and call `enrollLeadInEngagement` which inserts into `engagement_executions` and triggers Trigger.dev `run-engagement`. `receive-twilio-sms` does NOT auto-enrol on inbound SMS — only contact-create + intake-lead paths do (intentional — inbound SMS implies an existing lead, replies route to `process-setter-reply` instead). **Why now:** A1, A3, A4 closed; A2 in soak day 7/14 clean; the two A3 follow-ups (#2 + #3) shipped earlier today. A5 (voicemail) and A6 (sig verification) deliberately deferred — A6 must be LAST among A1-A6 because once secrets are set sig-mismatch returns 403 and silently kills inbound. Flipping A7 now unblocks the end-to-end real-lead test (Step 3 of the State-of-Play plan). | **DB-only revert (instant):** `UPDATE clients SET auto_engagement_workflow_id = NULL WHERE id = 'e467dabc-57ee-416c-8831-83ecd9c7c925';`. New leads will stop auto-enrolling immediately; any already-running `engagement_executions` continue to completion (revert doesn't cancel mid-flight cadences — use `stop-engagement` for that). |
| 2026-05-06 | night-a3-followup3-timezone-default — Default `get-available-slots` timezone to `clients.timezone` so the voice agent doesn't first offer Pacific slots before the user mentions their TZ | `c4499ed` | `phase-night-a3-followup3-timezone-default` | **Edge function — `frontend/supabase/functions/voice-booking-tools/index.ts`:** `ClientRow` type extended with `timezone: string \| null`. The `clients` SELECT at line 99 extended to include `timezone` (column was added in A4's `20260505100000_phase_night_a4_clients_timezone.sql` migration with default `Australia/Sydney`). `toolGetAvailableSlots` (line 294-305) now falls back to `client.timezone` when the caller body / URL don't supply `timeZone`/`timezone`. **Root cause:** Without a default, the function passed `undefined` to GHL's `/calendars/{id}/free-slots` endpoint, which falls back to Pacific — agent's first slot offer for AU leads was always wrong (recovered on the second tool call after the lead said "I'm in Australia"). **Verified:** deployed to bfd-platform Supabase via `npx supabase functions deploy voice-booking-tools --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. Behaviour will be exercised live in the upcoming end-to-end test (Step 3 of the State-of-Play plan); first call from BFD's voice agent without a `timeZone` arg should now return slots in Australia/Sydney. **No schema change** — column already existed from A4. | `git revert phase-night-a3-followup3-timezone-default..HEAD` then redeploy: `cd frontend && SUPABASE_ACCESS_TOKEN=$SUPABASE_PAT npx supabase functions deploy voice-booking-tools --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. |
| 2026-05-06 | night-a3-followup2-appointment-booked-mapping — Recognise Retell's `custom_analysis_data["Call result"] = "Call Booked"` shape so `call_history.appointment_booked` flips to true on real voice-tool bookings | `72823a8` | `phase-night-a3-followup2-appointment-booked-mapping` | **Edge function — `frontend/supabase/functions/retell-call-analysis-webhook/index.ts:368-377`:** the OR-chain that sets `appointmentBooked` from `customAnalysis` previously only recognised typed-bool variants (`appointment_booked === true \|\| booked === true`). Retell's post-call analysis prompts on BFD's agent emit the booking outcome as `customAnalysis["Call result"] = "Call Booked"` (proper-cased key, string value), so the flag stayed false even when the booking landed in `bookings` via the voice-tool path. **Fix:** read both `customAnalysis["Call result"]` (Retell default casing) and `customAnalysis.call_result` (snake-case alt) into `callResultStr`, then OR-in `/\bbook(?:ed\|ing)?\b/i.test(callResultStr)`. `call_history.appointment_booked` (line 408) now reflects the booking; lines 478 + 507 already mirror this boolean to GHL Note body + the per-client `ghl_call_appt_booked_field_id` custom field, so both downstream surfaces self-correct without further change. **Verified:** deployed to bfd-platform Supabase via `npx supabase functions deploy retell-call-analysis-webhook --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. Behaviour will be exercised live in the upcoming end-to-end test (Step 3 of the State-of-Play plan); a successful voice-tool booking should now write `call_history.appointment_booked=true` and a GHL Note with "Appointment Booked: Yes". **No schema change.** | `git revert phase-night-a3-followup2-appointment-booked-mapping..HEAD` then redeploy: `cd frontend && SUPABASE_ACCESS_TOKEN=$SUPABASE_PAT npx supabase functions deploy retell-call-analysis-webhook --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. |
| 2026-05-05 | night-a4-bookings-webhook-wired — Wire GHL Calendar workflow → `bookings-webhook` (Phase A item 4) and fix the TZ-naive date strings GHL emits via workflow merge tags | `d0f1b5e` | `phase-night-a4-bookings-webhook-wired` | **Schema (bfd-platform Supabase, Management API):** `ALTER TABLE public.clients ADD COLUMN IF NOT EXISTS timezone text NOT NULL DEFAULT 'Australia/Sydney';` Migration committed at `frontend/supabase/migrations/20260505100000_phase_night_a4_clients_timezone.sql`. **Edge function — `frontend/supabase/functions/bookings-webhook/index.ts`:** new `parseGhlTimestamp(s, tz)` helper that detects ISO 8601 inputs (with T + Z\|±hh:mm) and passes them through, OR treats TZ-naive locale strings ("Tuesday, 5 May 2026 8:52 PM") as wall-clock time in `tz` and returns ISO-with-offset. Client SELECT extended to include `timezone`; the column is read into `clientTz` (default `Australia/Sydney`) and used for parsing both `appointment_time` + `appointment_end_time` before insert. **Root cause:** GHL workflow custom-webhook merge tags only expose `{{appointment.start_time}}` / `{{appointment.end_time}}` as locale strings in the location's wall clock with no offset. `appointmentStatus`, `calendarId`, `locationId`, and `contactId` (as `{{appointment.contact_id}}`) are not in the picker — confirmed via [GHL merge fields docs](https://help.gohighlevel.com/support/solutions/articles/48001078171-list-of-merge-fields). Without a known timezone, Postgres parses the string as UTC, storing `appointment_time` ~10h off for AU clients. **GHL-side wiring (per-tenant pattern, documented in `Docs/WEBHOOKS.md` §G for the SOP):** two workflows per tenant, each filtered to one status case, with the missing fields hardcoded. **Workflow #1 BOOKED:** Appointment Status trigger × 2 rows (filter=`new`, filter=`confirmed`) → Custom Webhook POST `application/x-www-form-urlencoded` to `/functions/v1/bookings-webhook` with 8 key-value rows: `appointmentId={{appointment.id}}`, `contactId={{contact.id}}`, `calendarId=<hardcoded>`, `locationId=<hardcoded>`, `startTime={{appointment.start_time}}`, `endTime={{appointment.end_time}}`, `status=confirmed` (hardcoded), `type=appointment`. **Workflow #2 CANCELLED:** same shape, filter=`cancelled`, body `status=cancelled`. **Verified end-to-end against BFD's live GHL location:** 1) API-created appt `5D8DnFYa1VetJqTd1PRv` → BOOKED workflow fired in 5s → row written with `status=confirmed`, `source=ghl_calendar`, `appointment_time=2026-05-05 10:52:00+00` (correct UTC for 8:52 PM AEST input); 2) Soft-cancel via PUT `appointmentStatus=cancelled` → CANCELLED workflow fired in 5s → same row flipped to `status=cancelled`; 3) `error_logs WHERE source ILIKE '%bookings-webhook%'` returned 0 rows. Test row + GHL appointment cleaned up. **Docs:** `User Todos.md` A4 marked DONE; `Docs/WEBHOOKS.md` row updated + new §G tenant-onboarding webhook-setup section added; **Followup:** the hardcoded `calendarId`/`locationId` per-tenant pattern is a known scaling cost — multi-calendar tenants in Phase C+ will need a different path (likely the marketplace-app webhook subscription which gets the full payload). Logged in `User Todos.md §A4` and `Docs/WEBHOOKS.md §G`. | **Schema rollback:** `ALTER TABLE public.clients DROP COLUMN timezone;` (will re-break date parsing for all tenants on inbound bookings webhooks). **Code rollback:** `git revert phase-night-a4-bookings-webhook-wired..HEAD` then redeploy: `cd frontend && SUPABASE_ACCESS_TOKEN=$SUPABASE_PAT npx supabase functions deploy bookings-webhook --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. **GHL rollback:** delete (or pause) both `BFD bookings → 1prompt (BOOKED)` + `(CANCELLED)` workflows in BFD's GHL Automation tab — `bookings` will go back to only catching voice-tool bookings. |
| 2026-05-04 | night-a3-closed-and-webhooks-doc — Repoint BFD voice tools off legacy n8n onto native `voice-booking-tools` edge function (config-only change in the Retell UI), Bearer auth enforced, end-to-end test booking verified + cleaned up; adds `Docs/WEBHOOKS.md` as the canonical webhook URL reference for the project plus `scripts/snapshot_voice_tools.mjs` as a read-only Retell tool-inventory diff tool | `1f1ca45` | `phase-night-a3-closed-and-webhooks-doc` | **No edge-function or DB change in this repo.** The functional repoint was performed in the Retell dashboard against agent `agent_5ec5eb129f3165cfa07b581a1a` / LLM `llm_22e795de19b4d25cb579013586be` (BFD's only provisioned slot — `retell_outbound_agent_id` and `retell_outbound_followup_agent_id` are still NULL on `clients`, contrary to the original A3 spec which assumed 3 agents). For each of the 5 booking tools (`get-available-slots`, `book-appointments`, `get-contact-appointments`, `update-appointment`, `cancel-appointments`) the API Endpoint was changed from `https://n8n-1prompt.99players.com/webhook/e4cffeea-b611-4722-907d-4fc7554efe30` to `https://bjgrgbgykvjrsuwwruoh.supabase.co/functions/v1/voice-booking-tools?tool=<name>&clientId=e467dabc-57ee-416c-8831-83ecd9c7c925`, and an `Authorization: Bearer <intake_lead_secret>` header was added (mandatory — `voice-booking-tools/index.ts:106-113` returns 401 when the client has a secret set). The agent was published after the LLM patches. Sanity checks: `POST` with Bearer → HTTP 400 "startDateTime required" (auth + tenant resolution work), `POST` without Bearer → HTTP 401 "Authorization Bearer required for this client". Real test call `call_211ba69142d19f295bbcef6e904` (92s, agent_hangup, sentiment Positive) wrote `bookings` row `aa10c0dc-d80a-4d32-ab46-193d45fd659d` with `source=voice_call`, `ghl_appointment_id=j1dUa0ySnaIr0KSdmHzH`, GHL appointment + Google Meet generated; row hard-deleted + GHL appointment soft-cancelled as test-data cleanup. **Files in `1f1ca45`:** `User Todos.md` (A3 marked DONE 2026-05-04 with full summary), `Docs/WEBHOOKS.md` (NEW — single source of truth for every inbound/outbound/legacy webhook in the platform plus per-client URL templates and an auth conventions table), `scripts/snapshot_voice_tools.mjs` (NEW — Node ESM script that hits Supabase Mgmt API for `clients.retell_*_agent_id` and Retell REST `/get-agent` + `/get-retell-llm`, dumps every tool's name + URL + headers to `scripts/.snapshots/<timestamp>.json`), `scripts/.gitignore` (NEW — keeps `.snapshots/` output local). Known follow-ups (none block A3 or soak): (a) `bookings.cadence_execution_id` is null because auto-enrolment (A7) is still off; (b) `call_history.appointment_booked=false` despite the booking — `retell-call-analysis-webhook/index.ts:368-370` only checks `appointment_booked === true \|\| booked === true`, doesn't recognise Retell's `custom_analysis_data["Call result"] = "Call Booked"` shape; (c) `get-available-slots` defaults to Pacific Time when neither body nor URL passes `timeZone` (`voice-booking-tools/index.ts:294-305`) — agent corrected after the user said "I'm in Australia"; (d) Phase E persona — agent introduces itself as "Aria from Brendan's team", upstream Anne/Eugene scaffolding still present. | **Tag delete + commit revert:** `git tag -d phase-night-a3-closed-and-webhooks-doc; git push origin :refs/tags/phase-night-a3-closed-and-webhooks-doc; git revert 1f1ca45..HEAD`. **Retell tool URL revert:** in the Retell dashboard for each of the 5 booking tools on `agent_5ec5eb129f3165cfa07b581a1a`'s LLM `llm_22e795de19b4d25cb579013586be`, replace API Endpoint with `https://n8n-1prompt.99players.com/webhook/e4cffeea-b611-4722-907d-4fc7554efe30`, remove the `Authorization` header, save each tool, then publish the agent. The legacy n8n endpoint is still up. Optional: clear the per-client secret with `UPDATE clients SET intake_lead_secret = NULL WHERE id='e467dabc-…'` (skips bearer enforcement when null). |
| 2026-05-03 | night-docs-sweep-a1-close — Update CADENCE_DESIGN spec + User Todos A1 status to reflect today's three shipped patches | `2ef715e` | `phase-night-docs-sweep-a1-close` | **`Docs/CADENCE_DESIGN.md`:** rewrote the Default new-lead cadence section (lines 212-236). Old table described `delay`-between-engages (the model that was authored when the spec was written but that crashes the editor). New table shows the wait_for_reply model live in BFD's row, with the actual A1-reviewed copy + new timing (n2=60s, n4=1s, n6=8h, n8=16h). Added a model-note paragraph above the table warning future authors that the editor canvas crashes on bare `delay` nodes between engages — only `wait_for_reply` is supported between engagements; a single optional `delay` at the start (before engage1) renders as "Initial Wait" and is fine. Voicemail-config note kept at the bottom (still null on BFD; B4 will configure via UI). **`User Todos.md`:** A1 marked DONE 2026-05-03 with summary of the three shipped tags and a note that auto-enrolment (A7) remains gated. **`Docs/CHANGES_LOG.md`:** this row. No code changes; no schema changes. Pure docs sweep capturing what the three earlier 2026-05-03 commits actually did, so future readers can find the truth in the spec rather than chasing tags. | `git revert phase-night-docs-sweep-a1-close..HEAD`. No DB or runtime impact. |
| 2026-05-03 | night-bfd-cadence-restructure-for-editor — Convert BFD's Default New-Lead Cadence delay→engage→delay→engage pattern to engage→wait_for_reply→engage so the Engagement editor canvas renders, plus apply A1 copy edits | `4595805` | `phase-night-bfd-cadence-restructure-for-editor` | **Data (bfd-platform Supabase, Management API):** rewrote `engagement_workflows.nodes` for `id='40e8bea3-b6f6-4562-98d1-f7e6599af6a1'` (BFD's Default New-Lead Cadence). Node count 9 unchanged. Type breakdown was 5 engage + 4 delay + 0 wait_for_reply, now 5 engage + 0 delay + 4 wait_for_reply. Migration committed at `frontend/supabase/migrations/20260503094500_phase_night_bfd_cadence_restructure_for_editor.sql` (BFD-specific WHERE clause; no-op for other clients on rebuild). **Root cause:** `frontend/src/pages/Engagement.tsx:3110-3131` `canvasNodes` useMemo loops `engageNodes` and accesses `waitNodes[i]!.id` for every non-last engage. With 5 engages and 0 waits the access threw `TypeError: Cannot read properties of undefined (reading 'id')` and the page rendered blank. The `delay`-between-engages model authored in `Docs/CADENCE_DESIGN.md:216-226` was inconsistent with the editor's `wait_for_reply`-between-engages assumption. `trigger/runEngagement.ts` handles both node types correctly, so runtime was fine; only the editor crashed. **Runtime impact of the swap:** wait_for_reply ([trigger/runEngagement.ts:1039-1083](https://example/)) waits the same duration as delay then runs an extra `message_queue` lookup at the end of the wait window to detect any reply that arrived during it. Phase 4c `receive-twilio-sms` already cancels active cadences on reply via webhook, so the inline check is redundant but harmless and produces a `time_to_first_response_seconds` metric for free. **Copy edits rolled in (per User Todos.md A1):** n1 SMS now `Hey {{first_name}}, Brendan here — calling you in 1 min about your enquiry.` (dropped "from Building Flow Digital", changed 2 min → 1 min); n3 instructions updated "2 minutes ago" → "1 minute ago" to match new n2 timing; n7 SMS now `Hey {{first_name}}, still keen for a quick chat about your enquiry? Brendan` (dropped "Got a window today if you're around"); n9 instructions stripped of "If voicemail, drop the configured voicemail script" sentence (voicemail handling deferred to Phase B4 UI). **Timing changes:** n2 120s → 60s, n4 1680s → 1s (immediate; min=1 in editor), n6/n8 unchanged. **Verified:** `jsonb_array_length(nodes) = 9`, engage_count=5, wait_count=4, delay_count=0. **Followup:** `Docs/CADENCE_DESIGN.md` table at lines 216-226 still describes the old delay model — should be updated to wait_for_reply to keep the spec in sync with what the editor produces. | **Data rollback:** restore the prior nodes JSON from `frontend/supabase/migrations/20260503094500_*.sql` git history (`git show 9233674:frontend/supabase/migrations/20260503094500_phase_night_bfd_cadence_restructure_for_editor.sql` will not have it pre-this-commit; use the SHA before this row's commit to recover the prior in-DB shape if needed). The prior workflow can also be reconstructed from the row 7 cadence dump in this changelog. **Code rollback:** `git revert phase-night-bfd-cadence-restructure-for-editor..HEAD` — safe; the migration file is BFD-specific and reverting only removes the file, not the prod row (the prod row stays in the new shape unless you run an inverse UPDATE). |
| 2026-05-03 | night-engagement-campaigns-missing-cols — Add `enroll_webhook_token` + `text_setter_number` columns to `engagement_campaigns` so the Engagement editor renders for any workflow | `9233674` | `phase-night-engagement-campaigns-missing-cols` | **Schema (bfd-platform Supabase, Management API):** `ALTER TABLE public.engagement_campaigns ADD COLUMN IF NOT EXISTS enroll_webhook_token text NOT NULL DEFAULT gen_random_uuid()::text, ADD COLUMN IF NOT EXISTS text_setter_number integer NOT NULL DEFAULT 1;` plus `UNIQUE (enroll_webhook_token)` constraint so no two campaigns can collide on the same incoming webhook token. Migration committed at `frontend/supabase/migrations/20260503093000_phase_night_engagement_campaigns_missing_cols.sql`. **Root cause:** opening the Engagement editor for any workflow showed a blank page. `frontend/src/pages/Engagement.tsx:2787-2795` reads `enroll_webhook_token` + `text_setter_number` via `.maybeSingle()` which silently returned null on the missing-column error; the auto-create insert at `:2799-2802` also failed on its RETURNING clause (`.select('id, enroll_webhook_token').single()`); the page either crashed during render or stalled. `engagement_campaigns` had **0 rows total** because every auto-create on every page-load had been failing silently for the lifetime of the table. Same class of bug as `phase-night-engagement-workflows-missing-cols` 30 minutes earlier — `frontend/src/integrations/supabase/types.ts:1867,1871` declares both columns as Row fields (NOT NULL) so the TypeScript was generated against a schema that the production DB never matched. **Verified:** post-patch `information_schema.columns` confirms both columns present with expected defaults (`enroll_webhook_token text DEFAULT gen_random_uuid()::text NOT NULL`, `text_setter_number integer DEFAULT 1 NOT NULL`); no existing rows to backfill. **Followup risk surfaced:** the wider `types.ts` may declare other Row fields the prod schema is missing. Worth a one-shot audit before Client #2 onboarding (Phase C) to avoid hitting the same class of bug under demo conditions. | **Schema rollback:** `ALTER TABLE public.engagement_campaigns DROP CONSTRAINT engagement_campaigns_enroll_token_unique; ALTER TABLE public.engagement_campaigns DROP COLUMN enroll_webhook_token, DROP COLUMN text_setter_number;` (will re-blank the Engagement editor; only do this if a future migration replaces them with something else). **Code rollback:** none needed; `git revert phase-night-engagement-campaigns-missing-cols..HEAD` is safe. |
| 2026-05-03 | night-engagement-workflows-missing-cols — Add `sort_order` + `is_active` columns to `engagement_workflows` so the Workflows page Campaigns tab renders DB rows | `9578fd5` | `phase-night-engagement-workflows-missing-cols` | **Schema (bfd-platform Supabase, Management API):** `ALTER TABLE public.engagement_workflows ADD COLUMN IF NOT EXISTS sort_order integer NOT NULL DEFAULT 0, ADD COLUMN IF NOT EXISTS is_active boolean NOT NULL DEFAULT true;` plus belt-and-braces backfill (the `DEFAULT`s already cover the only existing row, BFD's default cadence `40e8bea3-…`). Migration committed at `frontend/supabase/migrations/20260503090000_phase_night_engagement_workflows_missing_cols.sql` for reproducibility on rebuilds (Client #2 onboarding will spin up a fresh external Supabase per the SOP and needs every prior surgical patch on disk). **Root cause:** `frontend/src/pages/Workflows.tsx:273-277` `fetchAll()` selects `'id, name, is_active, nodes, sort_order, is_new_leads_campaign, new_leads_tag, client_id'` and orders by `sort_order`, but neither column had ever existed on the table. PostgREST returned an error, the page swallowed it (no try/catch surface), and every Campaigns tab rendered "No campaigns yet" regardless of what was actually in `engagement_workflows` for the active client. Means **no engagement workflow has been editable in the UI for any client** since `engagement_workflows` was introduced, and Phase B2's NEW LEADS toggle + the drag-reorder code in `Workflows.tsx:188-195` could not have worked either. No frontend code change was needed once the columns existed; reload-with-cache-bust (`Ctrl+Shift+R`) is sufficient. **Verified:** post-patch `information_schema.columns` confirms both columns present with expected defaults (`is_active boolean DEFAULT true NOT NULL`, `sort_order integer DEFAULT 0 NOT NULL`); BFD's row reads back `sort_order=0, is_active=true`. | **Schema rollback:** `ALTER TABLE public.engagement_workflows DROP COLUMN sort_order, DROP COLUMN is_active;` (will re-break the Campaigns tab; only do this if a future migration replaces these columns with something else). **Code rollback:** none needed — no code commit other than the migration file + this docs row; `git revert phase-night-engagement-workflows-missing-cols..HEAD` is safe. |
| 2026-05-02 | night-modify-prompt-ai-empty-fix — Allow empty fullPrompt in modify-prompt-ai + surface body-level errors | `431e34f` | `phase-night-modify-prompt-ai-empty-fix` | **Edge function — `frontend/supabase/functions/modify-prompt-ai/index.ts`:** brand-new mini-prompts (e.g. BFD Voice Setter → Custom Instructions, "No versions yet", blank body) sent `fullPrompt=""` to the function. The validation `if (!fullPrompt \|\| !userMessage)` returned 400 "Full prompt and user message are required.", which surfaced in the AI Assistant chat panel as the generic supabase-js wrapper "Edge Function returned a non-2xx status code" and blocked the legitimate "generate from scratch" UX. **Fix:** validation now only requires `userMessage`; `fullPrompt` is optional and a new `isEmptyPrompt = fullPrompt.trim() === ""` flag picks one of two SYSTEM_PROMPTs. The empty path uses a new `SYSTEM_PROMPT_GENERATE` that asks for a single mini-prompt section's content (no separator-preservation rules, no section-count rules) and stores `original_section_count: 1`. The non-empty path uses `SYSTEM_PROMPT_MODIFY` (renamed from `SYSTEM_PROMPT`, identical content) preserving the multi-section flow. Trigger.dev payload shape unchanged. **Frontend — `frontend/src/components/MiniPromptAIDialog.tsx` + `frontend/src/components/PromptAIChatPanel.tsx`:** on non-2xx invokes, both files now read `error.context.json()` and override `errorMessage` with the body-level `error` string when present, so the actual cause reaches the chat panel instead of the wrapper. **Pre-flight diagnosis:** queried `ai_generation_jobs` and `function_edge_logs` via Supabase Management API. Two distinct bugs surfaced: (a) the empty-prompt 400 above (Bug A, fixed here); (b) a separate runAiJob `max_tokens=262142` inflation that hit on 2026-05-02 02:17 UTC for the only modify-prompt-ai job that reached Trigger.dev — already fixed by `61f3af6` and verified deployed since the next generate-setter-config run completed at 09:57 UTC. **Smoke test (curl against deployed bfd-platform fn):** empty `fullPrompt` + `userMessage="Add stronger objection handling - one paragraph"` → 200 + job_id; job `93958eed-…` completed with valid `{modifiedPrompt, summary}`. Multi-section regression with `## IDENTITY` + separator + `## TONE` + `userMessage="make tone slightly more confident"` → job `b46d04c6-…` completed; both sections + the `── ── ──` separator preserved verbatim, only TONE content updated to "Confident, friendly, and direct." Edge function deployed via `npx supabase functions deploy modify-prompt-ai --project-ref bjgrgbgykvjrsuwwruoh`. Frontend deploys with the next Railway push. | `git revert phase-night-modify-prompt-ai-empty-fix..HEAD` then redeploy `modify-prompt-ai` (`cd frontend && SUPABASE_ACCESS_TOKEN=<pat> npx supabase functions deploy modify-prompt-ai --project-ref bjgrgbgykvjrsuwwruoh`). |
| 2026-05-02 | night-ghl-push-gap-1 — Voice call summary + sentiment + appt_booked → GHL Note + custom fields | `9c91442` | `phase-night-ghl-push-gap-1` | **Schema (bfd-platform Supabase, Management API):** `ALTER TABLE public.clients ADD COLUMN IF NOT EXISTS ghl_call_sentiment_field_id text, ADD COLUMN IF NOT EXISTS ghl_call_appt_booked_field_id text;` (migration `frontend/supabase/migrations/20260502130000_phase_night_ghl_call_fields.sql`). **Edge function — `frontend/supabase/functions/retell-call-analysis-webhook/index.ts`:** GHL gap 1 block inserted after the call_history upsert (Step 3). Fires when `call_analyzed` event has a `contact_id` dynamic variable set AND client has `ghl_api_key`. Writes a `POST /contacts/{contactId}/notes` with the call summary, duration, sentiment, and appointment_booked flag. Also PATCHes `PUT /contacts/{contactId}` with `customFields` for `last_call_sentiment` + `last_call_appointment_booked` when the per-client field ids are configured. Best-effort: all GHL calls wrapped in try/catch, failures log `console.warn` only. Deployed to bfd-platform Supabase. **Docs:** `GHL_PUSH_AUDIT.md` gap 1 marked CLOSED, path 8 added to §A, §C recommendation summary updated. `CLIENT_ONBOARDING_SOP.md` §5.12 added with custom field creation steps. | **Schema rollback:** `ALTER TABLE public.clients DROP COLUMN ghl_call_sentiment_field_id, DROP COLUMN ghl_call_appt_booked_field_id;`. **Code rollback:** `git revert phase-night-ghl-push-gap-1..HEAD`, redeploy `retell-call-analysis-webhook` (`cd frontend && SUPABASE_ACCESS_TOKEN=<pat> npx supabase functions deploy retell-call-analysis-webhook --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`). |
| 2026-05-02 | night-copy-setter-fix — runAiJob max_tokens overflow → copy-setter 400 error | `61f3af6` | `phase-night-copy-setter-fix` | **`trigger/runAiJob.ts`:** `callOpenRouter` was unconditionally setting `max_tokens = modelMax` (the model's theoretical max completion tokens — 262142 for Gemini 2.5 Pro). When the prompt consumed ~30k tokens, total exceeded the 262144-token context window → OpenRouter 400 `maximum context length exceeded`. Fix: capture the caller's value before overwriting; use `Math.min(callerValue, modelMax)` so the model ceiling acts as a cap, not a floor. Callers passing 32000 (copy-setter) now have that value honoured. Trigger.dev tasks redeployed at version `20260502.2` (9 detected tasks). | `git revert phase-night-copy-setter-fix..HEAD` then `TRIGGER_ACCESS_TOKEN=tr_pat_... npx trigger.dev@4.4.4 deploy --env prod`. |
| 2026-05-02 | night-ghl-push-gaps-2-3 — Inbound + outbound SMS body mirror to GHL | `c8454c1` | `phase-night-ghl-push-gaps-2-3` | **Schema (bfd-platform Supabase, Management API):** `ALTER TABLE public.clients ADD COLUMN IF NOT EXISTS ghl_conversation_provider_id text;` Optional config knob — when set, the helper logs SMS bodies via GHL Conversations API (`POST /conversations/messages/{inbound\|outbound}`); when null, falls back to GHL Notes API (`POST /contacts/{id}/notes` with `[platform → SMS inbound\|outbound]` prefix). Backwards-compatible: existing clients keep working via Notes fallback. **NEW shared helper:** `frontend/supabase/functions/_shared/ghl-conversations.ts` exporting `pushSmsToGhl({ ghlApiKey, ghlLocationId, contactId, conversationProviderId, message, direction, altId, occurredAt })`. Direction-aware. Idempotent via `altId = twilio_message_sid` when Conversations endpoint is in use. Never throws — returns `{ ok, via: "conversations"\|"notes"\|"skipped", status?, error? }`. Mirrored at `trigger/_shared/ghl-conversations.ts` (Node-runtime copy, identical body). **Inbound mirror (gap 2) — `frontend/supabase/functions/receive-twilio-sms/index.ts`:** after `findOrCreateGhlContact` returns and `setGhlContactChannel` is fired, schedule `pushSmsToGhl(direction='inbound', altId=messageSid)` via a new `scheduleGhlMirror` helper that uses `EdgeRuntime.waitUntil` so the fetch completes after the TwiML response. STOP/START block extended: after the auto-reply send, both the inbound keyword and the outbound auto-reply are mirrored for every matched lead row (silently skipped when sender has no matching lead). `clients` select extended to include `ghl_conversation_provider_id`. **Outbound mirror (gap 3) — `trigger/processMessages.ts`:** after every successful direct-Twilio send in the setter-reply block (line ~417+), gated on `client.use_native_text_engine === true` (so the legacy n8n+GHL workflow path doesn't double-mirror), `await pushSmsToGhl(direction='outbound', altId=twilioJson.sid)`. `clients` select extended to include `ghl_api_key`, `ghl_location_id`, `ghl_conversation_provider_id`. **Outbound mirror — `trigger/runEngagement.ts`:** `sendTwilioSmsAndStamp` signature extended with `ghlApiKey`/`ghlLocationId`/`ghlContactId`/`ghlConversationProviderId`; after the `message_queue` stamp, `await pushSmsToGhl(direction='outbound', altId=twilioJson.sid)`. Both call sites (engage-node + legacy `send_sms` node) updated to thread the new fields through. `clients` select extended. **Deploys:** `receive-twilio-sms` redeployed with helper bundled. Trigger.dev tasks redeployed at version `20260502.1` (9 detected tasks). | **Schema rollback:** `ALTER TABLE public.clients DROP COLUMN ghl_conversation_provider_id;`. **Code rollback:** `git revert phase-night-ghl-push-gaps-2-3..HEAD`, redeploy `receive-twilio-sms` + redeploy Trigger.dev tasks (`npx trigger.dev@4.4.4 deploy --env prod`). |
| 2026-05-01 | night-audit-batch2 — SEC-001 JWT-gate analyze-chat-history + ARCH-016 sms_delivery_events dedupe | `dfadde8` | `phase-night-audit-batch2` | **NEW shared helper:** `frontend/supabase/functions/_shared/assert-client-access.ts` extracted from `retell-proxy/index.ts:53-100` so every JWT-gated edge function can use the same canonical pattern (decode JWT sub, look up `clients.agency_id` + `user_roles.role` + `profiles.agency_id|client_id`, allow only matching agency or client users). Exports `assertClientAccess(authHeader, clientId)` and `AssertAccessError`. **SEC-001 (Critical) — `frontend/supabase/functions/analyze-chat-history/index.ts`:** added JWT-gating block right after the `clientId` is parsed and validated. Without this an attacker who knows another client's UUID could read that client's chat-analytics output (the function reads per-client `supabase_url`/`supabase_service_key` and calls OpenRouter on the caller's data). Frontend already passes the user JWT via `supabase.functions.invoke()` so legitimate callers are unaffected. Smoke-tested: 401 on missing auth header. **ARCH-016 (Medium) — schema:** `ALTER TABLE public.sms_delivery_events ADD CONSTRAINT sms_delivery_events_sid_status_unique UNIQUE (twilio_message_sid, status);` (no existing duplicates per pre-flight check). `frontend/supabase/functions/twilio-status-webhook/index.ts` insert call switched to `.upsert({...}, {onConflict: "twilio_message_sid,status"})` so Twilio's retry-after-transient-failure pattern can no longer create duplicate delivery rows. Both functions deployed. | **Schema rollback:** `ALTER TABLE public.sms_delivery_events DROP CONSTRAINT sms_delivery_events_sid_status_unique;`. **Code rollback:** `git revert phase-night-audit-batch2..HEAD`, then redeploy `analyze-chat-history` + `twilio-status-webhook`. |
| 2026-05-01 | night-audit-batch1 — RLS deny-all + FK indexes + lovable-tagger removal | `1ad930d` | `phase-night-audit-batch1` | **Schema (bfd-platform Supabase, Management API):** added explicit `deny_all_authenticated` RESTRICTIVE policies on `public.message_queue`, `public.active_trigger_runs`, `public.openrouter_usage`, `public.probe_results` (closes audit `RLS-001` — these tables had RLS enabled but no policies; service-role bypass still works, the deny_all just makes intent explicit and fail-safe). Added `idx_clients_agency_id`, `idx_profiles_agency_id`, `idx_profiles_client_id` indexes (closes audit `IDX-001/002/003` — these columns sit on the RLS hot path for nearly every authenticated query). **Code:** removed `lovable-tagger` dev dep from `frontend/package.json` + `frontend/package-lock.json`; removed `componentTagger` import + plugin invocation from `frontend/vite.config.ts` (closes audit `ARCH-011/012` and `User Todos.md` Phase E2). Deleted `.lovable/` directory at repo root (orphan artifact). Frontend typecheck still clean. | **Schema rollback:** `BEGIN; DROP POLICY deny_all_authenticated ON public.message_queue; DROP POLICY deny_all_authenticated ON public.active_trigger_runs; DROP POLICY deny_all_authenticated ON public.openrouter_usage; DROP POLICY deny_all_authenticated ON public.probe_results; DROP INDEX idx_clients_agency_id; DROP INDEX idx_profiles_agency_id; DROP INDEX idx_profiles_client_id; COMMIT;`. **Code rollback:** `git revert phase-night-audit-batch1..HEAD`. |
| 2026-05-01 | night-F3-fix — Conversation-history server-side proxy | `c2978f3` | `phase-night-F3-conv-history-fix` | Root cause confirmed via the F3-diag deploy: browser was rejecting `Forbidden use of secret API key in browser` because the new sb_secret_*/sb_publishable_* key system (post 2026-04-30 rotation) refuses service-role keys in browser contexts. The frontend had been creating a per-client Supabase client with `clients.supabase_service_key` directly. **Fix:** new `frontend/supabase/functions/get-chat-history/index.ts` edge function (NEW). JWT-gated (Supabase edge gateway + assertClientAccess pattern from retell-proxy at `retell-proxy/index.ts:77-99`); reads per-client `supabase_url`+`supabase_service_key` server-side; constructs the per-client Supabase client server-side; runs the same paginated chat_history query the frontend was running; supports the alt-session-id fallback path; returns rows. **Frontend (`frontend/src/components/contacts/ContactConversationHistory.tsx`):** removed `createClient` import + `externalHistoryClient` useMemo; `fetchMessages` now calls `supabase.functions.invoke('get-chat-history', {body: {clientId, sessionId, altSessionId}})` instead of an in-browser per-client client. The `supabaseUrl`/`supabaseServiceKey` props are still received (other parts of `Chats.tsx` may use them) but no longer used here. Deployed `get-chat-history` to BFD platform Supabase. Gateway returns 401 on unauthenticated calls as expected. | `git revert phase-night-F3-conv-history-fix..HEAD` then `supabase functions delete get-chat-history --project-ref bjgrgbgykvjrsuwwruoh`. The browser will go back to the diagnostic-but-broken state from F3-diag. |
| 2026-05-01 | night-F3 — Conversation-history error diagnostics | `7a4457a` | `phase-night-F3-conv-history-diag` | `frontend/src/components/contacts/ContactConversationHistory.tsx` (catch block at the chat_history fetch path now exposes the underlying error message in the UI instead of the generic "Failed to load conversation history" string, AND inserts a row into platform `error_logs` with `source='frontend.ContactConversationHistory.fetchMessages'` so the failure is server-visible. **Root-cause investigation note:** read-only diagnosis confirmed BFD's per-client Supabase URL+key are valid (`https://qildpilxjodxdifggmto.supabase.co`, sb_secret_* 41-char), `chat_history` has 13 rows for BFD's only-real-lead session_id `9jvV1kqmQemdgNTpWb7n`, `@supabase/supabase-js@2.98` createClient against this URL+key returns the rows successfully from Node, and `getCanonicalLeadId` returns the same 20-char GHL contact id used as the chat_history session_id. Network/auth/RLS/session_id format are all clean. The error is therefore happening at the post-fetch processing stage in the browser — only visible with the actual `err.message`. After this commit deploys, refreshing BFD `Chats` will surface the real underlying error string, which the next session can use to apply the targeted fix. Three suspect spots to check first: `executionWindows` date parsing on `dm_executions` rows (line 441-448), `parseRawRows` on chat_history rows that don't match the expected `{type, content}` shape (line 314-364), the message-merging dedup loop (line 565+).) | `git revert phase-night-F3-conv-history-diag..HEAD` |
| 2026-05-01 | night-F2 — `lookup_contact` Retell tool | `1c1cc34` | `phase-night-F2-lookup-tool` | `frontend/supabase/functions/voice-booking-tools/index.ts` (NEW `toolLookupContact` + dispatch case for `lookup-contact` / `lookup_contact`. Phone-first GHL lookup, never creates a contact, enriches with `recent_bookings` from local `bookings` table, latest `engagement_executions` row, and `leads.last_message_preview`. Deployed to BFD platform Supabase). `scripts/install-lookup-contact-tool.mjs` (NEW one-shot REST patcher; snapshots BFD's Retell agents+LLMs to `Operations/handoffs/<date>-bfd-retell-pre-F2-backup.json`, idempotently appends `lookup_contact` tool to each LLM's `general_tools`, republishes agent. Patches `--include-all-agents` aside, only the inbound slot today since outbound + outbound_followup slot columns are NULL on BFD's clients row — Brendan can re-run after assigning them in the UI). Smoke-test: `POST /voice-booking-tools?tool=lookup-contact` with a fake phone returned `{match_quality: "none", contact: null, ...}` 200 OK. Tool description tells the LLM to call this near the start of every call. | `git revert phase-night-F2-lookup-tool..HEAD` then redeploy `voice-booking-tools` (drops the dispatch branch). For Retell rollback: read `Operations/handoffs/2026-05-01-bfd-retell-pre-F2-backup.json`, restore the original `general_tools` array per LLM via `PATCH /update-retell-llm/{id}`, then `POST /publish-agent/{agent_id}`. |
| 2026-05-01 | night-F1 — Setter display names (voice + text) | `b062c6b` | `phase-night-F1-setter-names` | `clients` schema (`ALTER TABLE clients ADD COLUMN setter_display_names jsonb DEFAULT '{}'`); `frontend/src/lib/setterLabels.ts` (NEW helper); `frontend/src/components/setters/SetterDisplayNamesCard.tsx` (NEW reusable rename card); `frontend/src/hooks/useClientCredentials.ts` (added `setter_display_names` field + widened mutation types to accept `Record<string, unknown>` for jsonb); `frontend/src/pages/VoiceAIRepConfiguration.tsx` (renders the card on the Agents tab with 4 voice slots + Inbound/Outbound/Followup hints); `frontend/src/pages/TextAIRepConfiguration.tsx` (renders the card with 10 text slots above the setup-phase grid); `frontend/src/pages/Simulator.tsx` (`getSetterLabel` now reads `credentials.setter_display_names`, fallbacks unchanged); `frontend/src/pages/Logs.tsx` (`getResourceInfo` + `getSetterLabel` + followup-detail span all read the names map; legacy `Setter-N` / `Voice Setter N` literals replaced with helper calls); `frontend/src/pages/OutboundCallProcessing.tsx` (`call_history` row mapper uses helper for `setter_name`). Falls back to `Setter N` everywhere when slot is unset. Empty input clears the name. | `ALTER TABLE clients DROP COLUMN setter_display_names;` then `git revert phase-night-F1-setter-names..HEAD` |
| 2026-04-30 | 0 — Docs scaffold | `0967238` | `phase-0-docs` | `Docs/*` (7 new) | `git revert phase-0-docs..HEAD` |
| 2026-04-30 | 6 — Bidirectional GHL sync | `68237a5` | `phase-6-bidi-sync` | `frontend/supabase/functions/push-contact-to-ghl/index.ts:33` (field id), `frontend/supabase/functions/sync-ghl-contact/index.ts` (echo guard), `n8n/exports/Text_Engine_REVERSE_ENGINEERED.md` (NEW) | `git revert phase-6-bidi-sync..HEAD` then redeploy push-contact-to-ghl + sync-ghl-contact |
| 2026-04-30 | 1 — Text Engine port | `0e76001` | `phase-1-text-engine-port` | `trigger/processSetterReply.ts` (NEW), `trigger/processMessages.ts` (branch on `client.use_native_text_engine`) | `git revert phase-1-text-engine-port..HEAD` then `npx trigger.dev deploy --env prod` |
| 2026-04-30 | 7a — Tracking schema | `22b61d5` | `phase-7a-tracking-schema` | `frontend/supabase/migrations/20260430120000_phase7a_tracking_schema.sql` (NEW). Tables: `lead_optouts`, `sms_delivery_events`, `cadence_metrics`, `bookings`. Cols on `clients`: `cadence_quiet_hours`, `intake_lead_secret`, `voicemail_audio_url`, `ghl_webhook_secret`, `unipile_webhook_secret`. RLS agency-scoped. | Migration is additive — manual revert via `DROP TABLE bookings, cadence_metrics, sms_delivery_events, lead_optouts; ALTER TABLE clients DROP COLUMN cadence_quiet_hours, DROP COLUMN intake_lead_secret, DROP COLUMN voicemail_audio_url, DROP COLUMN ghl_webhook_secret, DROP COLUMN unipile_webhook_secret;` |
| 2026-04-30 | 4a + 4c — STOP keyword + reply-detected cadence-end | `8809f1f` | `phase-4a-4c-stop-and-reply-end` | `frontend/supabase/functions/receive-twilio-sms/index.ts` (STOP/START regex + lead_optouts upsert + setter_stopped + endActiveCadences helper, reply-end after message_queue insert), `frontend/supabase/functions/receive-dm-webhook/index.ts` (column bug fix `lead_id`→`ghl_contact_id` on engagement_executions; canonical `inbound_reply` stop_reason). | `git revert phase-4a-4c..HEAD` then redeploy `receive-twilio-sms` + `receive-dm-webhook` |
| 2026-04-30 | 5 — intake-lead public endpoint | `f670942` | `phase-5-intake-lead` | `frontend/supabase/functions/intake-lead/index.ts` (NEW). Per-client shared-secret POST endpoint; find-or-create GHL contact; dual-write to platform.leads + client mirror; optional auto-enroll in cadence. | `git revert phase-5-intake-lead..HEAD` then `supabase functions delete intake-lead --project-ref bjgrgbgykvjrsuwwruoh` |
| 2026-04-30 | 4b + 4d — quiet hours + voicemail-drop | `70bb24b` | `phase-4b-4d-quiet-and-voicemail` | `trigger/runEngagement.ts` (quiet-hours guard before each engage / send_sms / send_whatsapp / phone_call node + per-lead TZ resolution from phone prefix; voicemail-drop branch via Twilio AMD `<Play>` bypassing Retell), `frontend/supabase/functions/_shared/business-hours.ts` (NEW shared helper), `frontend/supabase/functions/bulk-insert-leads/index.ts` (refactor to import shared helper). | `git revert phase-4b-4d..HEAD` then `npx trigger.dev deploy --env prod` + redeploy bulk-insert-leads |
| 2026-04-30 | 2 + 3 — voice-booking-tools + kb-ingest | `4b01011` | `phase-2-3-n8n-port-tools` | `frontend/supabase/functions/voice-booking-tools/index.ts` (NEW; tools: get-available-slots, book-appointments, get-contact-appointments, update-appointment, cancel-appointments; writes to bookings; ends cadence on book), `frontend/supabase/functions/kb-ingest/index.ts` (NEW). Brendan still must repoint Retell + ElevenLabs agent tool URLs to `/functions/v1/voice-booking-tools?tool=<tool>&clientId=<uuid>` for the cutover. | `git revert phase-2-3..HEAD` then `supabase functions delete voice-booking-tools kb-ingest --project-ref bjgrgbgykvjrsuwwruoh` |
| 2026-04-30 | 7b/c/d/e — Tracking funnel runtime | `d82c7a2` | `phase-7b-c-d-e-tracking-runtime` | `frontend/supabase/functions/twilio-status-webhook/index.ts` (NEW), `frontend/supabase/functions/bookings-webhook/index.ts` (NEW), `frontend/supabase/migrations/20260430130000_phase7d_cadence_funnel_view.sql` (NEW materialised view + refresh fn), `trigger/runEngagement.ts` (cadence_metrics buffer + writeCadenceMetrics on every exit + reply detection + voicemail/calls counters), `trigger/processMessages.ts` (StatusCallback param on outbound + outbound message_queue stamp), `frontend/supabase/functions/receive-twilio-sms/index.ts` (StatusCallback on compliance reply). | `git revert phase-7b-c-d-e..HEAD` then redeploy receive-twilio-sms + remove twilio-status-webhook + bookings-webhook + `DROP MATERIALIZED VIEW cadence_funnel; DROP FUNCTION refresh_cadence_funnel();` + `npx trigger.dev deploy --env prod` |
| 2026-04-30 | 8a/b/c — Webhook signature verification | `c9e1c38` | `phase-8-webhook-sigs` | `frontend/supabase/migrations/20260430140000_phase8_webhook_secrets.sql` (NEW: `clients.retell_webhook_secret`), `frontend/supabase/functions/receive-dm-webhook/index.ts` (GHL `x-wh-signature` HMAC-SHA256 hex when `clients.ghl_webhook_secret` is set), `frontend/supabase/functions/retell-call-analysis-webhook/index.ts` (Retell `x-retell-signature` when `clients.retell_webhook_secret` is set), `frontend/supabase/functions/unipile-webhook/index.ts` (Unipile `x-unipile-signature` when `clients.unipile_webhook_secret` is set). All three are backwards-compatible: skip verification when no secret configured. | `git revert phase-8-webhook-sigs..HEAD` then redeploy the three edge fns |
| 2026-04-30 | 11a — Cadence overrides schema + D-M5 + D-M3 | `949a37d` | `phase-11a-cadence-overrides-d-m5-d-m3` | `frontend/supabase/migrations/20260501000000_phase11a_cadence_overrides.sql` (NEW: `engagement_workflows.quiet_hours_override`, `is_new_leads_campaign`, `new_leads_tag`, `voicemail_config`; partial unique index for at-most-one new-leads campaign per client; `clients.ghl_last_synced_from_field_id` + BFD backfill); `frontend/supabase/functions/push-contact-to-ghl/index.ts` (D-M5 reads per-client field id, legacy fallback to BFD constant); `frontend/supabase/functions/sync-ghl-contact/index.ts` (echo-loop guard reads per-client field id); `trigger/refreshCadenceFunnel.ts` (NEW D-M3 hourly scheduled task running `refresh_cadence_funnel()`). Deployed via Trigger.dev v20260430.1 (8 tasks). User-facing docs: `User Todos.md`, `Docs/CLIENT_ONBOARDING_SOP.md`, `Docs/NEXT_SESSION_PROMPT.md`. | `git revert phase-11a..HEAD` then redeploy push-contact-to-ghl + sync-ghl-contact + `npx trigger.dev deploy --env prod`; manual SQL revert: `ALTER TABLE engagement_workflows DROP COLUMN quiet_hours_override, DROP COLUMN is_new_leads_campaign, DROP COLUMN new_leads_tag, DROP COLUMN voicemail_config; DROP INDEX engagement_workflows_one_new_leads_per_client; ALTER TABLE clients DROP COLUMN ghl_last_synced_from_field_id;` |
| 2026-04-30 | 11b — Cadence settings bar | `6644411` | `phase-11b-cadence-settings-bar` | `frontend/src/pages/Engagement.tsx` (new collapsible Cadence Settings bar between toolbar + canvas: Quiet Hours per-workflow override + Voicemail static/dynamic; reuses drip-schedule day-pill + time popover patterns; client default fetched from `clients.cadence_quiet_hours`; drip-node "Restrict to working hours" Switch disabled when override is on). | `git revert phase-11b-cadence-settings-bar..HEAD` |
| 2026-04-30 | 11c — NEW LEADS toggle | `159637a` | `phase-11c-new-leads-toggle` | `frontend/src/pages/Workflows.tsx` (Switch + tag-name Input on each campaign row; optimistic flip with server-side at-most-one enforcement via partial unique index; also writes `clients.auto_engagement_workflow_id` for legacy intake-lead path; `fetchAll` select extended). | `git revert phase-11c-new-leads-toggle..HEAD` |
| 2026-04-30 | 11d — runEngagement quiet-hours + voicemail + AMD removal + tag-removal | `648da16` | `phase-11d-runengagement-overrides` | `trigger/runEngagement.ts` (workflow select extended; quiet-hours fallback chain workflow > client > DEFAULT; voicemailConfig parsed and forwarded through placeOutboundCall; Twilio AMD voicemail-drop branch + placeVoicemailDrop + resolveVoicemailAudioUrl + escapeXml DELETED; EngageChannel.call_mode and voicemail_audio_url removed from type; removeNewLeadsTag helper called from writeCadenceMetrics on every terminal stop_reason); `trigger/placeOutboundCall.ts` (voicemail_config field added); `frontend/supabase/functions/make-retell-outbound-call/index.ts` (ensureVoicemailConfig + voicemailHashCache; PATCHes voicemail_option to api.retellai.com/update-agent/{agentId} before each /v2/create-phone-call); `Docs/CADENCE_DESIGN.md` (Phase 4d section rewritten as "Voicemail (Retell-native)"). Deployed via Trigger.dev v20260430.2. | `git revert phase-11d-runengagement-overrides..HEAD` then `npx trigger.dev deploy --env prod` + redeploy make-retell-outbound-call |
| 2026-04-30 | 11e — ghl-tag-webhook | `5ac5f12` | `phase-11e-ghl-tag-webhook` | `frontend/supabase/functions/ghl-tag-webhook/index.ts` (NEW; receives GHL ContactTagUpdate; resolves client by ghl_location_id; HMAC-SHA256 sig verification on raw body when `clients.ghl_webhook_secret` is set; finds workflow with `is_active && is_new_leads_campaign && new_leads_tag = ANY(addedTags||tags)`; idempotent — skips enrol if non-terminal `engagement_executions` already exists; calls Trigger.dev run-engagement); `Docs/RUNBOOK.md` (new "Tag-based auto-enrolment" subsection); `Docs/CLIENT_ONBOARDING_SOP.md` (§5.10 added). | `git revert phase-11e-ghl-tag-webhook..HEAD` then `supabase functions delete ghl-tag-webhook --project-ref bjgrgbgykvjrsuwwruoh` |
| 2026-04-30 | 11f — engage SMS via direct Twilio | `565acbb` | `phase-11f-engagement-twilio-direct` | `trigger/runEngagement.ts` (sendTwilioSmsAndStamp helper; engage-node SMS branch + legacy send_sms node refactored to direct Twilio Messages.create with StatusCallback to `/twilio-status-webhook` + outbound `message_queue` stamp; WhatsApp branch unchanged). Closes the funnel blind-spot where cadence SMS was not feeding `sms_delivery_events`. Deployed via Trigger.dev v20260430.2. | `git revert phase-11f-engagement-twilio-direct..HEAD` then `npx trigger.dev deploy --env prod` |
| 2026-04-30 | 11g — synthetic probe + probe_results | `c7e6b91` | `phase-11g-synthetic-probe` | `frontend/supabase/migrations/20260502000000_phase11g_probe_results.sql` (NEW table); `trigger/syntheticProbe.ts` (NEW hourly scheduled task: posts to intake-lead, polls engagement_executions, asserts outbound message_queue, cancels, writes probe_results, alerts on failure to PROBE_ALERT_WEBHOOK_URL); `Docs/RUNBOOK.md` ("Synthetic probe setup" SQL + env-var checklist). Deployed via Trigger.dev v20260430.2 (9 tasks). | `git revert phase-11g-synthetic-probe..HEAD` then `npx trigger.dev deploy --env prod` + manual `DROP TABLE probe_results;` |
| 2026-04-30 | 11h — onboard-client.mjs | `c591bd0` | `phase-11h-onboard-script` | `scripts/onboard-client.mjs` (NEW; Node ESM script automating SOP §4.1/§4.2/§4.3 — clients INSERT, GHL last_synced_from custom field create + capture, BFD default workflow clone; prints intake_lead_secret + follow-up checklist; --dry-run flag previews SQL + GHL POST without executing; uses Supabase Management API SQL endpoint and GHL REST API); `Docs/CLIENT_ONBOARDING_SOP.md` (§12 rewritten to point to the script). | `git revert phase-11h-onboard-script..HEAD` |
| 2026-04-30 | 11i — D-M1 native-vs-n8n diff harness | `1a8c216` | `phase-11i-diff-harness` | `scripts/native-vs-n8n-diff.mjs` (NEW; Node ESM. Picks N recent inbound message_queue rows for a client and renders a side-by-side markdown report of native processSetterReply output vs the legacy n8n text engine output. Path A replicates `processSetterReply.ts` in-script: fetches Setter-N system_prompt + chat_history from the client's external Supabase, calls OpenRouter with the same model/temperature, parses the `{messages:[...]}` shape. Path B POSTs to `clients.text_engine_webhook` with the same URL-encoded query params `processMessages.ts` uses, parses Message_1..N. Coarse Jaccard word-overlap hint. `--dry-run` previews picked messages without LLM/n8n calls. Run output: `Operations/handoffs/<date>-bfd-native-vs-n8n-diff.md`). | `git revert phase-11i-diff-harness..HEAD` |
| 2026-04-30 | 9-hotfix2 — SUPABASE_SERVICE_ROLE_KEY missing in Trigger.dev prod | `60753b9` | `phase-9-hotfix-trigger-env` | Trigger.dev runtime fix (no code change). Set `SUPABASE_SERVICE_ROLE_KEY = sb_secret_XFSZ9...REDACTED (full value retrievable from Supabase dashboard or `GET /v1/projects/{ref}/api-keys?reveal=true`)` in Trigger.dev project `proj_fdozaybvhgxnzopabtse` PROD env via API (`POST /api/v1/projects/{ref}/envvars/prod`). Root cause: when Trigger.dev was originally provisioned (~2026-04-24), only `SUPABASE_URL` + `TRIGGER_SECRET_KEY` were configured; `SUPABASE_SERVICE_ROLE_KEY` was never set. Every task with `getMainSupabase()` (processMessages, processSetterReply, runEngagement, sendFollowup, runAiJob, executeWorkflow, syntheticProbe, refreshCadenceFunnel) crashed at line 1 of run() with `Error: supabaseKey is required.` Why it didn't surface earlier: until phase 9 cutover, processMessages routed to the n8n branch (via `client.text_engine_webhook` HTTP fetch — never hit `getMainSupabase()` in a way that mattered, since the Supabase client was created up-front but only used for read-only `.from("clients")` lookups that... actually the throw was at task START so this should have always failed. Possible explanation: an older deployed version had hardcoded creds OR the Supabase SDK threw differently. Validation: BFD's "Hi you there?" SMS replay processed cleanly, native engine produced an Aussie-warm Brendan-toned reply ("Yep, still here! ..."), Twilio delivered to `+61405482446` at 2026-04-30 12:06:03 UTC. Used the new sb_secret_* key (per memory `project_session4_state` legacy JWT keys are disabled since 2026-04-29). | `DELETE /api/v1/projects/proj_fdozaybvhgxnzopabtse/envvars/prod/SUPABASE_SERVICE_ROLE_KEY` (would re-break native engine). Better: just leave it set. |
| 2026-04-30 | 9-hotfix — probe row collision blocking BFD inbound | `c59b572` | `phase-9-hotfix-probe-collision` | DB-only fix: `UPDATE clients SET retell_phone_1 = NULL WHERE id = 'b0e4f199-3fa5-4c8d-851b-6167ff46ad91'`. Root cause: phase-11g synthetic probe provisioning copied BFD's `retell_phone_1='+61481614530'` onto the probe client, creating two rows matching the same number. `receive-twilio-sms` does `.eq('retell_phone_1', toPhone).maybeSingle()`, which returns PGRST116 ("Results contain 2 rows") whenever 2+ match — function silently bails with 200 + empty TwiML at line 408-413. Symptom: BFD inbound SMS after the probe was provisioned (≥ 2026-04-30 09:25 UTC) never reached `message_queue`, no error_logs (the bail-path uses `console.warn` only). Diagnosed via Supabase analytics `function_logs` query (`PGRST116: Results contain 2 rows`). Fix verified: post-UPDATE, a valid-sig probe POST writes to message_queue normally. Probe outbound feature deferred — when Brendan enables the synthetic probe, the probe needs a DIFFERENT `retell_phone_1` (not BFD's number); this is a known follow-up. Code-defensive follow-up: change line 405 to add `.eq('dm_enabled', true)` AND `.order('created_at').limit(1).maybeSingle()` so future provisioning gotchas don't silently break inbound. | `UPDATE clients SET retell_phone_1 = '+61481614530' WHERE id='b0e4f199-…'` (only if you've simultaneously set up a different inbound number for BFD). |
| 2026-04-30 | 9 — BFD native cutover + B1 em-dash tweak | `a7b66f3` | `phase-9-bfd-native-cutover` | DB-only operations (no code change). 1) `UPDATE clients SET use_native_text_engine=true WHERE id='e467dabc-57ee-416c-8831-83ecd9c7c925'` — flips BFD's text engine to the in-house `processSetterReply.ts` path (n8n was returning HTTP 500 on every call; native produced 5/5 sensible replies in the phase-11i diff). 2) UPDATE `engagement_workflows.nodes` for workflow `40e8bea3-…` to remove em-dashes from n5 + n7 SMS copy (both top-level `message` field AND `channels[*].message`, the field actually rendered at runtime). Backup of pre-tweak nodes at `Operations/handoffs/2026-04-30-bfd-workflow-nodes-pre-b1-backup.json`. Workflow name DRAFT em-dash left intact (internal-only). 14-day soak window starts now; Phase 10 (n8n decommission) gated on clean soak. | `UPDATE clients SET use_native_text_engine=false WHERE id='e467dabc-…'` for the flag rollback (instant). For node rollback, restore from the pre-b1-backup JSON via UPDATE engagement_workflows. |
| 2026-05-01 | A3e — phone-first contact lookup + merge candidates table | `0f6e1ab` | `phase-A3e-phone-lookup-merge-candidates` | `frontend/supabase/functions/voice-booking-tools/index.ts` (dispatcher + resolveContactId rewrite); `frontend/supabase/migrations/20260501150000_phase_a3e_contact_merge_candidates.sql` (NEW table); 3x BFD Retell LLM patches (prompt addition + `previously_contacted` param on book/update tools). Surfaced by Brendan's third test call (`call_3d9c14dd…`): all 5 booking tools were now reachable post-A3d, but the agent looped on email transcription (Retell's STT mangled "B-R-E-N-D-A-N…" into hyphenated chunks then spaces) and never reached `book-appointments`. Brendan's design: use the caller's phone (already supplied by Twilio→Retell as `call.from_number`) for the GHL lookup, fall back to email; create new contact if no match; agent asks "have we spoken before?" and the answer flags the new contact for review. **Edge fn changes:** (a) Dispatcher reads `body.call.{direction,from_number,to_number,call_id}` and injects `body.phone` (caller side) + `body.source_call_id` so handlers see them flat. (b) `resolveContactId` rewritten to phone-first → email-fallback → create. Returns `{contactId, createdNew}`. When createdNew, inserts a row into `contact_merge_candidates` (best-effort, non-fatal). (c) `toolBookAppointments` passes supabase to enable logging. **DB:** `contact_merge_candidates` table with phone/email/name/caller_claims_prior_contact/source_call_id/status, agency-scoped RLS mirroring bookings. Partial index on `caller_claims_prior_contact=true AND status='pending'` for fast filtering of high-priority duplicates. **Retell:** all 3 BFD LLMs got an appended "CONTACT IDENTIFICATION (BFD-specific, post-A3e)" section instructing the agent to ask "have you spoken to BFD before?" once and pass `previously_contacted` to book/update tools. The new bool param added to both tool schemas. All 3 agents republished. Smoke-tested end-to-end: synthetic inbound body with `call.from_number=<random>` and no email/phone in args → edge fn injected phone → GHL search returned no match → created new contact → booking succeeded → merge-candidate row inserted with caller_claims_prior_contact=true. | `git revert phase-A3e..HEAD` then redeploy edge fn; manual SQL `DROP TABLE contact_merge_candidates;` if rolling back schema. Retell prompt revert: re-PATCH each LLM removing the appended section and the `previously_contacted` property (backup file at `/srv/bfd/Operations/handoffs/2026-05-01-bfd-retell-pre-A3-backup.json` is the pre-A3 baseline; for A3e specifically, just slice off the last 999 chars of `general_prompt` per LLM). |
| 2026-05-01 | A3d — voice-booking-tools parity with canonical n8n workflow | `b212319` | `phase-A3d-canonical-n8n-parity` | `frontend/supabase/functions/voice-booking-tools/index.ts` (3 changes). Brendan supplied the canonical 1prompt-os n8n booking workflow JSON (`Appointment_Booking_Functions.json`) + the Retell agent template (`Voice-Setter-1.json`). Cross-checked against our edge function, found the **real bug** that was still breaking real Retell calls even after A3c: Retell sends custom-tool calls as `{call:{...}, name:"<tool>", args:{...}}` (per `args_at_root: false` — BFD's default and the canonical pattern). Our handlers were reading params at the top-level `body.X`, so on real calls every param was `undefined` while smoke tests with flat bodies passed. **Fixes:** (a) Dispatcher now unwraps `body.args` when present so every handler reads params at top level. (b) `resolveContactId` rewritten to mirror n8n's `getContact` pattern: search first via `GET /contacts/?locationId=…&limit=1&query=<email>`, then create only if not found. Search-first avoids dupe risk and matches the canonical flow. (c) `toolBookAppointments` body now includes `meetingLocationType: "default"`, `ignoreDateRange: false`, `toNotify: true`, `ignoreFreeSlotValidation: false` to match the n8n workflow's bookAppointment node payload exactly. Smoke-tested all 5 tools end-to-end with both wrapped (Retell) and flat (direct curl) bodies — full create/book/cancel cycle green. Deployed via `npx supabase functions deploy voice-booking-tools --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. | `git revert phase-A3d..HEAD` then redeploy: `cd frontend && npx supabase functions deploy voice-booking-tools --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. |
| 2026-05-01 | A3c — voice-booking-tools edge function fixes (param names + email-based contact resolve) | `f576d49` | `phase-A3c-voice-booking-edge-fixes` | `frontend/supabase/functions/voice-booking-tools/index.ts` (5 changes). Surfaced by Brendan's second test call (`call_2f64ec1e…`): the inbound LLM now had the booking tools (post-A3b) and successfully called `get-available-slots` twice, but BOTH calls returned HTTP 400 from the edge function: "startDate and endDate query params required". Two bugs: (1) `toolGetAvailableSlots` only read URL search params; Retell sends args in the request BODY. (2) Param NAMES — Retell tool schemas use `startDateTime`/`endDateTime`/`email`/`eventId`/`timeZone`; edge function expected `startDate`/`endDate`/`contactId`/`appointmentId`/`timezone`. Plus a third issue surfaced via smoke test: `cancel-appointments` DELETE returns 401 ("This route is not yet supported by the IAM Service") on BFD's current GHL PIT — soft-cancel via PUT works without the IAM update. **Fixes:** (a) `toolGetAvailableSlots` rewritten to accept body OR url, normalises `startDateTime`/`startDate`/`timeZone`/`timezone`, converts ISO to ms epoch (GHL requires ms). (b) New `resolveContactId(client, body, createIfMissing)` helper — finds-or-creates GHL contact by `email` per memory `reference_ghl_contact_create_duplicate`. (c) `toolBookAppointments` now takes `email`, defaults `endDateTime` to start+30min if missing, derives a `firstName` from email-prefix when none supplied. (d) `toolUpdateAppointment` + `toolCancelAppointments` accept `eventId` as alias for `appointmentId`. (e) `toolCancelAppointments` switched from DELETE to PUT-with-`appointmentStatus=cancelled` (GHL-recommended soft-cancel; works without IAM scope upgrade). Smoke-tested all 5 tools end-to-end with the same args Retell would send: created appointment `dcSPQLbK4WjOJhifTZCE` for synthetic contact `z0dbDpOpPBz1A0tA8mAB`, soft-cancelled via update-appointment, all green. Deployed to BFD platform Supabase via `npx supabase functions deploy voice-booking-tools --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. | `git revert phase-A3c-voice-booking-edge-fixes..HEAD` then redeploy: `cd frontend && npx supabase functions deploy voice-booking-tools --project-ref bjgrgbgykvjrsuwwruoh --no-verify-jwt`. |
| 2026-05-01 | A3b — Extend booking tools to inbound + outbound_followup LLMs (BFD) | `2ccb794` | `phase-A3b-tools-on-all-agents` | Retell config only (no code change). Closes the gap surfaced by Brendan's first test booking call: he called BFD's inbound number, the inbound agent (`agent_5ec5eb…`, LLM `llm_22e795de…`) had only `end_call` and stalled / fabricated a booking confirmation. Now: all 3 BFD LLMs have an identical 6-tool set (`end_call` + 5 booking tools). PATCHed `llm_22e795de19b4d25cb579013586be` (inbound) and `llm_1807516860b71c5246a6b8cbe6cb` (outbound_followup) with the same booking-tools array we patched on the outbound LLM in phase-A3 — verbatim copy of url=`/functions/v1/voice-booking-tools`, query_params={tool, clientId=`e467dabc-…`}, headers={Authorization: Bearer <intake_lead_secret>}. Published `agent_5ec5eb129f3165cfa07b581a1a` and `agent_6e763a7153706815cbf1cf511a`. Readback verified all 3 agents now expose identical booking tool config. Same pre-patch backup file at `/srv/bfd/Operations/handoffs/2026-05-01-bfd-retell-pre-A3-backup.json` (it already captured all 3 LLMs before any patch). | For each of inbound + outbound_followup, restore `general_tools = [<end_call only>]` from backup file's `agents.<slot>.llm.general_tools`, PATCH `/update-retell-llm/{llm_id}` with that single-tool array, then `POST /publish-agent/{agent_id}`. |
| 2026-05-01 | A3 — Retell voice-tool URL repoint (BFD) | `2f0e026` | `phase-A3-retell-tool-repoint` | Retell config + DB-only (no code change in this repo). 1) Minted `clients.intake_lead_secret` for BFD via `UPDATE clients SET intake_lead_secret = encode(gen_random_bytes(24),'base64') WHERE id='e467dabc-…' AND intake_lead_secret IS NULL`. 2) Surveyed BFD's three Retell agent slots: `retell_inbound_agent_id` → LLM `llm_22e795de…` (only `end_call`, no booking tools), `retell_outbound_followup_agent_id` → LLM `llm_1807516860…` (only `end_call`), `retell_outbound_agent_id` → LLM `llm_692b220d…` (5 booking tools — the only one with anything to repoint). 3) PATCHed `llm_692b220d…` via `PATCH https://api.retellai.com/update-retell-llm/{llm_id}`, swapping each booking tool's `url` from `https://n8n-1prompt.99players.com/webhook/e4cffeea-…` to `https://bjgrgbgykvjrsuwwruoh.supabase.co/functions/v1/voice-booking-tools`, replacing each tool's `query_params` from `{"function-type": "<name>"}` to `{"tool":"<name>","clientId":"e467dabc-…"}`, and adding `headers: {"Authorization":"Bearer <intake_lead_secret>"}` (REDACTED) on each. Tools touched: `get-available-slots`, `book-appointments`, `get-contact-appointments`, `update-appointment`, `cancel-appointments`. 4) `POST /publish-agent/agent_1eaa0ffbb4c5bfe1f55b0457a4` (HTTP 200) to push the patched LLM live on the outbound agent. Inbound + outbound_followup agents not republished (their LLMs unchanged). 5) Readback verified all 5 tools have correct url + query_params + bearer header. Pre-patch backup of all 3 agents + LLMs at `/srv/bfd/Operations/handoffs/2026-05-01-bfd-retell-pre-A3-backup.json` (outside repo). Brendan still must run ONE live booking call to confirm a `bookings` row writes with `source='voice-booking-tools'` and the cadence ends with `stop_reason='booking_created'`. | For each booking tool in the backup JSON's `outbound.llm.general_tools`, restore original `url`, original `query_params` (`{"function-type":"<name>"}`), and `headers: {}`. PATCH `/update-retell-llm/llm_692b220d2af38656252df9018002` with the restored `general_tools` array, then `POST /publish-agent/agent_1eaa0ffbb4c5bfe1f55b0457a4`. Optional: clear secret with `UPDATE clients SET intake_lead_secret = NULL WHERE id='e467dabc-…'` to revert to no-auth on voice-booking-tools (it skips bearer check when secret is NULL). |

## Format for future entries

```
| YYYY-MM-DD | <phase> — <slug> | <sha> | `phase-N-<slug>` | <comma-separated files> | `git revert phase-N..HEAD` |
```

## Detailed notes (per phase)

### Phase 6 — Bidirectional GHL sync complete (2026-04-30)

**Why now (out of phase order):** Brendan asked to action the two pre-flight items from the master plan that needed his login. The GHL custom field can be created via API (this session has BFD's PIT). The n8n export needs Brendan's n8n login, so I reverse-engineered the I/O contract from the only caller in our codebase (`processMessages.ts`) instead. Phase 1 can now proceed in the new session without blocking on the actual n8n JSON export.

**GHL custom field `last_synced_from`** created via `POST /locations/xo0XjmenBBJxJgSnAdyM/customFields` (BFD location). Field id `PQNTqtTnIw9Uu0XLLE5M`, fieldKey `contact.last_synced_from`, dataType `TEXT`. Created on BFD's account; for non-BFD clients, recreate per-location at onboarding.

**Code changes:**
- `frontend/supabase/functions/push-contact-to-ghl/index.ts:33` — `BFD_LAST_SYNCED_FROM_FIELD_ID = "PQNTqtTnIw9Uu0XLLE5M"`. Every outbound `PUT /contacts/{id}` now stamps the field.
- `frontend/supabase/functions/sync-ghl-contact/index.ts` — added echo-loop guard: if the inbound contact webhook contains `customField.last_synced_from = "1prompt-os"` AND `leads.updated_at` is < 60s old, return early with `status: "skipped_echo"`. Logs the skip in `sync_ghl_executions` for observability.

**Deploys:**
- `push-contact-to-ghl` → v2
- `sync-ghl-contact` → v8

**n8n export equivalent:** `n8n/exports/Text_Engine_REVERSE_ENGINEERED.md` — full I/O contract from reading `processMessages.ts:204-282`. Phase 1 can build `processSetterReply.ts` against this. Brendan should still export the live JSON when convenient for the LLM prompt internals.

**Rollback:**
- `git revert phase-6-bidi-sync..HEAD`
- Redeploy push-contact-to-ghl + sync-ghl-contact
- (Optional) the GHL custom field can stay — harmless if unused

### Phase 0 — Docs scaffold (2026-04-30)

**Files:**
- `Docs/MASTER_PLAN.md` — mirror of canonical plan file
- `Docs/ARCHITECTURE.md` — component map + 4 mermaid sequence diagrams
- `Docs/CADENCE_DESIGN.md` — engagement engine spec, state machine, node types, Phase 4a-4d details
- `Docs/TRACKING.md` — funnel definition + table schemas + sample SQL queries
- `Docs/RUNBOOK.md` — deploys, rollback, incident playbooks, GHL config
- `Docs/CHANGES_LOG.md` — this file
- `Docs/FUTURE.md` — out-of-scope items including the appointment reminder GHL campaign

**Notes:**
- All 7 docs in repo to be readable from any session
- The canonical plan file lives at `C:\Users\brend\.claude\plans\resuming-1prompt-os-work-read-reactive-puffin.md` and `Docs/MASTER_PLAN.md` is its in-repo mirror

**Rollback:**
- `git revert phase-0-docs..HEAD && git push origin main` — purely additive, low risk
