# BFD-Setter — Bug / Issue List (canonical, OPEN only)

Open bugs and behavior fixes. Reconciled 2026-06-25 with Brendan (one-by-one triage of all lists).

- **Status:** `[ ]` open · `[~]` partially done · `[B]` needs a Brendan input · `[x]` done (moved to archive)
- **Companion lists:** features → `FEATURE_ROADMAP.md` · your manual actions → `BRENDAN_TODO.md` · things to verify → `TEST_LIST.md` · someday/gated → `DEFERRED.md` · closed items → `Docs/archive/COMPLETED_LOG.md`
- **Rule:** when a bug is fixed + verified, move it out of here (to `TEST_LIST.md` if it needs live verification, else to `COMPLETED_LOG.md`).
- All items below are **CODE** (Claude builds) unless tagged `[B]`.

---

> **Session 7.5 + F8 — MERGED to main + DEPLOYED LIVE 2026-07-01** (overnight; handoff `Operations/handoffs/2026-07-01-f8-plus-7.5-deploy.md`). The `[~]` bugs below are now **deployed, awaiting BEHAVIORAL live-verify** (entries in `TEST_LIST.md`; consolidated test matrix in the handoff), NOT staged. Deployed: Trigger 20260630.1 (SMS-OBS-1/BOOK-1 code/MODEL-1a), `tool_invocations` + `client_pricing_config` migrations, `execute-lead-webhook` v1, `get-blended-rate` v1, frontend (Railway), **retell-proxy v47 (VM-1)** — read-only Voice smoke passed (0 agents mutated); the behavioral answered-call Voice-regression is the one piece still owed → Session 7-finish. F8 trap proof 9/9 live. _Per-bug disposition (now deployed):_ **SMS-OBS-1** `[~]` (new `tool_invocations` table migration FILE + persist; Trigger.dev) · **BOOK-1** `[~]` code (prefetch+inject ground-truth + anti-fabrication; Trigger.dev) + prompt half still `[B]` report-only · **MODEL-1-HARDENING** `[~]` (MODEL-1a alias map + slash-sanity; Trigger.dev; MODEL-1b demoted — save UI is a known-IDs dropdown) · **F9-1** `[~]` (frontend build) · **VM-1** `[~]` (retell-proxy v46→v47, **Voice-regression GATED**) · **PHONE-CLEAR-1** `[~]` (frontend build) · **G3-8(a)** `[~]` (new `execute-lead-webhook` edge fn + LeadRow repoint; in-project re-test) · **BOOK-2/BOOK-3** stay `[ ]` shared-fn defer (a characterization test was added on the branch; the actual edit is a supervised daytime session) · **API-DEPR-1** + **G3-7** stay `[ ]` DEFER (their own sessions). test:node 80/0, test:edge 125/0, vite build green; adversarial review = DONE-CONFIRMED. Nothing pushed/merged/deployed.

---

## 🔴 High — core behavior

- [~] **BOOK-1 (found in Session 7 phone-half live TEST, 2026-06-30) — the SMS setter FABRICATES "booked out / snapped up" and never books, against an OPEN calendar.** A lead texted "can I book a meeting?", the setter offered real times (Thu 2pm/4pm), then on acceptance replied "that just got snapped up, mate" and **never called book-appointments** — no booking was created. **Proven NOT an API/calendar issue (read-only):** a direct live GHL `/calendars/2p9eg0Qv7QoKknk1Sp2d/free-slots` query (live `clients.ghl_api_key`, tz Australia/Sydney, Version 2021-04-15, HTTP 200) returned **180 free slots / 11 business days**; every time the setter called taken (Jul 1 11:00, Jul 2 14:00, Jul 2 16:00) is **free**. The 4 "slots-full" `dm_executions` completed with `has_error=false`, zero `error_logs`, `bookings` empty; the "booked out"/"snapped up" wording exists nowhere in code (the only code-emitted unavailability string is `buildSlotUnavailable()` "That time isn't available…", which never appeared) → the model **invented** it. **Root structural cause:** unlike the Voice setter (which PREFETCHES availability into a Retell dynamic var so the model sees open times before it speaks), the Text setter depends on the model voluntarily calling `get-available-slots`. **Fix = the OVERNIGHT TEXT-SETTER REPAIR SESSION** (see handoff `2026-06-30-session7-test-pass-phone-half.md`): structural CODE levers (prefetch + inject ground-truth open slots every reply; anti-fabrication guard forcing a `get-available-slots` turn before any "unavailable" claim; tighten `setterTools.ts` `TOOL_USAGE_INSTRUCTION`; tool_choice) + a **report-only** prompt recommendation for Brendan (text_prompts is report-only). Severity High (booking is the product; blocks 3.12). **3.12 SMS booking stays in TEST_LIST, blocked on BOOK-1.** Related latent shared-fn defects: **BOOK-2 / BOOK-3** (Low). Diagnosis observability gap: **SMS-OBS-1** (Medium).

## 🟠 Medium

- [~] **SMS-OBS-1 (found in Session 7 phone-half, 2026-06-30) — Text-engine tool calls/responses are not persisted → booking failures are DB-blind.** `processSetterReply.ts:344` hardcodes `tool_calls:[]` into `chat_history`; `loopResult.toolInvocations` (populated in `setterToolLoop.ts`) is never written to any DB table; `request_logs` is empty on this path. Diagnosing BOOK-1 required a live GHL query + the LLM transcript because the DB could not show whether `get-available-slots` / `book-appointments` even fired or what GHL returned. **Fix:** persist tool invocations (name/args/result) to a DB table. This is the **first deliverable of the overnight Text-Setter session** (highest-value, lowest-risk, Text-only, additive). Severity Medium.
- [~] **MODEL-1-HARDENING (found in Session 7 phone-half, 2026-06-30; the live misconfig itself FIXED → COMPLETED_LOG) — the free-text `clients.llm_model` field can silently break ALL SMS + cadence AI.** BFD's `clients.llm_model` was set to `google/gemini-flash-latest` — **not a valid OpenRouter model id** — and `trigger/_shared/llmModel.ts` `normalizeLlmModel` only strips a leading `~`/whitespace, so the invalid-but-non-empty value passed straight through (the `|| "google/gemini-2.5-flash/pro"` fallbacks never fire on a truthy string) → every `llm_model`-driven engine (processSetterReply / nudgeColdReply / sendFollowup / runAiJob / aiGenerateEngagementCopy) 400'd. **Live value corrected to `google/gemini-2.5-flash` via Mgmt API (Brendan's choice).** **Hardening fix:** validate the model against OpenRouter's model list (or map known aliases like `gemini-flash-latest`) at UI save time and/or in `normalizeLlmModel`, so an invalid model can't silently break the engines. Severity Medium.

- [~] **F9-1 (found in Session 7 TEST, 2026-06-28) — Retell lock leaks the tile display-name rename + misreports it as saved.** With a voice setter **Retell-locked**, the inline tile name editor still lets you rename it and reports success. Root cause: `InlineSetterNameEditor.commitEdit` (`frontend/src/components/setters/InlineSetterNameEditor.tsx`) writes `clients.setter_display_names` **unconditionally first**, *then* calls the guarded `retell-proxy set-agent-name`. The server **correctly refuses** the Retell write (HTTP 423 `setter_retell_locked` → live Retell `agent_name` **and** `voice_setters.name` stay unchanged — core lock safety holds, no Retell corruption), but the display-name map already persisted, so the locked tile shows the new name and the toast is a soft *"Setter name saved (Retell push warning)"* instead of a clean "Retell-locked" refusal. **Repro (live-confirmed):** lock slot 4 → click tile name heading → "Gary - Property Coach LOCKTEST" → Enter → `setter_display_names.voice-4` changed; `voice_setters.name` + Retell `agent_name` unchanged. **Expected (F9 contract):** the rename is refused with a clear "Retell-locked — unlock to rename" message and **no** display-name write. **Fix direction:** the tile already has the lock state (`isVoiceSlotLocked(slotId)` / `lockMap` at `PromptManagement.tsx:8168`) — pass it into `InlineSetterNameEditor` and gate `commitEdit` (block + error toast, skip the `setter_display_names` write); and/or reorder so the guarded `set-agent-name` runs first and the display-name write only persists on success, and surface the 423 body as an **error** (not a warning). Severity Medium (UX + tile/Retell name mismatch; no data-safety breach). Note: `setter_display_names.voice-4` was reverted to "Gary - Property Coach" during the test to keep the live tile correct.

- [ ] **VM-1 (found in Session 7 TEST, 2026-06-28; v47 fix INSUFFICIENT — behavioral verify FAILED 2026-07-03) — `set-voicemail` client-wide push reports "partial" and never lands.** Clicking **Save & push** on the client Voicemail card shows *"Push partial: voicemail_set"* — that is the `success:false` branch (`ClientVoicemailCard.tsx:150`), i.e. ≥1 **unlocked** agent's PATCH returned `ok:false`. Root cause (read-only evidence): `retell-proxy` `set-voicemail` (`index.ts:1573-1586`) PATCHes `update-agent` with **`enable_voicemail_detection` + `voicemail_detection_timeout_ms`**, but the live Retell agent schema **no longer has those fields** (`has("enable_voicemail_detection")` → `false`; only `voicemail_option` exists) → Retell rejects the PATCH; nothing lands (all 6 agents' `last_modification_timestamp` unchanged, `enable_voicemail_detection` null). It also does a **raw `update-agent` PATCH without `ensureEditableAgentDraft`** (every other write handler draft-first). Impact: the existing `voicemail_option:{hangup}` is already on the agents (set earlier), so *hangup* still works, but **no voicemail change can be pushed** — a `static`/`prompt` voicemail message would never apply, and every push shows a scary "partial". **Pre-existing (not F9)** — F9 only added the lock-skip loop. **Fix:** drop the two deprecated fields from `patchBody` (map the intended behavior onto the current `voicemail_option` schema), and route through `ensureEditableAgentDraft` like the other handlers. Confirm the exact Retell error from the `results[].error` in the `set-voicemail` response. Severity Medium. **⚠️ UPDATE — behavioral verify FAILED 2026-07-03 (Session 7-finish voice gate); v47 was necessary but NOT sufficient.** Live re-test: Brendan set `clients.voicemail_config` mode=`prompt` (non-empty text) + Save & push → still **"Push partial"**; read-only re-check of all 5 push-target agents (`agent_b2f6495` shared Main-Outbound/Inbound + the 4 Garys) shows `voicemail_option` UNCHANGED (`{action:{type:"hangup"}}`, `last_modification_timestamp` still = the A2 re-save) → the push landed on **0/5** agents. v47 correctly dropped the deprecated `enable_voicemail_detection`/`_timeout_ms` fields and the emitted body is a **valid** `{voicemail_option:{action:{type:"prompt",text}}}` (Retell OpenAPI confirms the enum is `prompt` / `static_text` / `hangup` / `bridge_transfer`, and `prompt` is what the code sends), so the enum is NOT the blocker for prompt mode. The remaining blocker is the **raw `update-agent` PATCH with NO `ensureEditableAgentDraft`** (`retell-proxy/index.ts` set-voicemail handler): Retell published versions are immutable (the v39 fix reason; every other write handler goes draft→edit→publish→repoint), so this raw PATCH never sticks. **Refined fix (Session 9 fold-in or a dedicated fix-pass; Voice-gated): (1)** route `set-voicemail` through `ensureEditableAgentDraft` like the other write handlers; **(2)** fix the latent `static` enum — `voicemail.ts:34` emits `type:"static"` but Retell requires **`static_text`** (`prompt` + `hangup` are already correct). Does NOT gate v47 — the calling path (booking + B-3 + B-5) all PASSED the same 2026-07-03 gate, so v47 stays live.

- [ ] **API-DEPR-1 (raised by Brendan 2026-06-28, Session 7) — Retell + GHL API deprecation / version review (CODE; dedicated session).** Brendan has received Retell's **legacy-list-endpoints deprecation notice twice** (https://docs.retellai.com/deprecation-notice/2026/06-15_legacy_list_endpoints). Read-only scan confirms a **mixed/partial migration state**: `make-retell-outbound-call` already uses `/v2/list-phone-numbers`, but **`retell-proxy` still calls the bare legacy list endpoints** — `list-agents` (1270-71), `list-calls` (1415-17), `list-phone-numbers` (1387-88), `/list-retell-llms` (1297), `list-knowledge-bases` (1337-38), `list-voices` (919/1152/1431-32) — and `verify-credentials` uses `/list-agents`, `elevenlabs-manage-agent` uses `list-voices`. The 2026-06-09 migration (`b835675`, list-llms/phone→v2, list-calls→v3) was **incomplete**, leaving these legacy GETs as the deprecation trigger. **Scope of the review (Brendan's ask):** (1) go through **every** notice in https://docs.retellai.com/deprecation-notice/overview and migrate **all** Retell calls (list + any other deprecated endpoints/params) to the current endpoints/versions; (2) **also review GHL LeadConnector API** — the codebase mixes `Version` headers `2021-04-15` (×11), `2021-07-28` (×10), `2022-11-28` (×2); confirm each against GHL's current supported versions and align. Verify read-only against the live APIs, deploy the touched edge fns, re-confirm no deprecation notice fires. Severity Medium (deprecated endpoints get removed → future hard breakage; actively firing now).

## 🟢 Low — hardening / cleanup

- [ ] **SMS-METER-1 (found by the 2026-07-02 completion council; SHARED-FN, fold into the BOOK-2/3 supervised session) — the voice agent's mid-call `send_sms` texts are invisible to F13 usage metering.** `voice-booking-tools/index.ts` `toolSendSms` (~line 913-987) sends a real lead-facing SMS via Twilio direct and writes only the external `chat_history` + GHL mirror — it never inserts the platform `message_queue` `channel='sms_outbound'` row that `get-client-usage` counts, so every mid-call text undercounts the client's "Texts sent", the SMS billed line, and overstates margin. The tool is force-injected into every voice agent (retell-proxy ~line 585), so this fires in normal operation. **Fix (supervised session only — `voice-booking-tools` is the FROZEN shared Voice baseline, same constraint as BOOK-2/3):** add the same post-send non-fatal `message_queue` stamp the other writers use (`ghl_account_id = client.ghl_location_id || client.id`, `channel 'sms_outbound'`, `twilio_message_sid`). The sibling gap on the Bug-28 booking-confirmation SMS (`retell-call-analysis-webhook`, NOT frozen) was fixed on the F13/F14 branch. Related decision for Brendan: demo-page texts via `twilio-send-sms` are also unstamped — arguably not client-billable; confirm intent. Severity Low today (dogfood volume), Medium at first client. Effort S.
- [x] **AUTH-LEN-1 (found in the 2026-07-02 full re-audit) — password min-length mismatch: frontend validated 6 chars vs the live GoTrue policy of 12.** FULLY FIXED on branch `feature/usage-billing-auth` (second commit): `ResetPassword.tsx`, `ManageClients.tsx` (update-password + create-user forms + placeholder + button gate), `CreateClient.tsx` create-login, and the `update-client-password` edge fn server check all now enforce 12. Council research note: GoTrue's ADMIN update endpoint DOES enforce the policy (so the old 6-char client check just produced confusing server errors), but admin CREATE bypasses it — which is why the client-side 12 checks matter. → live E2E in TEST_LIST (F14). Move to COMPLETED_LOG after the live pass.
- [ ] **RLS-SHAPE-1 (found in the 2026-07-02 full re-audit; WATCH) — `sms_delivery_events` agency SELECT policy lacks the `get_user_role(...)='agency'` gate (the F8 trap shape).** The policy scopes by `client_id -> clients -> agency_id`, but every client IS its own agency, so a client-role JWT matches it and could read its own delivery events directly. Not cross-tenant, and there are ZERO browser reads of this table today (F13 usage counting runs service-role inside `get-client-usage`), so this is a watch item: add the role gate the next time a migration touches this table. Severity Low.
- [ ] **BOOK-2 (found in Session 7 phone-half, 2026-06-30; SHARED-FN, defer to a SUPERVISED daytime session) — `resolveCanonicalSlot` off-grid-minute false-negative.** `voice-booking-tools/index.ts` `resolveCanonicalSlot` (~line 212-228) deliberately ignores the model's tz offset and matches **wall-clock HH:MM** against GHL's own slot grid — this is a **load-bearing, live-proven** fix (same slot books with +10:00, 400s without; do NOT change the offset handling). The narrow defect: it requires an **exact** HH:MM-vs-grid match, so a minute off GHL's :00/:30 grid → `canonical=null` → `buildSlotUnavailable` → false "isn't available". **Fix (eventual):** snap to the nearest real grid slot, or return `slot_unavailable` with real alternatives — NEVER loosen to fuzzy matching, NEVER POST an unvalidated time. **`voice-booking-tools` is SHARED with the live Voice path and has ZERO tests** → the overnight Text-Setter session writes this up + adds a characterization test; the actual edit is for a supervised daytime session (it would also change live Voice booking + bump the frozen Session-7 edge baseline). Severity Low (did not fire in BOOK-1; booking path was never reached).
- [ ] **BOOK-3 (found in Session 7 phone-half, 2026-06-30; SHARED-FN, defer) — `toMs()` parses offset-less ISO as UTC for the free-slots window.** `voice-booking-tools/index.ts` `toMs()` (~line 445-451) does `new Date(s).getTime()` on the model's ISO; `setterTools.ts:46` tells the model to send an **offset-less** ISO (e.g. `2026-07-01T00:00:00`), which V8/Deno parses as **UTC** → an Australia/Sydney lead's `get-available-slots` window shifts ~10h and the local day can be off by one → sparse/empty results for the day the lead asked about. **Fix (eventual):** interpret offset-less ISO in `client.timezone` (or have the model pass `startDate`/`endDate` epoch ms). Same shared-fn / frozen-baseline caveat as BOOK-2 → overnight session writes it up; edit is supervised-daytime. Severity Low.
- [~] **PHONE-CLEAR-1 (found in Session 7 phone-half, 2026-06-30) — clearing a lead's phone does not clear `normalized_phone`.** Clearing the phone on a lead in the BFD UI correctly nulls `leads.phone` **and** the GHL contact phone (B-3 verified live on the "Hayden" lead), but `leads.normalized_phone` retains the old `+61…` value → the lead is still inbound-/STOP-matchable by the old number (by-phone resolution keys off `normalized_phone` since the Session-5 pivot). **Fix:** recompute/clear `normalized_phone` when the phone is cleared or changed (the save/update path should derive it from the new phone). Severity Low.

- [ ] **G3-7 (found in Session 2) vite dev-server advisories need a major bump.** `npm audit` (frontend) flags vite ≤6.4.2: path-traversal in optimized-deps `.map` handling (GHSA-4w7w-66w2-5vf9, high) + `server.fs.deny` bypass on Windows + launch-editor NTLM disclosure. All **dev-server only** (prod ships a static build). Fix = bump vite 5.4 → 7/8 (a breaking change), its own session. Also pre-existing moderate advisories on `dompurify` and `tar` (the latter via the `supabase` optional dep) are `npm audit fix`-able. Effort M (vite is breaking).
- [~] **G3-8 (found in Session 6) residual secret-to-webhook forwards + dead secret-edit code.** Out of the explicit G3-6 scope and left for a follow-up because dropping the key would change live/legacy behaviour, not just harden a read: (a) **`LeadRow.tsx`** still reads `supabase_service_key` (via a `campaigns→clients` join) and forwards it in the **database-reactivation webhook** payload — this is a live in-project feature, so it needs a server-side reactivation path (e.g. route via the existing `reactivate-lead` edge fn) before the key can be dropped; (b) **`PresentationAgentChatInterface.tsx` + `WebinarPresentationAgentChatInterface.tsx`** read `openrouter_api_key` and POST it to a **hardcoded cross-project n8n URL** (`n8n-1prompt.99players.com`) — a genuine cross-project secret leak, but the feature (webinar presentation agent) is legacy/likely-dormant in bfd; confirm dead → delete, else migrate to an edge fn. ~~Also **dead code** that still references secret columns but is never rendered/routed: `pages/ApiManagement.tsx`, `components/SupabaseConfigCard.tsx`, `components/RefreshCostDialog.tsx`.~~ **(c) DONE — deleted in Session 6.5 (2026-06-26), App.tsx lazy import removed, build green → `TEST_LIST.md`.** **(b) DONE — 2026-06-29 (overnight frontend-only build):** `PresentationAgentChatInterface.tsx` + `WebinarPresentationAgentChatInterface.tsx` confirmed dead (imported only by 2 unrouted `_archived` pages; no live route) and the whole 4-file cluster (both components + `_archived/PresentationAgent.tsx` + `_archived/WebinarPresentationAgent.tsx`) was deleted, closing the cross-project `openrouter_api_key`→hardcoded-n8n forward; tsc + build green. Remaining open: **(a)** LeadRow service-key webhook forward (needs a server-side reactivation path before the key can be dropped). Effort S. _(NB pre-existing orphan left in place: `ClientLayout.tsx:618` still has a `presentation_only_mode` redirect to `/webinar-presentation-agent`, a route that no longer exists — harmless dead branch, out of scope.)_

---

> Shipped in **Session 6 (2026-06-26, secret-read hardening)** → moved to `TEST_LIST.md`: **G3-6**. Defense-in-depth (no live exposure: secrets were already RLS-scoped + the `clients_public` boundary shipped). No migration (the view + all 13 `has_<col>` booleans already existed). **Tier 1** (mechanical, zero behaviour change): central hook `useClientCredentials` reads `clients_public` + `has_*` (13 secret VALUE columns dropped; secret writes still hit `clients`, optimistic cache only tracks `has_*`); presence-only readers repointed (Dashboard, CampaignCreate, KnowledgeBase, AnalyticsV2, PromptManagement direct reads, Chats/Contacts/ContactDetail/SupabaseUsage/VoiceAIRepSetup via the hook; `ContactConversationHistory` prop → boolean); the 3 dormant (env-unset) n8n webhook chat interfaces (EmbeddedPromptChat, PromptChatInterface, AnalyticsChatInterface) stop reading the secret + drop it from the payload; secret-EDIT pages **ApiCredentials** + **SetupGuideDialog** made write-only (blank input = "unchanged", `has_<col>`-driven "Configured" indicator, blank-save guard so a blank box can't NULL a stored secret). **Tier 2** (deploy + network-tab verify): new **get-openrouter-usage** fn (kills the 3 direct browser→OpenRouter calls in `useOpenRouterUsage`); **analyze-metric** + **analytics-v2-suggest-widgets** flipped to read `openrouter_api_key` server-side (callers send `client_id`; the latter also gained auth + `client_id`); ChatAnalytics' in-browser OpenRouter "similarity" dedup dropped (exact-name dedup kept). **Tier 3** (live-behaviour, live re-test owed): the 2 in-browser external-Supabase `createClient` reads removed — **Contacts** → `fetch-thread-previews` (messages_per_session=1), **ChatAnalytics** time-series → `get-chat-history` new `mode:'range'` branch; service-key reads dropped from ChatAnalytics. tsc/build/deno all green; analyze-metric **v18**, analytics-v2-suggest-widgets **v14**, get-openrouter-usage **v1**, get-chat-history **v7** ACTIVE. New **G3-8** logged for the residual webhook-forwards + dead code left out of scope (see Low section).
>
> Shipped in **Session 5 (2026-06-26, by-phone pivot)** → moved to `TEST_LIST.md`: **B-2**. Verify-first found most of B-2 already live from Spec 1 (STOP fully internal + by-phone, zero GHL; inbound already internal-first via `resolveLeadByPhone`; `normalized_phone` set on the main create paths). The residual GHL `findOrCreateGhlContact` fallback on the inbound miss (only `receive-twilio-sms`) was made **(1a) deterministic** (filter exact-phone matches → most-recently-updated survivor, not first-match) and **(1b/1c) resilient**: a GHL outage no longer drops the reply (REL-03) — it mints a deterministic internal lead `bfd-<normalized_phone>` (reply flows Twilio-direct), logs `ghl_contact_resolve_degraded`, and a background `EdgeRuntime.waitUntil` reconcile repoints the synthetic row to the real GHL id (bounded child set: leads/message_queue/dm_executions/active_trigger_runs) behind a UNIQUE-collision guard. Also fixed the **CSV-import gap**: `process-lead-file` now sets `normalized_phone` via the shared helper + a one-time idempotent backfill migration (`20260627120000`). receive-twilio-sms **v29**, process-lead-file **v14**, migration applied. Server-side verified (column/index present, `rows_missing_norm=0`, no dup `bfd-%` rows); no Trigger redeploy (`trigger/*` unchanged). Deferred (unchanged): the full Spec-2 N-row merge.
>
> Shipped in **Session 3.1 (2026-06-26, F2b inbound-toggle hotfix)** → moved to `TEST_LIST.md`: **B-6**. Read-only diagnosis confirmed the persistence path already works on the live build (live DB: slot 8 "Inbound BFD Agent" `is_inbound=true`, `clients.retell_inbound_agent_id=agent_b2f6495…`, Retell `+61481614530` inbound→`agent_b2f6495…` — the whole chain was correctly bound; Brendan's original failure was testing mid-Railway-deploy on the old build). Real bug = **split-brain list badges**: they read `prompts.is_active` ("Not Active") + `prompts.directions` ([]) instead of the SoT `voice_setters.is_inbound`. Fix: list badges now read `voice_setters.is_inbound` (uncached) → inbound setter shows green **"Inbound" + "Bound"** (or amber **"Inbound · rebind"** if `clients.retell_inbound_agent_id` doesn't match), not red "Not Active"; toggle made non-silent + race-safe (`disabled` while writing + `.select('id')` 0-row detection in `useSetInboundSetter`). Frontend-only; tsc + build green; **no edge deploy**.
>
> Shipped in **Session 2 (2026-06-25, security/quality sweep)**: **G3-1** was already fixed in `49a594e` (both `voice-booking-tools` + `kb-ingest` fail-closed on NULL `intake_lead_secret`) → `COMPLETED_LOG.md`. **G3-2** shared-master-agent disambiguation (match `ghl_account_id`→`ghl_location_id`, log ambiguous), **G3-3** mandatory `active_call_id` bind (refuse stamp when `call_id` missing), **G3-4** real 400/502 status codes + 2 UI callers read `error.context`, **G3-5** esbuild forced to 0.25.12 (override), **types.ts drift** 5 columns added to live `clients` + `clients_public` (security boundary preserved) → all to `TEST_LIST.md`. retell-call-webhook v21, test-external-supabase v17.
>
> Shipped in **Session 1 (2026-06-25, voice reliability)** → moved to `TEST_LIST.md` (need Brendan's re-Save + live verify): **B-1** setter-rename cascade, **B-3** outbound auto-follows `latest_published`, **B-5** default-vars net (root cause found: the field is LLM-level, not agent-level — the v43 agent-level set was a silent no-op; now on `llmPayload`, verified end-to-end on a throwaway agent). retell-proxy v45, duplicate-setter-config v8.
>
> Closed in the 2026-06-25 reconciliation: inbound neutral greeting, Trigger latency, 6.8 `{{first_name}}`, F10 key rotation, 6.13 GHL secret-field check — see `Docs/archive/COMPLETED_LOG.md`. Prior shipped clusters (audit waves, billing B1/B2, session-1 hardening, S6, clients_public boundary) are in `Docs/ROADMAP.md` + the dated handoffs.
