# BFD-Setter — Consolidated Bug List (single source of truth)

Canonical list of **open bugs / issues that need addressing.** Consolidated 2026-06-19 from `FEATURE_ROADMAP.md` §6, `User Todos.md`, the 2026-06-19 live-E2E session, and `Docs/AUDIT_2026-06-10_full-system-audit.md`.

- **Feature roadmap** (what to build next) lives in `FEATURE_ROADMAP.md` — not here.
- Status: `[ ]` open · `[x]` fixed/verified · `[B]` needs a Brendan decision/input · `[~]` partially fixed.
- Last updated: 2026-06-24 (Billing: B2 Stripe cluster + B1 server-side gate (dormant) shipped — see the dated block below).

---

## 2026-06-23 audit sweep — shipped this session (branch `fix/audit-sweep-2026-06-23`)

A fresh adversarially-verified system audit (32 confirmed, 6 refuted) drove these fixes, all deployed live + boot-probed:

**Frontend (`d2035a1`, Railway on push):** RetroLoader infinite-hang on null role / client-without-client_id (`RedirectToFirstClient` terminal else); campaign dashboard always-401 (sent anon key not user JWT → `CampaignDashboard` now sends `session.access_token`); agency "Create Sub-Account" left the client non-bookable (omitted `intake_lead_secret` → now mints it + `ghl_webhook_secret`); AttendeeAvatar object-URL leak (revoke cleanup).

**Edge fns (`222577f`):** see S3b-4, S3b-6, S2b-5, S2b-10 below + create-client-user rollback on failed profile/role update (no privilege-defaulted orphan); compute-analytics custom-range last-day drop; sync-external-contacts missing `normalized_phone`; intake-lead double-enroll guard; double GHL note when `ghl_conversation_provider_id` NULL (legacy note + `pushCallEventToGhl` fallback made mutually exclusive).

**Trigger (`a57a510`, `20260623.2`):** see S4-4, S3a-3, S3a-6 below.

**Migrations applied live:** `20260623140000_processed_webhook_sids` (Twilio replay dedup), `20260623150000_drip_position_idempotency`.

**Session-1 hardening SHIPPED 2026-06-24** (branch `fix/session1-hardening-2026-06-24`, deployed live):
**B3** receive-dm-webhook now fails CLOSED — client lookup + webhook-auth hoisted ahead of all side-effects; a NULL `ghl_webhook_secret` 403s unless `DM_WEBHOOK_REQUIRE_SECRET=false` (kill-switch). receive-dm-webhook v18. **B4** sendFollowup + processMessages send-idempotency via the new `outbound_send_claims` table (claim-before-send, release-on-fail); Trigger `20260624.1`. **B5/S6-5** ApiCredentials route wrapped in AgencyRoute. **S2b-5/E** campaign-enroll-webhook per-token rate-limit (`webhook_rate_limits` + `bump_rate_limit`, 429 over `ENROLL_RATE_LIMIT_PER_MIN`=60) + `normalized_phone` dedup (v13). See those rows below.

**Still planned (NEW audit findings, NOT yet built — need a Brendan nod):** B5/S1-1 secret columns to browser (architectural). **Top features to build:** S6-3 CI/test runner, S6-2 readiness dashboard, S6-4 onboarding automation.

---

## 2026-06-24 Billing — B2 Stripe cluster + B1 server-side gate SHIPPED (branch `fix/billing-b2-b1-2026-06-24`)

Stripe is NOT live for BFD yet (`STRIPE_SECRET_KEY`/`STRIPE_WEBHOOK_SECRET` unset, verified) and `subscription_status` is managed manually, so the entire change set is zero-behavior-change today and activates at go-live. All deployed live + deno-type-checked.

- [x] **B2.1 — stripe-webhook event idempotency.** New `stripe_webhook_events` table (event_id PK, RLS on / no policies; migration `20260624140000`, applied live). Claim the event id right after signature verify; a 23505 → `200 {duplicate:true}` without reprocessing. Handler body wrapped so a throw DELETEs the claim before the 500 (Stripe's retry is processed, not dedup-swallowed). Protects the non-idempotent `invoice.payment_failed` retry_count increment. **stripe-webhook v17.**
- [x] **B2.2 — cancel_at_period_end immediate-lock.** A pending cancel (status still active/trialing) was OR'd into the terminal-cancel branch (webhook) + short-circuited to 'cancelled' (check-sub `resolveSubStatus`), so the gate paywalled a user who'd paid through period end. Now: terminal branch fires only on canceled/unpaid; the pending-cancel keeps 'active' + stamps `subscription_end_date`; the real cancel lands at period end via `customer.subscription.deleted`. Coupled basil-API fix: `current_period_end` lives on subscription items under the pinned apiVersion (confirmed TS2339), so it was silently storing null — added `subPeriodEnd()` reading `items.data[0].current_period_end`. **stripe-webhook v17 + check-client-subscription v16.**
- [x] **B2.3 — check-client-subscription wrong-client activation.** Step-3 `fallbackCandidates` matched unbound paid sessions by `metadata.user_id`/email → an agency user owning many clients could activate the wrong client. Restricted to client-BOUND evidence: Step 2 (email) now requires the sub's `metadata.client_id === client_id`; Step 3 keeps only the exact `client_reference_id`/`metadata.client_id` match (fallback dropped). **check-client-subscription v16.**
- [x] **B2.4 — stripe-checkout customer-by-email.** Reused the first email-matched customer without binding/persisting it (shared email → cross-entity mixing; no `metadata.client_id` → forced downstream fuzzy matching). Now: prefer the client's stored `stripe_customer_id`; else email lookup that reuses only an unbound/same-client customer (stamps metadata) or creates fresh for a different-client collision; persist the resolved id back to the clients row (makes B2.3 Step-1 deterministic). Drive-by: catch block `error.message` on unknown → `instanceof Error` guard. **stripe-checkout v16.**
- [x] **B1 — server-side subscription gate (SHIPPED DORMANT).** Frontend SubscriptionGate was cosmetic-only (billing bypass). New `_shared/assertActiveSubscription.ts` wired into the billable leaves: `make-retell-outbound-call` (v26, the outbound-voice choke point), `twilio-send-sms` (v13), `campaign-enroll-webhook` (v14), `intake-lead` (v12). **No-op unless `ENFORCE_SUBSCRIPTION_GATE="true"` (env absent today, verified).** Exemptions mirror the frontend useSubscription: `is_system` clients (probe) + the globally-oldest client; passing statuses `active`/`grace_period`; throws 402. **Go-live order:** backfill real clients to active/grace_period → confirm `is_system` on the probe + 'active' on the default/oldest client → set `ENFORCE_SUBSCRIPTION_GATE=true` in the Supabase edge-fn env.

---

## 🔴 Critical / High priority

- [ ] **6.11 — Missed/voicemail call → fallback SMS ~10 min late.** Voicemail/no-answer calls don't get `last_call_outcome` stamped on `engagement_executions`, so `runEngagement.waitForCallOutcome` polls its full 600s ceiling before advancing. Answered calls stamp `human_pickup` + advance promptly (verified). Voicemail/no-answer only. Fix: have the call-ended/analysis webhook stamp `last_call_outcome` for non-pickup outcomes too. **Effort M. Speed-to-lead impact.** (Confirmed live 2026-06-19.) **→ Assigned to the GHL-sync session (shares `retell-call-analysis-webhook` with 6.12); the `runEngagement` `active_call_id`-clear-on-timeout part stays with the feature session.**
- [ ] **6.12 — 🔴 GHL sync incomplete: SMS not in the Conversations tab + call/SMS outcome variables not written to GHL.** Flagged by Brendan 2026-06-19. **(a)** SMS messages don't appear in the **GHL Conversations tab** — the mirror (`_shared/ghl-conversations.ts` `pushSmsToGhl`/`pushCallEventToGhl`) needs `ghl_conversation_provider_id` (NULL; can't be API-provisioned — requires a GHL **marketplace-app custom conversation provider**), so it falls back to a note or nothing. **(b)** Call + SMS **outcome variables aren't updating GHL contact fields** — only `sentiment` + `appt-booked` are wired; the rest of the outcome suite (Call Outcome, AI Call Summary, Call Intent, Last Call Date, status…) isn't written, and SMS conversations write NO fields at all. Needs deep research (what fields, WHEN, WHERE from: SMS-as-they-happen vs Retell end-of-call report) then build. **Owns `retell-call-analysis-webhook` + `_shared/ghl-conversations.ts` + `push-contact-to-ghl`. Effort L. [B]** marketplace-app conversation provider. See `Docs/GHL_CUSTOM_FIELDS_HITLIST.md` for the field map.
- [ ] **6.13 — 🔴 SECURITY: GHL custom fields named "Supabase Service Role Key" + "Supabase Project URL" exist on the location.** Storing a service-role key in a CRM custom field would expose it to anyone with GHL access. VERIFY whether they hold real values (read-only) and, regardless, **delete the field definitions** (secrets belong in the server vault / clients row, never GHL). Found in the 2026-06-19 custom-fields audit (`Docs/GHL_CUSTOM_FIELDS_HITLIST.md`). **[B]** Effort S.
- [x] **6.10 — 🟡 `sync-ghl-contact` creates leads with `normalized_phone = NULL` (latent).** ✅ CODE-COMPLETE on branch `fix/bug-sweep-2026-06-19` (`75aee37`): `_shared/lead-insert.ts buildLeadInsert()` sets `normalized_phone` on both `sync-ghl-contact` + `campaign-enroll-webhook` inserts; +Deno test. Pending edge-fn deploy. The GHL-contact intake path inserts the leads row without `normalized_phone`; `resolveLeadByPhone` matches only on it. Currently MASKED by the GHL fallback + allow-dup-off (first inbound self-heals it; no dupe observed). Still fix: leaves a null window + blocks §6.5 internal-only. Fix = add `normalized_phone: normalizePhone(phone)` to the `sync-ghl-contact` insert (~L512) + audit other create paths. **Effort S.** (Found live 2026-06-19.)
- [ ] **6.5 — STOP + inbound resolution should be internal + by-phone (drop GHL lookup).** (a) STOP should stop ALL leads sharing a phone, handled in BFD; (b) inbound (`receive-twilio-sms`) resolves via GHL `/contacts/search` (first phone match, non-deterministic for shared numbers). Pivot to internal by-phone dedupe; no GHL round-trip. Spec-1 partly addresses this; 6.10's fix is a prerequisite. **Effort L. [B]** confirm internal-first model.
- [x] **6.6 — Retell inbound webhook 403s when `retell_webhook_secret` is armed.** ✅ ADDRESSED (test+doc, Brendan's call) on `fix/bug-sweep-2026-06-19` (`68e16c8`): Retell signs inbound with the SAME scheme; `verify-webhook.test.ts` locks it (8 cases). Root cause = the column must hold the **Retell API key**, not an arbitrary secret. No handler change. **[B]** still: arm via a single controlled live call, revert to NULL on any 403. Arming the secret makes `verifyRetellSignature` reject the inbound call → 403 → no `dynamic_variables` → agent speaks literal `{{first_name}}`. Inbound webhook is likely signed differently than post-call webhooks. **Do NOT arm `retell_webhook_secret`** until the scheme is confirmed. **Effort M. [B].**

## 🟠 Medium

- [ ] **Setter rename doesn't propagate across all name surfaces (card name vs Title vs slot label vs `voice_setters.name` vs Retell `agent_name`).** Renaming a setter binds in some places but not others, so one setter shows under conflicting names and the "Setter N" placeholder is never overwritten. Concrete evidence (2026-06-24, client `e467dabc`, the duplicated inbound setter in slot `Voice-Setter-8`): card heading = "Main Outbound V2" (auto-named by the duplicate), `prompts.name` = "Inbound BFD Agent" (the Title field), slot label / `prompts.slot_id` = "Setter-8", `voice_setters.name` = still "Voice Setter 8" (stale, not re-synced by the duplicate), and the Retell `agent_name` is the old placeholder. Brendan typed "Main BFD Inbound" and it bound to none of them. WANT: one canonical rename that cascades atomically on save to ALL surfaces: the setter card name, the persona/Title, the slot placeholder label (overwrite "Setter N" / "Voice Setter N"), `voice_setters.name`, and the Retell `agent_name`. Likely touches the duplicate-setter flow, the rename handler in `PromptManagement.tsx`, the `retell-proxy` sync (should PATCH `agent_name`), and the `voice_setters` dual-write. Also note the duplicate landed the copy `is_active=false` (had to be activated manually). Reported by Brendan 2026-06-24. **Effort M. [B]** confirm the single source-of-truth name field.
- [x] **6.3 — Refactor 8 raw-`fetch` sites to `supabase.functions.invoke`.** ✅ CODE-COMPLETE on `fix/bug-sweep-2026-06-19` (`010a6e5` POST sites + `cbe3fe7` unipile GET/Blob); all 8 converted, `tsc --noEmit` 0 errors, 429-retry + Blob + GET-query preserved. Pending Railway deploy. They build the URL from `VITE_SUPABASE_PROJECT_ID`/`VITE_SUPABASE_PUBLISHABLE_KEY`; when unset → `https://undefined.supabase.co/...` → silent "Failed to fetch" (the 2026-06-17 manual-send outage class). Sites: manual-send, `twilioNumbers.ts`, InstagramDMs, ApiCredentials, EmailInbox, DemoPageContactChat, AttendeeAvatar/ProfileDialog, ContactConversationHistory. **Effort M.**
- [x] **6.4 — Lead field edits vs GHL re-sync overwrite.** ✅ VERIFIED + regression-locked on `fix/bug-sweep-2026-06-19` (`6abe831`): the identity-guard `buildExistingLeadUpdatePayload()` excludes `phone`, so a cleared phone stays cleared; `push-contact-to-ghl` pushes `null` (clears GHL too). Phone-removal cases added to `sync-identity-guard.test.ts`. No code change needed (already fixed); test-only. Original symptom: cleared fields (e.g. phone) reappear after Save because the GHL re-sync overwrites. **2026-06-19 (S4): the no-overwrite fix is VERIFIED for name fields** (BFD edit held vs a differing GHL value). The specific **cleared-field / phone-removal** case was NOT directly retested — verify it before closing. **[B]** confirm source-of-truth for cleared fields.
- [x] **6.7 — Synthetic-probe canary race v2 (~2/24 hourly pass).** ✅ CODE-COMPLETE on `fix/bug-sweep-2026-06-19` (`27ad516`): `trigger/_shared/probePoll.ts` polls Step 3 on a 60s deadline (within the 180s maxDuration; does NOT wait for `completed` since Step 4 cancels mid-cadence); +node test (4 cases, fake clock). Pending Trigger deploy. NOTE: probe still dark until PROBE_* env is set in Trigger prod (separate, operator). Fails "no outbound message_queue row after cadence ran" due to Trigger.dev start latency 45–82s; the check asserts too soon after `status='running'`. Poll on a longer deadline / wait for `status='completed'`. Probe also currently dark (missing PROBE_* env in Trigger prod). **Effort M.**
- [x] **6.1 — Sidebar "SYSTEM" section labels confusing + unconfigurable.** ✅ CODE-COMPLETE on `fix/bug-sweep-2026-06-19` (`caad021`), per Brendan's IA decision: "Sub-Account Config" removed from the sidebar; reach a sub-account's config by clicking it inside Manage Sub-Accounts (→ `/client/<id>/settings`, already param-scoped + agency-gated). Manage Sub-Accounts + My Account + Sign Out stay. `tsc` clean. Pending Railway deploy. "Sub-Account Settings" vs "Account Settings" vs "Manage Sub-Accounts" read as near-duplicates; block sits outside the configurable menu model. Rename + group; decide if it joins the catalog. Frontend-only. **[B]** confirm labels.
- [x] **6.2 — Verify admin + sub-account logins.** ✅ VERIFIED (read-only, 2026-06-19): `AuthProvider.fetchUserRole` role-resolution + redirect logic is sound by inspection (user_roles→role, profiles→client_id, least-privilege sign-out on missing role/client_id). Live data: `user_roles` = **1 agency, 0 client-role users** → the client path is a DATA GAP, not a code bug. No defect found; blocked only on (i) a provisioned client-role test account + (ii) a concrete symptom (none reported). No code change. Agency login verified 2026-06-14; the client-role (sub-account) path is untested (no client-role user exists yet). Provision a test client user + confirm RLS-scoped landing. **[B]** provide the exact failing behaviour if admin login is actually broken.

## 🟢 Low / report-only / deferred polish

- [ ] **6.8 — (report-only) Greeting opener doesn't use `{{first_name}}` on pickup.** Variable is available from the first utterance but the opening line omits it. Brendan adds it in the BFD setter UI (prompt content is report-only). 📍 LOCATED 2026-06-19: `frontend/src/data/bfdVoiceSetterPrompt.md:27` opener = `"Hey, this is Gary, I'm Brendan's AI assistant at Building Flow Digital. Just so you know, this call is being recorded for quality. What can I help you with?"`. Recommended: prefix `Hey {{first_name}}, …` (interpolates on outbound/callback; degrades to "Hey ," on inbound where the var is empty). NOT edited — Brendan applies in the BFD setter UI (live wording may differ from the repo file per the repo/live drift note).
- [ ] **New-setter seed prompt is the unconfigured "Joe's Diner" default.** Every new setter is born with the raw demo template (generic `[Company]` script + Joe's-Diner modules). Tools/model/structure are correct; the CONTENT needs a full setup-wizard + backend prompt-compiler reconfig. Deferred post-launch (Brendan 2026-06-19). (Memory: `project_create_setter_skips_wizard_bug`.)
- [x] **delete-setter leaves an orphaned `voice_setters` row.** Deleting a setter removes the Retell agent + clears the client slot, but the `voice_setters` DB row remains (points at the deleted agent). Fix: delete the row in the delete-setter handler. Found 2026-06-19. (Memory: `project_delete_setter_orphans_voice_setters_row_2026_06_19`.) ✅ CODE-COMPLETE on `fix/bug-sweep-2026-06-19` (`706ae9c`): `retell-proxy` `delete-voice-setter` now soft-deletes the row (`is_active:false` + null agent/llm pointers, keyed on `client_id`+`legacy_slot`, runs even when the slot is already null); +Deno test. Existing UI delete handler already calls it. Pending edge-fn deploy.

## 🔵 Capability gap (tracked as a feature)

- [ ] **6.9 — SMS/text engine is reply-only (no booking/callback/appointment tools).** Behind the MAJOR feature **`FEATURE_ROADMAP.md` §3.12 (SMS tool parity)** — build that to close it.

---

## 🔭 Audit-sourced (2026-06-10 full-system audit → reconciled 2026-06-19)

The 2026-06-10 full-system audit (62 findings) was **reconciled finding-by-finding against current code on 2026-06-19**: **34 FIXED · 11 still-open · 14 partial · 3 moot**. The big IDOR/RLS/data-integrity/dependency waves are FIXED (`06425c3` + `47b97d5` + later build clusters — see memories `project_audit_fixes_deploy_2026_06_10`, `project_audit_second_wave_2026_06_10`). Full per-finding ledger with citations: **`Docs/AUDIT_RECONCILIATION_2026-06-19.md`**. The genuinely-still-open items promoted from it are below, tagged with their audit id (`S…`).

### 🔴 High
- [B] **S1-1 — Frontend pulls client secret columns into the browser via `.select()` (F7 residual).** ~36 src files select `supabase_service_key`/`twilio_auth_token`/provider API keys straight to the browser; `clients` RLS is row-level not column-level (verified live), so any agency user can read every client's secrets via the network tab / XSS. Interim: change the presence-only checks (`RetellPhoneNumbersTab.tsx:63`, `RetellPhoneNumberSelector.tsx:147`) to a non-secret computed boolean. Proper: a `clients_public` view (or column GRANT) omitting secret columns + route key-needing flows behind edge fns. **Effort L. [B] architectural.**

### 🟠 Medium
- [ ] **S2b-4 — `voice-booking-tools` (+`kb-ingest`) fail-OPEN when `intake_lead_secret` is NULL.** Money/state tools (send-sms, book/cancel GHL appts, schedule-callback) run for any caller who knows the clientId UUID (`voice-booking-tools:120`, `kb-ingest:94`). Masked today (both live clients have the secret); a new client without one reopens it. Fix = fail-closed (return 401 when a money/state tool is requested and the secret is NULL). **Effort S.**
- [x] **S2b-5 — `campaign-enroll-webhook` authed only by a URL-query token.** ✅ DONE 2026-06-24 (`fix/session1-hardening-2026-06-24`, campaign-enroll-webhook v13): per-token rate-limit (new `webhook_rate_limits` table + `bump_rate_limit(bucket, window)` RPC; 429 over `ENROLL_RATE_LIMIT_PER_MIN`=60, keyed `enroll:<campaign_id>`, fail-open on RPC error) + `normalized_phone` 5-min dedup (`isPhoneRecentDuplicate`, +Deno test; mirrors `ghl-tag-webhook`, returns 200 `{skipped:"duplicate_phone"}`, lead row still written for audit). Header-first token landed earlier 2026-06-23 (`222577f`, v12). STILL OPEN (minor): retire the deprecated `?token=`/body fallback once GHL configs move to the header. Original: `enroll_webhook_token` with no HMAC/replay/rate-limit/dedup → anyone who learns the token could create leads + fire billable engagement. **Effort M.**
- [x] **S3a-3 — Quiet-hours/schedule gate bypassed for later channels in a multi-channel engage node.** ✅ FIXED 2026-06-23 (`a57a510`, Trigger `20260623.2`): re-gate via `enforceQuietHoursBeforeSend` after the inter-channel delay wait in the engage per-channel loop (`runEngagement.ts`). `enforceQuietHoursBeforeSend` runs only at node entry (`runEngagement.ts:962`); a channel with a delay crossing the boundary (SMS now + call +6h) sends inside quiet hours. Fix = re-gate at the top of each channel iteration after the inter-channel wait. **Effort S.** Compliance.
- [x] **S3b-4 — Outbound voice calls get an empty `{{chat_history}}` (query a non-existent `messages` table).** ✅ FIXED 2026-06-23 (`222577f`; make-retell-outbound-call v25 + outbound-call-processing v13): both `fetchChatHistory` repointed to the EXTERNAL client DB `chat_history` (session_id=lead, message JSON `{type,content}`), mapping human→user / ai→assistant; `supabase_url`/`supabase_service_key` added to the client SELECT. `fetchChatHistory` queried a bare `messages` table with no `role/body/lead_id`; errors were swallowed so the agent lost all prior SMS context on outbound calls. **Effort M.**
- [x] **S3b-6 — `voice-booking-tools` GHL contact fallback can pick the wrong contact.** ✅ FIXED 2026-06-23 (`222577f`, voice-booking-tools v22): both `tryQuery` helpers now pull `limit=5` and accept only a result whose normalized phone / lowercased email EXACTLY matches the search term. `resolveContactId`/`tryQuery` accepted `contacts[0]` from a fuzzy GHL substring query with no equality check. **Effort S.** Related 6.10/6.5.
- [x] **S4-4 — `nudgeColdReply` generates copy without business name / custom fields.** ✅ FIXED 2026-06-23 (`a57a510`, Trigger `20260623.2`): added `business_name, custom_fields` to the candidate SELECT. The candidate query SELECT omitted `business_name` + `custom_fields` (`nudgeColdReply.ts:76-77`) but the AI copy reads them → always undefined → silent personalization regression. **Effort S.**

### 🟢 Low / hardening
- [ ] **E-1 — `fetch-thread-previews` returns HTTP 500 (not graceful-empty) if an external client DB throws.** When the per-client external Supabase query *throws* (network/timeout/bad creds) rather than returning an `{error}` (which is already handled with a `continue`), the outer catch returns 500, which would break the Chats thread list. **LATENT, not live: the function has no caller** — the only repo reference is a stale comment in `Chats.tsx:535` (the page builds previews from `leads.last_message_preview` + `dm_executions` instead). Investigated 2026-06-24 (Session 1 item E); left unfixed per "fix only if a live defect". If/when it's wired up, wrap the per-chunk external query in try/catch and return 200 with empty previews. **Effort S.**
- [x] **S2b-10 — Twilio sig verify non-constant-time + no replay protection.** ✅ FIXED 2026-06-23 (`222577f`; receive-twilio-sms v28, twilio-status-webhook v8, twilio-inbound-sms v13): constant-time compare in all 3; MessageSid dedup added at the top of the STOP/START branch (new `processed_webhook_sids` table, migration `20260623140000`) so a signed replay can't re-bill the compliance SMS / re-toggle opt-out. **Effort S.**
- [ ] **S2b-11 — `retell-call-webhook` stamps the call outcome from a spoofable `agent_id` before the optional sig check.** Outcome stamp matches only `.eq('id', executionId)` (`:168-185`); a forged `call_ended` mis-routes a cadence (advance vs terminate). Code fix = require the execution's `active_call_id == call.call_id` before stamping. Distinct from arming the secret (**6.6**). **Effort S.**
- [x] **S3a-6 — Drip node not idempotent on Trigger retry.** ✅ FIXED 2026-06-23 (`a57a510`, Trigger `20260623.2`, migration `20260623150000`): `claim_drip_position` gained a `p_execution_id` param + a `drip_position_claims` side table; a repeat call for the same execution returns the original position instead of incrementing. `runEngagement` passes `execution_id`; stale "idempotent" comment corrected. **Effort M.**
- [ ] **S4-8 — `test-external-supabase` returns HTTP 200 on every validation/connection failure.** Inconsistent with the fleet (400/404/409/500); defeats status-based monitoring. Fix = 400 for input/validation, 502 for upstream-connection (keep the `success:false` body for the UI). **Effort S.**
- [ ] **S4-10 — `retell-call-webhook` picks `clients[0]` for a shared master agent.** No disambiguation when `agent_id` maps to >1 client (`index.ts:67-78`); latent at single tenant, cross-tenant integrity hazard as clients grow. Fix = disambiguate via `dynamicVars` (ghl_account_id / execution owner) + `error_logs` on ambiguous match. **Effort M.**
- [ ] **S5-4 — Edge functions have no committed Deno lockfile/import_map.** `aa9e72b` dropped+gitignored `frontend/deno.lock`; transitive `npm:`/`esm.sh` versions still float at deploy time. Fix = commit a `deno.lock` (+ optional import_map pinning supabase-js) for the edge-fn workspace. **Effort S.**
- [ ] **S5-7 — Transitive esbuild still 0.21.5 (dev-server SSRF GHSA-67mh-4wv8-2f99).** vite is bumped (5.4.21) but esbuild isn't; dev-server only (prod is a static build). Fix = override/resolution to esbuild ≥0.25, or accept-and-document. **Effort S.**
- [x] **S6-5 — `ApiCredentials` raw-secret page under `ClientRouteGuard` only, not an agency/creator guard.** ✅ DONE 2026-06-24 (`fix/session1-hardening-2026-06-24`): both routes (`credentials` + legacy `api/credentials`, `App.tsx:264/278`) now wrapped in `<AgencyRoute>` (same pattern as settings/manage-clients/create-client), which redirects a client-role user to their own dashboard. Defence-in-depth — agency-only RLS already scoped the data. **Effort S.**

### 🔵 Feature backlog (audit-sourced)
- [ ] **S6-2 — Per-client onboarding/health readiness dashboard.** `ClientManagement`/`ManageClients` render only display fields, never the ~15 provisioning-gating columns (GHL calendar/assignee, retell agents/key/phone, openrouter key, webhook secrets, `auto_engagement_workflow_id`, external supabase). Add a computed red/amber/green readiness column + detail panel (pure read). **Effort M.** Highest-leverage reseller "is this client live?" feature.
- [ ] **S6-3 — Wire a test runner + CI.** 11 money-path `*.test.ts` exist but no `test` script / vitest / `.github/workflows`. Add a root `test` script (`node --experimental-strip-types --test` over the existing `*.test.ts`) + one CI job; optional vitest for the frontend. **Effort S-M.**
- [ ] **S6-4 — Automate the manual onboarding wiring.** `onboard-client.mjs` never calls `twilio-configure-webhook` per provisioned number and never PATCHes the 5 deterministic Retell custom-tool URLs. Add an onboard step for both (confirm the Retell PATCH with Brendan; config not prompt content). **Effort M.**
