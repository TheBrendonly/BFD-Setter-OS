---
description: Phase 9 soak day 13 status — final formal check before Phase 10 unblocks. Auto-generated by scheduled remote agent.
---

# Phase 9 Soak Day 13 Status — 2026-05-13

## ⚠️ Credentials missing

The following environment variables were absent in the remote agent environment at check time. **All live data checks were skipped.** Brendan must resolve credentials and re-run (or run the queries manually) before Phase 10 ships.

| Variable | Expected prefix | Status |
|---|---|---|
| `SUPABASE_PAT` | `sbp_` | ❌ Not set |
| `TRIGGER_PROD_API_KEY` | `tr_prod_` | ❌ Not set |

**How to re-run manually:**
- Set both env vars in the remote agent environment (check Railway / Supabase dashboard / Trigger.dev project settings).
- Re-trigger the soak agent for day 13, or run the six SQL queries from the prompt directly via `curl` against `https://api.supabase.com/v1/projects/bjgrgbgykvjrsuwwruoh/database/query`.
- Alternatively, run the three quick queries from `User Todos.md §A8` manually and paste results into this file before merging.

---

## TL;DR

⚠️ **WARNING — hold Phase 10, investigate first**
Both credentials were missing; zero live signals could be verified. Soak context and CHANGES_LOG review are clean (two post-cutover hotfixes were infra-level, not native-engine regressions), but the primary signal — `error_logs` for `process-setter-reply` / `process-messages` over the full 14d window — was not readable. Cannot issue a GO without that query.

---

## Soak summary (14 days post cutover)

| Metric | Value |
|---|---|
| Days into 14-day soak | 13 |
| Native engine errors (full 14d window) | ❓ N/A — SUPABASE_PAT missing |
| Inbound SMS (14d / 7d / 24h) | ❓ N/A — SUPABASE_PAT missing |
| Most recent inbound | ❓ N/A — SUPABASE_PAT missing |
| BFD use_native_text_engine | ❓ N/A — SUPABASE_PAT missing |
| BFD dm_enabled | ❓ N/A — SUPABASE_PAT missing |
| Reply % avg over last 7 days | ❓ N/A — SUPABASE_PAT missing |
| FAILED Trigger.dev runs over 14d (process-messages) | ❓ N/A — TRIGGER_PROD_API_KEY missing |
| FAILED Trigger.dev runs over 14d (process-setter-reply) | ❓ N/A — TRIGGER_PROD_API_KEY missing |
| SMS undelivered/failed last 24h | ❓ N/A — SUPABASE_PAT missing |

---

## Trend vs day 7

No day-7 report exists at `Docs/SOAK_REPORTS/2026-05-07-day-7.md` — this is the first soak report generated. No trend baseline is available. Going forward, day-14 (final) report should be compared to this file once credentials are resolved and data is populated.

---

## Post-cutover context (from CHANGES_LOG.md — no credentials required)

The cutover landed at commit `a7b66f3` / tag `phase-9-bfd-native-cutover` on **2026-04-30**. Two hotfixes shipped immediately after:

### Hotfix 1 — `phase-9-hotfix-probe-collision` (`c59b572`, 2026-04-30)
- **Issue:** the synthetic probe client (`b0e4f199-…`) had BFD's `retell_phone_1 = '+61481614530'` duplicated onto it. `receive-twilio-sms` calls `.maybeSingle()` on `retell_phone_1` — with two matching rows it returned PGRST116 and silently bailed, dropping all BFD inbound SMS from the `message_queue` between the probe-provisioning time and the fix.
- **Fix:** `UPDATE clients SET retell_phone_1 = NULL WHERE id = 'b0e4f199-…'` (DB-only, instant).
- **Assessment:** Infrastructure gotcha, not a native-engine defect. The native engine (`processSetterReply.ts`) was never invoked on the dropped messages — the inbound gate (`receive-twilio-sms`) was the failure point.
- **Residual risk:** the duration of the silent-drop window is unknown without querying `message_queue`. **This is one of the queries that requires `SUPABASE_PAT` — Brendan should confirm no inbound messages fell into this gap window that produced a bad experience.**

### Hotfix 2 — `phase-9-hotfix-trigger-env` (`60753b9`, 2026-04-30)
- **Issue:** `SUPABASE_SERVICE_ROLE_KEY` was never set in Trigger.dev PROD env. Every task that calls `getMainSupabase()` (including `process-setter-reply` and `process-messages`) crashed at startup.
- **Fix:** Set the key via Trigger.dev Management API. Validated with a live BFD inbound SMS replay — native engine produced a correct Aussie-warm reply and Twilio delivered it at 2026-04-30 12:06:03 UTC.
- **Assessment:** Infrastructure mis-config corrected on day 0. The native engine has been running cleanly on real traffic since that timestamp. Days 1–13 of the soak window post-hotfix are the true soak data.

**Both hotfixes were infra-level, not native-engine logic failures. No CHANGES_LOG entry records a regression in reply quality, incorrect routing, or a rollback of `use_native_text_engine`.**

---

## Native-engine errors (Q1)

❌ Query not run — `SUPABASE_PAT` not set.

**Manual query to run:**
```sql
SELECT created_at, source, error_type, substring(error_message,1,300) AS msg
FROM error_logs
WHERE source IN ('process-setter-reply','process_setter_reply','process_messages')
  AND created_at > now() - interval '14 days'
ORDER BY created_at DESC LIMIT 100;
```
Expected result for Phase 10 GO: zero rows.

---

## FAILED Trigger.dev runs

❌ API not queried — `TRIGGER_PROD_API_KEY` not set.

**Manual check:** Trigger.dev console → Production → filter by task `process-messages` and `process-setter-reply` → set date range 2026-04-30 to 2026-05-13 → count FAILED status rows.

Expected result for Phase 10 GO: zero FAILED runs (or failures explainable by the two hotfix windows on day 0, which are already documented and resolved).

---

## Cadence funnel last 7 days (Q4)

❌ Query not run — `SUPABASE_PAT` not set.

**Manual query to run:**
```sql
SELECT day, leads_texted, leads_replied,
       round(100.0 * leads_replied / NULLIF(leads_texted,0), 1) AS reply_pct
FROM cadence_funnel
WHERE client_id='e467dabc-57ee-416c-8831-83ecd9c7c925'
  AND day > current_date - 7
ORDER BY day DESC;
```
Expected result for Phase 10 GO: `leads_texted > 0` on at least some days (confirms the soak is informative, not vacuous), and `reply_pct` not materially below pre-cutover baseline.

---

## SMS delivery health (Q5)

❌ Query not run — `SUPABASE_PAT` not set.

**Manual query to run:**
```sql
SELECT status, error_code, count(*)
FROM sms_delivery_events
WHERE received_at > now() - interval '24 hours'
GROUP BY 1,2 ORDER BY 3 DESC;
```
Expected result for Phase 10 GO: no spike in `failed` / `undelivered` rows relative to `delivered`.

---

## Probe results (Q6)

❌ Query not run — `SUPABASE_PAT` not set.

Note from CHANGES_LOG: synthetic probe env vars (`PROBE_ALERT_WEBHOOK_URL` and the probe client's `retell_phone_1`) were not fully configured as of the last session. Probe results table (`probe_results`) may be empty or only partially populated. This query is a secondary signal; the primary signal is Q1 (native-engine `error_logs`).

---

## Phase 10 readiness

**HOLD.** Both runtime credentials are missing — the primary signal (Q1 native-engine error count over 14d) and the Trigger.dev FAILED-run count are unverified. Phase 10 **must not ship** until Brendan (or the day-14 agent with valid credentials) confirms:

1. **Q1 returns zero rows** — no `error_logs` for `process-setter-reply` / `process_setter_reply` / `process_messages` over the 14-day window.
2. **Q2 confirms inbound traffic > 0** — the soak must be informative, not a 14-day idle period.
3. **Q3 confirms `use_native_text_engine = true`** still set on BFD client `e467dabc-57ee-416c-8831-83ecd9c7c925`.
4. **Trigger.dev FAILED count = 0** (or failures are exclusively from the day-0 hotfix window, already documented and resolved).

If all four checks pass, Phase 10 can ship:
- Delete the n8n branch in `trigger/processMessages.ts` lines 191–218 (the `else` block that calls `client.text_engine_webhook`).
- Drop `clients.text_engine_webhook` column (optional — harmless if left).
- Decommission the n8n service on Railway.

**Rollback SQL (if anything fails):**
```sql
UPDATE clients SET use_native_text_engine = false
WHERE id = 'e467dabc-57ee-416c-8831-83ecd9c7c925';
```

---

## RUNBOOK Phase 10 preconditions (from `Docs/RUNBOOK.md`)

| Precondition | Status |
|---|---|
| BFD running on `use_native_text_engine = true` for ≥ 14 days | ✅ Cutover was 2026-04-30; day 14 is 2026-05-14 |
| Zero regressions logged in CHANGES_LOG.md | ✅ Both post-cutover entries are infra hotfixes, not engine regressions |
| `voice-booking-tools` agent URL repointed + live booking tested | ✅ Closed in `phase-night-a3-closed-and-webhooks-doc` (2026-05-04) |
| `kb-ingest` tested with a real KB doc upload | ❓ No CHANGES_LOG entry confirms a live `kb-ingest` test — confirm manually |
| All clients (currently just BFD) flipped | ❓ Assumed true from Q3; unverifiable without SUPABASE_PAT |
| Zero native-engine errors over 14d (primary soak signal) | ❓ **Unverifiable — SUPABASE_PAT missing** |
| Inbound traffic > 0 (soak is informative) | ❓ **Unverifiable — SUPABASE_PAT missing** |
