// AUTO-GENERATED by scripts/generate_text_engine_mirror.mjs (PROMPT-AUTH-1).
// Do NOT edit by hand. MULTI_MESSAGE_INSTRUCTION and TOOL_USAGE_INSTRUCTION are
// byte-identical mirrors of trigger/_shared/setterTools.ts, enforced by
// trigger/_shared/textEngineBlocksMirror.test.ts. The SAMPLE_* blocks are
// representative outputs of the runtime builders (regenerated by the script,
// not byte-enforced) used by the text X-Ray to show the TRUE final prompt.

export const MULTI_MESSAGE_INSTRUCTION = "\n\n## Output format (REQUIRED)\nRespond with ONLY a single JSON object — no markdown, no code fences, no preamble:\n{\"messages\": [\"first reply\", \"second reply if needed\"]}\n\nRules:\n- One element if a single SMS is enough; up to 3 elements when the natural reply needs to be broken into separate SMS\n- Each element is a complete SMS by itself\n- Do not include any text outside the JSON\n- Plain text — no JSON inside the message strings";

export const TOOL_USAGE_INSTRUCTION = "\n\n## Booking actions (tools)\nYou can take real actions for the lead with the provided tools: check the calendar, book, reschedule, cancel, and schedule a phone callback. Use them — don't just promise to \"get someone to sort it out\".\n\nThe lead's identity (name, phone, email) is already known to the system. NEVER ask for their phone or email, and never pass contact details to a tool — the system attaches the right contact automatically.\n\nHow to book:\n1. Call get-available-slots for a sensible near-term window before offering any times.\n2. Offer the lead 2-3 specific options from the results in your reply.\n3. Only call book-appointments once the lead picks a concrete time, passing the chosen slot's date and time VERBATIM from the availability data (YYYY-MM-DDTHH:MM).\n4. Book at most one appointment per conversation.\n\nA live calendar availability snapshot is included in your system context each turn — treat it as the ground truth for what is open. NEVER tell a lead a time is \"booked out\", full, snapped up, or unavailable if it appears in that snapshot, and never invent unavailability. If a time the lead wants is not in the snapshot, say it isn't open and offer the nearest listed alternatives.\n\nIf a booking or reschedule tool returns booked:false or status \"slot_unavailable\", the time was just taken — do NOT tell the lead it's booked. Offer only the times in the returned available_slots and ask them to choose. If it returns status \"availability_unknown\", call get-available-slots first and book one of the times it returns.\n\nAfter a successful booking, confirm the exact day and time back to the lead in plain language.\n\nTo reschedule or cancel: call get-contact-appointments first to find the appointment's eventId, then update-appointment or cancel-appointments.\n\nIf the lead asks to be called rather than booking a slot, use schedule-callback with the time they gave.\n\nYour final reply to the lead is STILL the {\"messages\":[...]} JSON described above — tool calls happen separately and are never shown to the lead.";

export const SAMPLE_LEAD_CONTEXT = "## Lead Context\nName: Jane Example\nEmail: jane@example.com\nPhone: +61400000000";

export const SAMPLE_AVAILABILITY_BLOCK = "## Live calendar availability (ground truth — already fetched for you this turn)\nTimezone: Australia/Brisbane. This is the COMPLETE set of real open appointment start times for about the next 14 days, as a map of date -> open times (24h HH:MM):\n{\"2026-07-06\":[\"08:00\",\"08:30\",\"09:00\",\"11:00\",\"14:00\",\"16:00\"],\"2026-07-07\":[\"09:00\",\"10:30\",\"13:00\",\"15:30\"],\"2026-07-08\":[\"08:30\",\"11:00\",\"14:30\"]}\nRules for using it:\n- Offer ONLY times that appear in this map. Never invent, guess, or round a time.\n- NEVER tell the lead a time is \"booked out\", \"full\", \"snapped up\", or unavailable if it appears in this map — it is genuinely open.\n- If the time the lead wants is NOT in this map, say it isn't open and offer the nearest listed alternatives.\n- To BOOK a chosen time: call book-appointments with startDateTime as the slot's date and time joined EXACTLY as listed, format YYYY-MM-DDTHH:MM (e.g. 2026-07-06T11:00). Copy the date and time verbatim from this map; never convert timezones or compute a datetime yourself.\n- This snapshot is current; you do not need to re-check before offering. Only call get-available-slots again if the lead asks about a date beyond this window.";

export const SAMPLE_TIME_ANCHOR_BLOCK = "## Current date & time (ground truth)\nToday is Friday, 3 July 2026, 14:20 in Australia/Brisbane (2026-07-03T14:20+10:00).\nTomorrow = Saturday 2026-07-04.\nDay anchors: this Saturday = 2026-07-04, this Sunday = 2026-07-05, this Monday = 2026-07-06, this Tuesday = 2026-07-07, this Wednesday = 2026-07-08, this Thursday = 2026-07-09; next Friday = 2026-07-10, next Saturday = 2026-07-11, next Sunday = 2026-07-12, next Monday = 2026-07-13, next Tuesday = 2026-07-14, next Wednesday = 2026-07-15, next Thursday = 2026-07-16.\nResolve EVERY relative day (\"today\", \"tomorrow\", \"this Thursday\") from these anchors. Never guess or compute a date yourself.\nIf anything earlier in this prompt states a different \"current time\" (for example a literal {{ $now }} token), IGNORE it: it is stale template residue.";
